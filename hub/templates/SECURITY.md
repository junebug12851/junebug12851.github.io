# Security Policy

> Template — copy to the repo root as `SECURITY.md` and fill in `<PROJECT>` /
> the supported-versions line. Satisfies the OpenSSF Scorecard **Security-Policy**
> check. See `hub/standards/supply-chain-hardening.md`.

## Reporting a vulnerability

Please report security issues **privately** — do not open a public issue for a
suspected vulnerability.

- Use GitHub's **private vulnerability reporting** for this repo:
  **Security → Report a vulnerability** (Settings → Code security → enable
  *Private vulnerability reporting* if it isn't on).
- Or email **junehanabi@gmail.com** with details and, if possible, a reproduction.

You'll get an acknowledgement as soon as it's seen. Please allow a reasonable window
for a fix before any public disclosure.

## Supported versions

Only the latest released version of `<PROJECT>` is supported. Fixes ship in a new
release rather than as back-ports.

## Scope

This project runs client-side / self-hosted and stores nothing on a server on the
maintainer's behalf; describe here anything project-specific (e.g. "API keys stay on
the user's device", "no server relay"). Keep this section accurate to the code — see
the `legal-docs` standard for the same accuracy discipline.
