# ── Round-up "last seen" markers ─────────────────────────────────────────────
# Records the dev-branch commit each project was at when the most recent round-up
# pass ran, so the next pass knows where to resume. Updated by the cross-project
# round-up pass (see notes/reference/blogging-workflow.md). Git-only awareness;
# nothing here is a dependency.
#
# reports_through: the LIST of process-report filenames (notes/fairyfox-reports/)
# this hub has already digested for that project, so the report-review pass knows
# where to resume — it reads any report file NOT on the list. A filename list, not a
# bare date or a commit SHA: two reports can share a date (and a same-day follow-on
# can even sort lexically before its sibling), and a bare date or SHA is a fragile
# resume key, so the filename is the stable one. Updated (appended) by the review
# pass — see hub/standards/process-reports.md. [] = none digested yet.

pokered-save-editor-2:
  commit: b31286522b85d2301b27186754ba9a2876f85e34
  branch: dev
  seen: 2026-07-21
  reports_through:             # digested-report filenames (append-only); see header
    - 2026-06-26-adopting-updates.md             # folded into standards 0.9.1
    - 2026-06-26-adopting-updates-express-auth.md # folded into standards 0.9.3 (verification floor + this marker scheme)
    - 2026-06-26-check-for-updates.md            # digested 2026-06-28 report-review (0.9.8) — no-change confirming pass, nothing to fold
    - 2026-06-28-check-for-updates.md            # 2026-07-19 report-review — check-only, no-change confirming pass
    - 2026-06-29-check-for-updates.md            # 2026-07-19 report-review — check-only, no-change confirming pass
    - 2026-06-30-check-for-updates.md            # 2026-07-19 report-review — check-only, no-change confirming pass
    - 2026-07-01-check-for-updates.md            # 2026-07-19 report-review — check-only, no-change confirming pass
    - 2026-07-02-check-for-updates.md            # 2026-07-19 report-review — check-only, no-change confirming pass
    - 2026-07-06-adopting-updates.md             # 2026-07-19 report-review — run feedback (successful adopt), no standalone standard change
    - 2026-07-19-adopting-updates.md             # 2026-07-19 report-review — run feedback (successful adopt), no standalone standard change
  # 2026-07-21 round-up: clean fast-forward 5ae9dad3 → b3128652, 3 commits ALL dated 2026-07-20 (a complete
  # day — today is 07-21), VERSION unchanged at 0.43.6-alpha (docs/identity only, no engine change). A
  # de-personalization pass took the project's own name out of its copyright line, credits and first-person
  # notes (→ Fairy Fox attributions), the same neutral-voice discipline the site keeps, plus a typo fix in
  # the docs domain (→ pokeredsaveeditor2.fairyfox.io, correct TLD). Reconciled: projects.yml (PSE2 date →
  # Jul 20; version held at 0.43.6). Folded into the new 2026-07-20 day post. Marker → b3128652; seen →
  # 2026-07-21.
  # 2026-07-20 round-up: clean fast-forward 4d2ffc8c → 5ae9dad3, VERSION 0.43.4-alpha → 0.43.6-alpha, 14
  # commits ALL dated 2026-07-19 (a complete day — today is 07-20). Two strands. (1) The map reads a save's
  # real progress: 0.43.5 retires the "Custom (matches no researched state)" row — the app scores every
  # resting stage against dead-giveaway flags + missables + step byte and picks the closest (later stage wins
  # ties), and every raw script-table step is now an italic "Step N" entry; tst_map_states pins all 98
  # blueprints answering with a state. 0.43.6 gives all 228 filter flags researched lifecycles (five classes),
  # the state matcher reads a stage's own new giveaways (a played Pallet Town → "3. The journey underway"), an
  # "Idle (script parked)" rename, and — the visible fix — the canvas redraws a filter flag the moment it
  # changes. Plus a notes/tooling piece: the global progression graph (only 70 of 2,560 flags ever reset, five
  # classes, mandatory M0–M20 spine; new game-progression.md + miner). (2) Adopted the fairyfox shared-chrome
  # bundle (chrome 2.0.0 then 2.2.1 coins) replacing hand-built chrome, and ran a professional-tone doc scrub
  # across CLAUDE.md + notes (emoji/metaphor removal, first-person → role-based leadership). Reconciled:
  # projects.yml (PSE2 → 0.43.6 / Jul 19). Folded into the new 2026-07-19 day post. Marker → 5ae9dad3; seen →
  # 2026-07-20. (New reports 2026-07-19-adopting-updates-chrome-2.2.1.md + 2026-07-19-check-for-updates.md
  # left for the inbound report-review pass.)
  # 2026-07-19 round-up: clean fast-forward 1679d2f0 → 4d2ffc8c, VERSION 0.42.13-alpha → 0.43.4-alpha, 5
  # commits ALL dated 2026-07-18 (a complete day — today is 07-19). Map states went LIVE, then a consistency
  # overhaul of the map screen. (1) 0.43.0 — the 07-17 progression blueprints become a live picker: roll a map
  # fwd/back one story stage, build a proper destination on map change; "map script" renamed "map state" on the
  # screen. (2) 0.43.1–0.43.4 the map screen learns ONE language: one canonical ink table (three disagreeing
  # palettes collapsed, keyed to the Layers panel), a hidden sprite ALWAYS shown as a recognisable dimmer ghost
  # (silhouette served by the image provider so even the offscreen renderer can see it — "a fix you can't
  # capture is a fix you haven't verified"), ONE "useless edits" gate on the top toolbar (only values the game
  # overwrites/never reads), event flags grouped by stage, "Map Storage" panel renamed "World", outline-only
  # door/sign boxes. ctest 92/92 throughout. Reconciled: projects.yml (PSE2 → 0.43.4 / Jul 18). Folded into the
  # new 2026-07-18 day post. Marker → 4d2ffc8c; seen → 2026-07-19.
  # 2026-07-18 round-up: clean fast-forward cb8b1d79 → 1679d2f0, VERSION 0.41.8-alpha → 0.42.13-alpha, 37
  # commits ALL dated 2026-07-17 (a complete day — today is 07-18). The project's single biggest day yet, and
  # a day about naming/measuring the right thing. (1) MAP STATES as DATA (0.42.13) — every map's default states
  # per progression computed as 98 per-map JSON blueprints (196 resting stages, 34 story maps hand-curated off
  # pret's own scripts), so the app can roll a map fwd/back a stage, build a proper destination on map change,
  # and hand the randomiser whole legal states. Finding: a map's SCRIPT_* value is NOT its state (every gym
  # rests at the same script on both sides of its leader) — a stage = script byte + ABSOLUTE event set +
  # ABSOLUTE missable visibility + badges. pret has no format for this (executable asm only) → genuinely new
  # artefact, extractor is --check-idempotent; data-only, no app code, suite 91/91. Also renamed "map script" →
  # "map state" on the map screen. (2) THE 16f INTERACTION MODEL briefed — whose click wins when flag boxes
  # overlap draggable sprites/warps/signs: NOT any of the three invented-precedence candidates. Hover reaches
  # everything; click priority IS the Layers panel's own visible order; each layer's colour tab is a click-AND-
  # DRAG escape hatch. Reuses a rule already on screen + under user control (inspectable, re-orderable) and
  # retires the buried-object risk instead of mitigating it — the answer that adds the least. (3) THE UNITS:
  # there are THREE, and the middle was miscalled a "tile". Tile 8×8 (attributes), HALF-BLOCK 16×16 (the walk
  # grid: objects/warps/signs/hidden items+coins/coord triggers), Block 32×32 (.blk/shape/connections). The
  # game proves it (low bit of wYCoord = which half of the block). Boxes shipping were the right SIZE; the WORD
  # was wrong. Rename, don't resize — a self-contradicting notes sentence ("walk in half-blocks = 2×2 tiles")
  # retired. (4) ONE Qt EVERYWHERE (Fairy Fox chose to bring the shipping toolchain UP, not hold the app back):
  # kit == container == CI at 6.11.0. The Windows 6.11 install failure was a repo-layout reorg fixed in
  # aqtinstall PR #1000 (merged 4 months ago, never released) → pinned via aqtsource to the merge commit;
  # "the latest release can't" ≠ "can't". Twin lesson one layer down: the container's 3 emu-test "failures"
  # were a path-exists availability gate (checks a FILE, not whether the interpreter RUNS) tripping on host
  # scratch rsynced in → now runnability-based. (5) CI GREEN first time since 0.29.0 (0.41.9): with the
  # toolchain level + container fixed, the safety nets ran and found 4 real things — a heap-use-after-free in
  # the music parser (label by-ref into a growing list → append frees the caller's ref; ASan 7× from 5 tests,
  # never crashed, rode 12 releases), a null deref, a dead check hiding a missing bounds test, a needless copy.
  # Reconciled: projects.yml (PSE2 → 0.42.13 / Jul 17; blurb gains in-game trades + completion milestones).
  # Folded into the new 2026-07-17 day post. Marker → 1679d2f0; seen → 2026-07-18.
  # 2026-07-17 round-up: clean fast-forward 9d0fdbd4 → cb8b1d79, VERSION 0.40.4-alpha → 0.41.8-alpha, 29
  # commits ALL dated 2026-07-16 (a complete day — today is 07-17, tip cb8b1d79 is a 07-16 commit). The day
  # the project put its own confident claims in front of the cartridge and lost four of them. (1) ROUTE 22
  # REFUTED — the founding case of the Phase 11 conflicting-flags system (two SPRITE_BLUE objects on one
  # tile, both battle flags on "must" collide), recorded suspected-strong on 07-15, is WRONG:
  # Route22DefaultScript is an ordered if/else (1ST set → first battle, 2ND never read) = MASKED, not
  # conflicting; console-armed (WANTS+1ST, script=DEFAULT, rival shown, upper 29,4 trigger — geometry
  # matters) → clean trainer battle. Three rules banked: suspected is a LEAD not a risk (only confirmed
  # warns); refuted is KEPT (negative knowledge, generator defers via REFUTED_SUBJECTS); adjudicate on the
  # console. A real candidate fell out: route22-rival-armed-but-hidden (suspected/softlock). (2) "ALL FLAGS
  # ON CRASHES" UNREPRODUCED — all 2,560 set → healthy boot, Oak's Lab clean, ~12 map transitions healthy;
  # a slice not a proof, but the notes/plan corrected: mechanism real (bad script ptr → jp hl), all-on crash
  # UNVERIFIED; bulk-set warning kept but reworded to "unpredictable and unverified", never "this crashes".
  # (3) EVENT FLAGS UI shipped (0.41.7) — and reading the map dropdown found storagePages() never considered
  # events: 33 maps (Celadon, Lavender, Route 1, Fuchsia, Indigo Plateau, Mt Moon B1F, 5 S.S. Anne decks,
  # both elevators, ~20 more) had NO page → their flags unreachable. 108 → 141 pages. Descriptions took 3
  # passes (v1 leaked the research log, v2 vague) → v3 names the places; rule: a description must ADD what
  # the row doesn't say, telemetry is not context. Model verified byte-perfect (508/508 byte==0x29F3+ind/8)
  # but 14 bits MISLABELLED in the Pokémon Tower block (~2-bit shift; "Beat Pokemontower 7 Trainer 0" is
  # really EVENT_BEAT_GHOST_MAROWAK) → events.json regenerated from pret, 508 → 2,560. (4) DEV AUTOPILOT
  # (0.41.3–0.41.5) — pathfind + auto-navigate the real game over the new pokered-dev MCP; three same-day
  # briefs (navigation → progression: guards/elevators/surf/cut/spinners/bike/win-on-request → game-life:
  # train/menus/new game), natural drop-in the default, 16/16 console-verified. Traps: held direction = a
  # DOUBLE step; wCurMap updates FIRST in a transition; a warp square can be solid and still warp;
  # wXCoord/wYCoord update at step START (wWalkCounter==0 is the real signal). (5) CI RED SINCE 0.29.0
  # (0.41.8) — QImage::flipped is Qt 6.9+; kit is 6.11, CI pins 6.8.3, so it compiled locally every time and
  # never remotely. release.yml builds the same appcore on 6.8.3 and hadn't run since v0.16.6 — the pending
  # Events ship would have merged to main and failed to build the downloads. Guarded; the #else branch
  # force-compiled + tst_map run on it (27 passed) BEFORE the push. Finding: no local way to catch this class
  # — the remote is the project's only 6.8 compiler. Flagged for leadership: align dev/ship Qt, or keep
  # guarding. Also: map scripts (97) + missables (228) on the Map Storage panel, forged saves at ANY map
  # (the console authors them), the PyBoy wedge root-caused (chimera forge → STOP → tick() never returns;
  # kill the TREE). Reconciled: projects.yml (PSE2 → 0.41.8 / Jul 16; blurb gains per-map script progress +
  # the 2,560 event flags). Folded into the new 2026-07-16 day post. Marker → cb8b1d79; seen → 2026-07-17.
  # 2026-07-16 round-up: clean fast-forward ff38b460 → 9d0fdbd4, VERSION 0.39.0-alpha → 0.40.4-alpha, ~39
  # commits ALL dated 2026-07-15. (Marker parked at the last 07-15 commit 9d0fdbd4, NOT at HEAD 69e1ef16 —
  # that tip is a 07-16 test-wrapper commit, no VERSION bump, a day still in progress, left for the next
  # pass.) The day the Map screen got its last big object type AND opened the project's largest feature.
  # (1) CONNECTIONS (Phase 7, 0.39.1→0.39.8) — the four edge joins, the last object type the map couldn't
  # yet edit. Two human inputs (neighbour map + one signed offset); the other nine bytes macro-derived, so
  # editing derives them too. The neighbour renders FULL, bleeding off the edge, dragged to set the offset
  # (snaps to landmarks); add by ghost-arrow-pick or drag-a-map-on; rotation not representable → offer
  # re-home instead. The render is reframed as a CONTINUE-LOAD (ring drawn from the SAVE's own connection
  # bytes, edits included — the BIT_NO_PREVIOUS_MAP linchpin again); neighbour sprites shown static.
  # tst_connections 17. (2) WILD ENCOUNTERS panel (0.40.1) — research-first caught a real model bug: the
  # slot bytes are stored LEVEL-then-SPECIES, the model read species-first, inverting every save's table;
  # fixed as a model change + pinned before any UI. Plus a 0-based artwork +1 fix (box precedent). (3) Three
  # more state panels, each research→probe→model-fix→UI: AREA STATE (v1's "Map" page fields — nothing lost,
  # a UI just never built; curMapScript + its auto-run bit are one feature in two bytes), CHARACTER STATE
  # (nine transient NPC flags, three v1 labels corrected, no hidden fields), MAP STORAGE (six gym/safari
  # bytes incl. a big-endian step word). Doctrine sharpened out loud: derived values KEPT IN SYNC BY DEFAULT
  # + raw-edit + deliberate break-sync; every panel marks which bytes the console keeps vs wipes on load.
  # (4) EVENTS — the largest feature yet BEGINS: name/classify all 2,560 wEventFlags. Count pinned to source
  # ($A00 = 2,560 bits = 320 bytes); 507 named, 30 block-swept, ~2,023 placeholders (alignment padding). An
  # earlier "flags can't crash" claim CORRECTED (bad script-pointer → jp hl → crash), so the plan warns
  # before any bulk-set. Groundwork only — no editor UI yet, on purpose. Plus a dev-harness upgrade (reach
  # anything by name/path, trigger anything, component-cropped shots) and a CI fix (qtshadertools genuinely
  # needed for the pixel-art shader; the reflex to drop it as unused was wrong, caught by local build).
  # ctest green throughout. Reconciled: projects.yml (PSE2 → 0.40.4 / Jul 15; blurb gains edge connections +
  # wild encounters). Folded into the new 2026-07-15 day post. Marker → 9d0fdbd4; seen → 2026-07-16.
  # 2026-07-15 round-up: clean fast-forward dc1f21b → ff38b460, 12 commits ALL dated 2026-07-14,
  # VERSION 0.34.0-alpha → 0.39.0-alpha. The map screen gained its object types — doors, signs, the player —
  # each preceded by console-verified research, plus a firm scope-control lesson. (1) WARPS (Phase 5, 0.36.0):
  # warps placeable/draggable/re-aimable on the sprite machinery; research against the cartridge OVERRULED one
  # field (wStatusFlags3's alias wCableClubDestinationMap is zeroed whole every Continue) and confirmed ten;
  # two v1 fields (From Warp/From Map) proven DEAD (written, never read anywhere in the disassembly); wLastMap
  # surfaced in the toolbar as [ Outside is: … ] (relabels every $FF door at once); two loaded guns (Fly/dungeon
  # dest — unbounded table lookups) offered only legal values, the old randomiser's illegal fills fixed pre-UI;
  # a cry-wolf ! caught in screenshot review (fires only when legal-false AND armed). (2) "ADJACENCY IS NOT A
  # BRIEF": signs had walked into the warps phase (same ROM block + shape) — CUT back out, and a new CLAUDE.md
  # rule written (a feature gets its own conversation → research → design → code; a phase can't absorb a
  # neighbour; a briefed feature that needs an unbriefed one READS it, doesn't build a UI for it) + "research
  # lands in the notes, every time". (3) SIGNS (Phase 6, 0.38.0), built on their own: save model already
  # correct (rare — no bug first); a self-validating importer extracts the real text from pret/pokered
  # ADDITIVELY (strip it → byte-identical), so a sign resolves to its real words on screen. (4) The PLAYER
  # details panel (Phase 5f, 0.39.0): all 26 map-state bytes, ten rewritten on load (facing forced DOWN, a
  # status byte zeroed, Strength reset unless battle-over, door/ledge/jump bits cleared), three dead —
  # gathered behind a Reloaded-values switch, filtered in the model; five misnamed fields corrected; a
  # movement byte shown cleared bit-by-bit not whole (the sprite-pass mistake avoided). Plus the tile-anim +
  # walk controls merged into one Simulation button/panel (0.37.2). ctest green throughout (tst_warps 24,
  # tst_signs 15, tst_player 7). Reconciled: projects.yml (PSE2 → 0.39.0 / Jul 14; blurb now notes doors +
  # signs + player state on the map view). Folded into the new 2026-07-14 day post. Marker → ff38b460;
  # seen → 2026-07-15.
  # 2026-07-14 round-up: clean fast-forward 0686b58 → dc1f21b, 20 commits ALL dated 2026-07-13,
  # VERSION 0.24.4-alpha → 0.34.0-alpha. One theme, and it is narrower than yesterday's oracle:
  # LOOK AT WHAT YOU ACTUALLY SHIPPED — five review passes in a day, each of which found that the
  # INSTRUMENT was blind, not only the code. (1) Every screenshot the map screen had been reviewed
  # with was TOFU BOXES (--shot never set QT_QPA_FONTDIR): the layout was reviewed, the screen was
  # not (0.28.0). With text on, the first honest look found two identical ▶ buttons, dead zoom
  # controls, and a menu off the bottom of the window. The zoom was then rebuilt as a shader (snap
  # to texel centre, one-screen-pixel ramp via fwidth) — which caught its own trap: ShaderEffect
  # does not run on the software backend every headless test uses, so the first capture came back
  # black; falls back to nearest. (2) A SPRITE RESEARCH pass (0.25.1) found four model bugs, three
  # proven on the console: SpriteMobility and SpriteGrass INVERTED, setTo() writing WALK for a STAY
  # sprite, and face/range being ONE byte scattered into two fields. Every fix negative-controlled.
  # It also overruled a confident source-reading: LoadMapHeader zeroes and reloads the sprite tables,
  # so sprite edits "must" be erased on boot — the cartridge, booted headless with three edited saves,
  # says the edits SURVIVE (the continue path sets BIT_CUR_MAP_LOADED_1, so the header is never
  # re-read). (3) The towns FILL: all 72 overworld sprites imported (the repo had carried only red.png),
  # the importer refusing its own first version because a third art shape exists — the 20 "still" people
  # with no walking art (0.26.0). Then the cast becomes editable with the mouse (0.27.0–0.31.0): drag in,
  # drag out to delete, and a details panel where every field declares a KIND and the kind picks the
  # control — the raw byte box appears ONLY when the combo cannot name the value. Two "warnings" deleted
  # as noise (a scratch byte the game zeroes; a ROM-vs-save cast diff that fires on every real save
  # because WALK sprites walk): a warning that always fires is a bug. (4) INDOORS THERE IS NO SPRITE SET
  # (0.32.0) — the game loads each character's own art, the cast IS the set, bounded only by VRAM (10
  # walking + 2 still slots); outdoors the NPC→VRAM lookup has NO BOUNDS CHECK and runs off the table into
  # WRAM, so "drawn as garbage" is literal. The editor had been asking the save's cached list (bytes the
  # game throws away). (5) The dev HARNESS could not send a real click — its `click` emitted a clicked()
  # SIGNAL, exercising none of the pointer-delivery machinery the bug was about; `tap` (a real QMouseEvent)
  # reproduced it first try: floating docks sit over the canvas and both TapHandlers fired (0.33.0). The
  # dead-and-squashed drop-downs were ONE binding loop. (6) The MUSIC now ships as the game's own SHEET
  # MUSIC (0.34.0): the importer always read .asm, but what SHIPPED was an invented bank image whose .asm
  # was not in the repo at all — 376 .asm vendored verbatim, parsed on first use (85 ms), the .bin demoted
  # to a test fixture (cartridge → importer → fixture → parser). Plus a claim CORRECTED: glitch tracks do
  # not need bytes — a header is 3 bytes PER CHANNEL and ids multiply by 3, so a misaligned id always lands
  # on a whole, well-formed entry (which is why it plays a clean bassline, not noise). Also: CI had been RED
  # on every push for a week (Qt Multimedia never installed after the music landed); with the gate open,
  # clang-tidy produced 9 findings it had been unable to see. Reconciled: projects.yml (PSE2 → 0.34.0 / Jul 13;
  # blurb notes the map's editable cast). Folded into the new 2026-07-13 day post. Marker → dc1f21b; seen → 2026-07-14.
  # 2026-07-13 round-up: clean fast-forward 93756b7 → 0686b58, 23 commits ALL dated 2026-07-11/12,
  # VERSION 0.16.6-alpha → 0.24.4-alpha — the project's single biggest day so far, and one long theme:
  # THE ORACLE. (1) The MAP is now genuinely emulated (0.17.0) — the game's own overworld pipeline
  # (wOverworldMap → wSurroundingTiles → wTileMap), not a picture of it — and a legally backed-up
  # cartridge booted headless in PyBoy became the judge (0.18.0, tst_emu_parity; the ROM is git-ignored,
  # refused by .gitignore anywhere in the tree, and every emulator case SKIPs without it). It matched on
  # four arrays and FAILED on the border ring, held as an explicit QEXPECT_FAIL. Connection strips closed
  # it (0.19.0): the `connection` macro re-derived from the cartridge, all 11 compiled bytes of all
  # 78 connections verified, zero mismatches; the ring is the neighbouring maps bleeding in, not a wall of
  # border block. (2) The SIX GLITCH PALETTES are real and render (0.20.0) — wMapPalOffset is SUBTRACTED
  # from a pointer into eight contiguous 3-byte fade palettes, so multiples of 3 give the four contrast
  # levels and everything else straddles two entries: exactly four and exactly six, verified 10/10 on the
  # console. Then the player is drawn (0.21.0) and the two "harmless" palettes stop being harmless (their
  # damage is to the OBJECT palettes). (3) MUSIC, end to end: imported from pret/pokered and lockstep-
  # verified against the cartridge (0.22.0); a DMG APU + a transliterated Gen 1 sequencer in C++
  # (`pse-audio`); then tst_sound_parity — the cartridge's own 256 bytes of engine state photographed EVERY
  # FRAME, all 46 tracks byte-for-byte (0.22.1). It immediately caught three bugs of *tidiness* (the port
  # had done the right thing where the game does the wrong one) and, best of all, caught the ORACLE ITSELF
  # being wrong (a map fade reads the new track ids while the OLD bank is still mapped). Glitch music turned
  # out not to exist: ids are computed from header addresses, so a 3-channel song occupies 3 ids and the
  # other two are its inner voices — 46 tracks + 105 inner voices, ZERO garbage ids, all 151 now in the
  # panel (0.23.0). (4) The map's MEANING layer (0.24.0) uncovered a SAVE-CORRUPTING bug: the collision
  # lists are SHARED in the cartridge (RedsHouse1/2 are one list), the v1 importer walked the chain one step
  # out of phase for three tilesets, and the editor WRITES that pointer — so a Poké Mart got a bedroom's
  # walls. Fixed in 3 lines, negative-controlled. (5) The Map screen overhaul: a chassis (0.24.2), a real
  # layer system (0.24.3), and ANIMATION (0.24.4) — writing which exposed two more invented facts (the water
  # ROTATES −1→+3, it does not slide 0→4; the flowers run 1,1,2,3, not 2,3,1,1). Reconciled: projects.yml
  # (PSE2 → 0.24.4 / Jul 12; blurb gains the emulated map + soundtrack). Folded into the new 2026-07-12 day
  # post. Marker → 0686b58; seen → 2026-07-13.
  # Covered through the project-side fairyfox-mesh onboarding (mesh-aware CLAUDE.md +
  # cross-project-sync adoption) and theming its Doxygen docs site to the fairyfox
  # design system (Case B — themed generator). Folded into the 2026-06-25 day post
  # (_posts/2026-06-25-nodes-that-check-the-hub-for-updates.md). Earlier static-analysis
  # gate / test push was blogged at _posts/2026-06-23-…. (dev was force-pushed; re-cloned.)
  # 2026-06-26 round-up (later pass): new commit ef95787 — adopted the fairyfox git-flow,
  # process-reports & compliance standards (docs/notes/CI-comment only, no VERSION bump,
  # still 0.14.2-alpha). Blogged in _posts/2026-06-26-…. Marker advanced f88bbfd → ef95787.
  # 2026-06-26 round-up (final pass): commits 593859b/ebb36f0/3ff32c6 — adopted the
  # express-authorizations standard + the verification-floor clarification (docs/notes only,
  # still 0.14.2-alpha). Folded into the 2026-06-26 day post ("The loop runs once more").
  # Marker advanced ef95787 → 3ff32c6.
  # 2026-06-27 round-up: new commit 1bae326 — a fairyfox check-for-updates pass against hub
  # 0.9.4, no change to adopt (still 0.14.2-alpha). No-op docs/notes; nothing blog-notable.
  # Marker advanced 3ff32c6 → 1bae326. (It also added a new fairyfox report file,
  # 2026-06-26-check-for-updates.md — left for the inbound report-review pass, not this one.)
  # 2026-06-28 round-up: no new commits past 1bae326 (still 0.14.2-alpha). Nothing to blog.
  # Marker unchanged; seen advanced to 2026-06-28.
  # 2026-06-29 round-up: still no new commits past 1bae326 (clean ff "Already up to date",
  # still 0.14.2-alpha). Nothing to reconcile or blog. Marker unchanged; seen → 2026-06-29.
  # 2026-06-30 round-up: still no new commits past 1bae326 (clean ff "Already up to date",
  # still 0.14.2-alpha). Nothing to reconcile or blog. Marker unchanged; seen → 2026-06-30.
  # 2026-07-01 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-01.
  # 2026-07-02 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-02.
  # 2026-07-03 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-03.
  # 2026-07-04 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-04.
  # 2026-07-05 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-05.
  # 2026-07-06 round-up: still "Already up to date" at 1bae326 (still 0.14.2-alpha). Nothing to
  # reconcile or blog. Marker unchanged; seen → 2026-07-06.
  # 2026-07-07 round-up: first change in ~10 days — fast-forwarded 1bae326 → acc09b9 (no force-push),
  # still 0.14.2-alpha (docs-site only, no VERSION bump). The Doxygen docs adopted the hub 0.14 chrome +
  # shared reader, flattened to match the reference (no gradients, muted rose accent, neutral nav, centred
  # prose, framed screenshots), rebuilt the Overview into one left-aligned column re-synced to the hub's
  # exact measures, and added a themed /screenshots/ gallery as an internal subnav item. Mentioned in the
  # 2026-07-06 day post. Marker → acc09b9; seen → 2026-07-07. (New report 2026-07-06-adopting-updates.md
  # left for the inbound report-review pass.)
  # 2026-07-08 round-up: "Already up to date" at acc09b9 (still 0.14.2-alpha). Nothing to reconcile or blog.
  # Marker unchanged; seen → 2026-07-08.
  # 2026-07-12 round-up: clean fast-forward 43effcf → 93756b7, 14 commits dated 2026-07-10/11,
  # VERSION 0.14.6-alpha → 0.16.6-alpha. Two threads, both on 2026-07-11 apart from the Exchange
  # sub-tabs (dated the 10th). (1) The Market gained an **Exchange** — Currency / Healing / a custom
  # item-for-item trade (give any owned item, get any item; Potion ↔ Fresh Water default) — then a
  # real fix: the exchange had been priced per step, minting phantom refunds; it now prices the whole
  # trade once, pinned by a new tst_item_exchange. (2) The name-entry screen was rebuilt from a
  # character grid into an **actual ASDF keyboard deck** (0.16.0 → 0.16.6): lowercase base layer, a
  # real Caps Lock that locks the shift PAGE (Shift inverts it), punctuation on its own keys (36 → 47),
  # two explicit modes, no invented modifier chords, box-drawing glyphs laid out AS the box (QWE/ASD/ZXC),
  # corner legends dropped where the key already types what a real keyboard would, no text field in
  # keyboard mode, and a real modifier now drops a latched page. Reconciled: projects.yml (PSE2 →
  # 0.16.6 / Jul 11). The Exchange strand was folded into the existing 2026-07-10 day post; the keyboard
  # rebuild into the new 2026-07-11 day post. Marker → 93756b7; seen → 2026-07-12.
  # 2026-07-11 round-up (three-day catch-up): clean fast-forward acc09b9 → 43effcf, 9 commits dated
  # 2026-07-09/10, VERSION 0.14.2-alpha → 0.14.6-alpha. Two save-file truth fixes (single-type mons write
  # duplicate-of-type1; copyFrom wrote Speed stat-exp to spExp), then the trainer card rebuilt to the
  # Pokémon-details "combo" language — [dice | trash] action groups on Money/Coins/Playtime, a titled
  # Playtime grouping box, Enabled/Paused as real toggles, a wider card (the clock had been overlapping the
  # artwork), button columns aligned (0.14.4–0.14.6) — on a new DEBUG-only automation harness (CLI flags,
  # live TCP channel, QML hot-reload that now preserves the current screen). Docs: manual releases, mandatory
  # screenshot review, notes-over-memory. Reconciled: projects.yml (PSE2 → 0.14.6 / Jul 10). Folded into the
  # 2026-07-09 + 2026-07-10 day posts. Marker → 43effcf; seen → 2026-07-11. (New report
  # 2026-07-06-adopting-updates.md still pending for the inbound report-review pass.)

random-ai-prompt:
  commit: 35b1370a882381511e854f8b3a75fcc2d4b07679
  branch: dev
  seen: 2026-07-21
  reports_through:             # digested-report filenames (append-only); see header
    - 2026-06-26-adopting-updates.md             # folded into standards 0.9.1
    - 2026-06-26-adopting-updates-2.md           # digested 0.9.8 — CI-owns-tag release callout (git-workflow) + hub_version-placeholder backfill note (process-reports)
    - 2026-06-26-adopting-updates-3.md           # digested 0.9.8 — verification floor already in 0.9.3; the "promote reset --hard" ask superseded by 0.9.6
    - 2026-06-27-adopting-updates.md             # digested 0.9.8 — working-tree sanity-check (adopting-updates check flow); "promote reset --hard" ask superseded by 0.9.6
    - 2026-06-28-adopting-updates.md             # digested 0.10.0 — check-only vs 0.9.6; already-addressed, and the shallow-clone recovery prose is now fully removed mesh-wide
    - 2026-06-30-propose-legal-docs-standard.md  # digested 0.12.0 — new legal-docs.md standard + templates/legal/
    - 2026-06-30-propose-tooling-standard.md     # digested 0.12.0 — new agent-tooling.md standard
    - 2026-07-01-gitattributes-eol-standard.md   # digested 0.12.0 — templates/project.gitattributes + agent-tooling line-ending hygiene
    - 2026-07-01-propose-git-workflow-backmerge.md # digested 0.12.0 — git-workflow dev-contains-main invariant + branch-sync.yml
    - 2026-07-01-upgrade-policy-proposal.md      # digested 0.12.0 — new dependencies.md standard
    - 2026-07-02-propose-scorecard-hardening.md  # digested 0.12.0 — new supply-chain-hardening.md (mandatory branch protection, reconciled release flow)
    - 2026-06-29-adopting-updates.md             # 2026-07-19 report-review — run feedback (successful adopt), no standalone standard change
    - 2026-06-30-adopting-updates.md             # 2026-07-19 report-review — run feedback (successful adopt), no standalone standard change
    - 2026-07-01-check-for-updates.md            # 2026-07-19 report-review — check-only, nothing to fold
    - 2026-07-02-check-only-update-scan.md       # 2026-07-19 report-review — check-only, nothing to fold
    - 2026-07-02-shareable-systems-roundup.md    # digested 0.18.0 — repo-hygiene.md + docs-lifecycle.md (+ check-links/check-tidy templates)
    - 2026-07-04-propose-regression-testing-standard.md # digested 0.18.0 testing.md; demonstrate-fail-then-pass + right-home nuance folded 0.20.0
    - 2026-07-05-adopt-docs-chrome-reader.md     # 2026-07-19 report-review — node docs-chrome/reader adoption feedback, already reflected
    - 2026-07-09-propose-maintenance-sweep-standard.md # digested 0.20.0 — new maintenance-sweep.md
  # 2026-07-21 round-up: "Already up to date" at 35b1370 (still 2.60.1) — no commits dated 2026-07-20; the
  # tip is still the 07-19 docs-adoption commit. Nothing to reconcile (projects.yml left at 2.60.1 / Jul 12)
  # or blog; RAP is absent from the new 2026-07-20 day post's tags. As the mesh's only standards-bearing
  # sibling, an unchanged RAP means no hub standard moved this pass. Marker unchanged; seen → 2026-07-21.
  # 2026-07-20 round-up: clean fast-forward 2f1459f → 35b1370 (5 commits, all dated 2026-07-19), VERSION
  # unchanged at 2.60.1 — docs/adoption only, no engine change. RAP's docs site adopted the hub's new coins
  # counter into its chrome and took six of the new mesh standards in as reference notes (a check-for-updates
  # report vs hub 0.20.2; CodeRabbit review addressed on PR #55). No product change, so projects.yml stays at
  # 2.60.1 / Jul 12; RAP is tagged in the new 2026-07-19 day post for the docs adoption. Marker → 35b1370;
  # seen → 2026-07-20. (New reports 2026-07-19-adopting-updates.md + 2026-07-19-check-for-updates.md left for
  # the inbound report-review pass.)
  # 2026-07-19 report-review (0.20.0): read the eight RAP reports above past the 0.12.0 marker. The three
  # proposals were folded — shareable-systems-roundup + regression-testing into the 0.18.0 batch
  # (repo-hygiene/docs-lifecycle/testing), maintenance-sweep into new 0.20.0 maintenance-sweep.md; the run
  # reports (adopt/check) are run-feedback with no standalone standard change. pse2/games/stories run
  # reports past their markers remain a follow-up (run feedback; their conventions were already extracted
  # in the 0.18.0 deep-read). See notes/fairyfox-reports/2026-07-19-report-review.md.
  # 2026-07-19 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-18; seven
  # quiet days now. Nothing to reconcile (projects.yml left at 2.60.1 / Jul 12) or blog; RAP is absent from the
  # new 2026-07-18 day post's tags. As the mesh's only standards-bearing sibling, an unchanged RAP means no hub
  # standard moved this pass. Marker unchanged; seen → 2026-07-19.
  # 2026-07-18 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-17; the
  # tip is still the 07-12 session-log commit. Six quiet days now. Nothing to reconcile (projects.yml left at
  # 2.60.1 / Jul 12) or blog; RAP is absent from the 2026-07-17 day post's tags. It is also the mesh's only
  # standards-bearing sibling, so an unchanged RAP means no hub standard could have moved this pass. Marker
  # unchanged; seen → 2026-07-18.
  # 2026-07-17 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-16; the
  # tip is still the 07-12 session-log commit. Five quiet days now. Nothing to reconcile (projects.yml left
  # at 2.60.1 / Jul 12) or blog; RAP is absent from the 2026-07-16 day post's tags. It is also the mesh's
  # only standards-bearing sibling, so an unchanged RAP means no hub standard could have moved this pass.
  # Marker unchanged; seen → 2026-07-17.
  # 2026-07-16 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-15.
  # Nothing to reconcile or blog; RAP is absent from the 2026-07-15 day post's tags. Marker unchanged;
  # seen → 2026-07-16.
  # 2026-07-15 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-14.
  # Nothing to reconcile or blog; RAP is absent from the 2026-07-14 day post's tags. Marker unchanged;
  # seen → 2026-07-15.
  # 2026-07-14 round-up: "Already up to date" at 2f1459f (still 2.60.1) — no commits dated 2026-07-13.
  # Nothing to reconcile or blog; RAP is absent from the 2026-07-13 day post's tags. Marker unchanged;
  # seen → 2026-07-14.
  # 2026-07-13 round-up: clean fast-forward 20e36af → 2f1459f (36 commits), VERSION 2.58.0 → 2.60.1 — all
  # commits dated 2026-07-12 by author date, so the marker now goes to HEAD (the previous pass had parked it
  # at 20e36af because the tip was dated a day still in progress; that day is now complete). NOTE: RAP's own
  # changelog dates the 2.60.1 entry 2026-07-13 while git says 2026-07-12 21:52 −0600 — a timezone artefact,
  # not a second day; its session log for the whole run is notes/sessions/2026-07/2026-07-12.md, so the day
  # post follows the session log. The day's theme is the same as PSE2's: MEASURE, don't assert. (1) 2.58.0
  # put the mobile app in CI at all — every control PRESSED by a test rather than merely rendered, behind
  # coverage gates; most of the work was fighting the harness (the emulator launching the app behind its own
  # lock screen, a dark screen granting window focus to nobody, the LAUNCHER itself ANR-ing on top of the app
  # under test). (2) 2.60.0 verified the headline 1000-prompt promise on a REAL Android runtime (Detox,
  # release APK): 1000 prompts render in 253 ms with memory FLAT as the list grows 50× (110 MB vs 112 MB) —
  # and the "defect" the gate first reported was THE TEST's own: results accumulate across rolls by design, so
  # a test waiting for "200 generated" after a 20-prompt baseline (label reads "220") waited out a ten-minute
  # timeout and reported it as the app being slow. The instrumentation added to diagnose it is what disproved
  # it. Also: CI had been RED on dev while every local gate said green (format:check joined npm test); the last
  # hand-port (the DPL insert grammar) moved into the engine; notes-first became a hard rule (§A0). (3) 2.60.1
  # is a CRITICAL security fix CodeQL found on the release PR: /api/image/open and /api/image/reveal built a
  # SHELL STRING around a request-derived path, so an image named `x" & calc & ".png` closes the quote and runs
  # the rest (js/command-line-injection). Fixed structurally — no shell at all, program + argv handed to
  # execFile — plus prototype pollution via mergeSidecar, a world-writable temp cache in os.tmpdir(), and three
  # check-then-use races. Reconciled: projects.yml (RAP → 2.60.1 / Jul 12). Folded into the new 2026-07-12 day
  # post. Marker → 2f1459f; seen → 2026-07-13.
  # 2026-06-28 report-review (0.9.8): digested the three reports above + PSE2's check-for-updates.
  # Folded Theme B (elevate CI-owns-tag to a callout in git-workflow.md "Cutting a release"),
  # Theme C (backfill note for stale hub_version placeholders in process-reports.md), and Theme D
  # (check-only runs glance at the node's own working tree — mid-merge/conflicts/detached HEAD —
  # in adopting-updates.md). The siblings' recurring "promote the reset --hard force-push fallback"
  # request was NOT adopted: 0.9.6 already established that was a shallow-mirror misread, not a real
  # force-push; instead added a pre-0.9.6 re-adopt nudge to adopting-updates.md step 1.
  # Covered through: project-side mesh onboarding + fairyfox-themed JSDoc docs, the
  # SFW-by-default + top-bar NSFW toggle (2.6.1), and adopting the git-flow standard
  # with the master → main rename (CI/Pages/release repointed). All folded into the
  # 2026-06-25 day post. (dev was force-pushed; re-cloned.)
  # 2026-06-26 round-up (later pass): dev force-pushed again, re-synced via fetch +
  # reset --hard on the mirror. Cut 2.7.0 (MINOR) — the repo split into engine-v3 (active)
  # + engine-v1-2 (frozen pre-revival snapshot), v3-only, legacy <expansion> mechanism
  # removed end-to-end — and adopted the process-reports + compliance standards (notes-only).
  # Blogged in _posts/2026-06-26-…. Marker → 53fe12d, VERSION 2.6.1 → 2.7.0.
  # 2026-06-26 round-up (final pass): re-synced (fetch + reset --hard); no new commits past
  # 53fe12d, still 2.7.0. Nothing to blog. Marker unchanged.
  # 2026-06-27 round-up: fast-forwarded cleanly 53fe12d → 1868b58 (no force-push). The 2.7.x
  # line (2.7.0 → 2.7.25, all 2026-06-26) built out the engine-v3 web app's image-generation
  # layer: prompts dispatch to 16+ hosted/local providers, an optional AI rewrite pass, a
  # central output/ folder, and a photo gallery + dedicated single-image view with per-image
  # .json metadata sidecars (keys stripped). Also re-synced the 0.9.2/0.9.3 standards deltas
  # (commits f0cf652/0cd35ce, no version bump). Folded into the 2026-06-26 day post
  # ("The prompt tool starts generating images"). Marker → 1868b58, VERSION 2.7.0 → 2.7.25.
  # 2026-06-28 round-up: 1868b58 → e491232, 32 commits all dated 2026-06-27, VERSION 2.7.25 → 2.10.4.
  # (Mirror hit an "incorrect old value" fetch anomaly mid-refresh — re-cloned fresh, full history,
  # per the 0.9.6 standard.) The 2.7.26–2.10.4 line: CodeMirror DPL editors, sidecar priority+nsfw,
  # result/gallery polish, the stripped generate-only ONLINE build (now hosted at prompt.fairyfox.io
  # via Netlify), keyword tooling + DPL insert bar, the provider-header redesign (Providers dropdown +
  # per-key BYOK + Prompt/Negative switch), and the headline DPL intensity dial ({#name NN%}, conditions,
  # auto-scaling) with a content refactor across all five categories (incl. stripping masterpiece/best-
  # quality from the default wrapper). Folded into the 2026-06-27 day post
  # ("A dial for the prompt language, a home for it online…"). Marker → e491232, VERSION 2.7.25 → 2.10.4.
  # 2026-06-29 round-up: fast-forwarded cleanly e491232 → ba5b65a (no force-push), 33 commits all dated
  # 2026-06-28, VERSION 2.10.4 → 2.14.0. The 2.10.5–2.14.0 line: browser-direct generation in the now-fully-
  # static online build (2.11.0); the in-app **Manage tab** — a disk-backed catalog editor (blocks/lists/folders)
  # with live hot-apply, virtualized list editing, restore-default + ghost pills, external-edit watch (2.12.0,
  # probe-hardened in 2.12.1); the **focus dial** alongside intensity ({#name i25% f80%}, mandatory i/f prefixes,
  # $ keyword sigil) + global layer auto-merge (2.13.0); and list Dedupe/Sort + Manage Insert bar + AI Expand +
  # live DPL-validity lint (2.14.0). Folded into the 2026-06-28 day post ("An editor for the catalog, a second
  # dial…"). Marker → ba5b65a, VERSION 2.10.4 → 2.14.0.
  # 2026-06-30 round-up: fast-forwarded cleanly ba5b65a → 2b6b8e6 (no force-push), VERSION
  # 2.14.0 → 2.28.18 — RAP's biggest single day (all commits dated 2026-06-29). The 2.15.0–2.28.18
  # line: SPA internationalization (react-intl + FormatJS, 2.15.0); the single-image view's actions —
  # re-roll/variation + tracked ancestry + Markdown export (2.16.0) then inline actions/live strips/
  # resize/View Raw (2.17.0); a big provider expansion (2.18–2.28.0) — an AI upscale/enhance framework +
  # NSFW soft-lock, a dedicated Upscaler/Enhancer row, and a run of upscale/enhancer providers (DeepAI,
  # Picsart, Segmind, local SD A1111/Forge/SD.Next/ComfyUI, Clipdrop, Venice, WaveSpeed, Claid, Deep-Image,
  # neural.love, VanceAI, Leonardo, Replicate) plus text providers to 18 total; then the Phase 4–6 refactor
  # (floor-up modular decomposition of Home/SingleView/Manage/listManifest/contentSafety/DPL with new test
  # nets landed alongside each extraction), a Lighthouse polish (SEO+A11y 100, Perf 97) + SEO meta + CI gate,
  # and a loader memoization (~280× faster generate(), no behavior change). Folded into the 2026-06-29 day
  # post ("More providers, a deep refactor, and two new games"). Marker → 2b6b8e6, VERSION 2.14.0 → 2.28.18.
  # 2026-07-01 round-up: clean fast-forward → c38373d, VERSION 2.28.18 → 2.35.1 (all commits dated 2026-06-30).
  # The 2.29.0–2.35.1 line: a unified versioned cookie-free storage overhaul (2.29.0); GUI polish + header links
  # menu (2.30.0); in-app **Privacy/Terms/Cookies** pages + **self-hosted fonts** ending the IP-to-Google flow
  # (2.30.1); the header/provider UX batch — app-settings cog, provider-settings accordion, unified derived grid
  # (2.31.0) with follow-up polish (2.32.1/2.33.1/2.33.2); the local edition's **real release stage** (`npm start`,
  # backend factored out of the Vite dev plugin) + full file-watch hot reload + atomic settings writes (2.32.0);
  # natural-language artists/styles + a `{#styles}` block (2.33.0); a **README revamp + SEO repositioning** away
  # from SD-first (2.33.3); **in-app dialogs** replacing native alert/confirm/prompt (2.34.0); a **seedable,
  # deterministic engine** (real threaded PRNG, reproducible-from-seed) + async batch boundary (2.35.0); and a
  # plain-English **description alignment** everywhere (2.35.1). Plus docs/CI-only, no bump: security & code-health
  # integrations (Dependabot/CodeQL/Scorecard/Sonar/CodeRabbit) and README badges → shields.io + Codecov. Folded
  # into the 2026-06-30 day post ("Legal pages, a seedable engine, and a fifth game"). Marker → c38373d.
  # (New sibling reports 2026-06-30-propose-legal-docs-standard.md + -propose-tooling-standard.md left for the
  # inbound report-review pass — not this round-up.)
  # 2026-07-02 round-up: clean fast-forward → 2ed5787, VERSION 2.35.1 → 2.38.1 (all commits dated 2026-07-01).
  # The 2.35.2–2.38.1 line: a full dependency upgrade (2.35.2) + stylelint CSS gate (2.35.3); the floor-up
  # CSS overhaul + theming framework (Phases 0–7, 2.35.4→2.37.0) — monolithic styles.css split into a modular
  # styles/ tree on a two-tier token system + cascade layers, a System/Dark/Light theme engine, 9 accent presets,
  # a header Appearance picker, built-in themes as a folder of JSON files, and user theme import/export; a web-perf
  # pass — prompt corpus off the first-paint path (2.37.1) then the online build prerendering its first paint to
  # static HTML + hydration (2.38.0, mobile Lighthouse Perf mid-50s→80s); and a SonarCloud tech-debt sweep — all
  # 70 code smells cleared incl. 7 ReDoS regex rewrites + 7 complexity refactors (2.38.1) plus the last bug/2 vulns
  # resolved and the quality-gate coverage measurement fixed; a git-workflow back-merge (main→dev) fix + branch-sync
  # guard. Folded into the 2026-07-01 day post ("A theming system, a faster first paint, and a sixth game").
  # Marker → 2ed5787, VERSION 2.35.1 → 2.38.1. (New sibling reports 2026-07-01-gitattributes-eol-standard.md,
  # -propose-git-workflow-backmerge.md, -upgrade-policy-proposal.md left for the inbound report-review pass.)
  # 2026-07-03 round-up: clean fast-forward 2ed5787 → 43f4c2b, VERSION 2.38.1 → 2.41.0 (all commits dated
  # 2026-07-02). The 2.40.1–2.41.0 line: a responsive rebuild of the web app — fluid token foundation +
  # layout cascade layer, overflow menu for the top-bar pile, phone drawers (palette, Manage master/detail),
  # single-view image-over-metadata stacking, 44px touch targets, full-width bottom-sheet submenus, then a
  # tablet split-screen tier (769–1024px). Underneath: the repo was FLATTENED (engine-v3/engine-v1-2 split
  # retired — project now at the root under src/ data/ gui/ tests/; pre-revival CommonJS system removed from
  # the tree, kept in git history), architecture docs rewritten + stale-ref sweep; main branch-protected
  # (releases via PR), keyless-signed release assets, repo-hygiene guardrails + dev deletion-protection +
  # dependabot bumps. Reconciled data: projects.yml (RAP → 2.41.0 / Jul 2), the engine-v3 → repo-root fix in
  # downloads.yml + _projects/random-ai-prompt.md + _docs/random-ai-prompt.md, responsive note on the node.
  # Folded into the 2026-07-02 day post ("A responsive rebuild, a seventh game, and six new standards").
  # Marker → 43f4c2b. (New sibling report 1263fba roundup-proposal left for the inbound report-review pass.)
  # 2026-07-04 round-up: clean fast-forward 43f4c2b → cdfaf7c, VERSION unchanged at 2.41.0 (all commits
  # dated 2026-07-03). CI/tooling only: fixed the release-signing step so v2.41.0 could actually publish
  # (pinned cosign 2.x), then modernized signing to cosign 3.x Sigstore bundles (<asset>.sigstore.json,
  # pinned v3.1.1; deployment.md verify updated) + an in-range Vite build-tool bump. No product change, so
  # projects.yml/downloads left at 2.41.0 / Jul 2; the signing thread was folded into the 2026-07-03 day
  # post ("An eighth game, and a modernized release signature"). Marker → cdfaf7c.
  # 2026-07-05 round-up: clean fast-forward cdfaf7c → 5dcd621, VERSION 2.41.0 → 2.42.1 (all commits dated
  # 2026-07-04). The 2.41.1–2.42.1 line: a mobile composer settings-gear backdrop fix + a standing
  # "every fix ships a regression test" rule (2.41.1); an explicit **Random-seed control** replacing the
  # -1 magic-number sentinel that had pinned every roll to one prompt — free-text seeds, reproducible rolls
  # (2.41.2); seed-preview independence + zero SonarCloud debt (2.41.3); the headline **large-scale
  # performance** pass (2.42.0) — gallery **virtualized** to a bounded window at 100k images, results list
  # bounded, **placeholder-first chunked** image generation with per-provider image/text concurrency limiters
  # via a new folder-based shared-settings system, and a real perf test suite asserting scroll quality at the
  # stated max load; and a **dark-mode DPL autocomplete contrast fix** (unlayered CodeMirror tooltip theme
  # beat the app's cascade layers) + an automated per-release screenshot/GIF pipeline (2.42.1). Reconciled:
  # projects.yml (RAP → 2.42.1 / Jul 4). Folded into the 2026-07-04 day post ("A ninth game, and prompts
  # that scale to 100k"). Marker → 5dcd621, VERSION 2.41.0 → 2.42.1. (New sibling report
  # 2026-07-04-propose-regression-testing-standard.md left for the inbound report-review pass.)
  # 2026-07-06 round-up: clean fast-forward 5dcd621 → 8f56153, VERSION 2.42.1 → 2.43.1 (all commits
  # dated 2026-07-05). The 2.43.0–2.43.1 line turned the tool outward: a **pre-built desktop edition**
  # (Tauri shell running the unmodified local app + Node backend as a bundled sidecar, user data
  # preserved across upgrades) and a "every edition ships pre-built" reframing — hosted online build,
  # a self-hostable copy of it, and the desktop installers all interchangeable (2.43.0, MINOR). Plus a
  # full **docs-site rebuild**: adopted the fairyfox.io hub header + organized subnav, self-hosted the
  # doc fonts (no third-party call), a comfortable centred reading column + softer light/sepia themes +
  # a Kindle-style reader menu, full SEO/social/sitemap across all pages, a WCAG 2.1 AA pass (0 axe
  # violations), sidebar confined to the API pages, then adopted the hub's shared chrome + reader menu
  # directly on the new `fairyfox:reader:b` key (2.43.1) with Overview-spillover / sidebar-flash /
  # footer-logo fixes; README + CONTRIBUTING rewritten, screenshots recaptured. Reconciled: projects.yml
  # (RAP → 2.43.1 / Jul 5, blurb notes the desktop edition + Desktop tag). Folded into the 2026-07-05 day
  # post ("A tenth game, a desktop app, and a sharper reader"). Marker → 8f56153, VERSION 2.42.1 → 2.43.1.
  # 2026-07-07 round-up: clean fast-forward 8f56153 → 5ea0761, VERSION 2.43.1 → 2.49.6 (all commits dated
  # 2026-07-06). auto-updating — check-and-notify banner + desktop auto-install (2.44.0); a gallery composer
  # with live placeholders, multi-select delete, a11y + SEO (2.45.0); a repo-root user/ content overlay
  # beside data/ (2.46.0); a restructure into engine/ + targets/ with targets/desktop → web-shell (2.47.0)
  # and the "dynamic prompts" → "blocks" rename everywhere (2.48.0); then an interactive DPL editor layer —
  # hover intensity/focus dials, per-line actions, line numbers, active-line highlight, colouring fixes —
  # plus a Manage AI-refine toolbar + draft-from-description + free-text modify box (2.49.0–2.49.6).
  # Reconciled: projects.yml (RAP → 2.49.6 / Jul 6). Folded into the 2026-07-06 day post ("Auto-updating
  # prompts, an eleventh game, and a shelf of stories"). Marker → 5ea0761, VERSION 2.43.1 → 2.49.6. (New
  # sibling report 2026-07-05-adopt-docs-chrome-reader.md left for the inbound report-review pass.)
  # 2026-07-08 round-up: clean fast-forward 5ea0761 → 3147535, VERSION 2.49.6 → 2.50.1 (all commits dated
  # 2026-07-07). A new **command-line target** — `targets/cli/`, the `prompt` CLI (2.50.0, MINOR): a
  # traditional args-and-flags tool covering every feature as a subcommand (generate/list/config/keys/
  # rewrite/upscale/completion), with --help, colours, and bash/zsh/fish/PowerShell completion, reusing the
  # SAME engine/providers/settings/on-disk store as the web+desktop app (Node provider registry replacing
  # the SPA's Vite globs; the real backend run in-process on an ephemeral port with a fetch shim; api
  # providers only billed with --images). Then 2.50.1: disposed of the pre-DPL **`chaos`** knob for good
  # (already unmounted 2026-06-19, never an engine setting; byte-identical output) and extracted the
  # duplicated seed/reroll logic into an engine-owned isomorphic `engine/promptRun.js` the web facade + CLI
  # now share (groundwork for the PLANNED-not-built ComfyUI target). Reconciled: projects.yml (RAP → 2.50.1
  # / Jul 7, blurb editions list gains the command-line tool). Folded into the 2026-07-07 day post ("A
  # command-line prompt tool, and a fifth game onto varied structure"). Marker → 3147535, VERSION 2.49.6 → 2.50.1.
  # 2026-07-12 round-up: clean fast-forward 3686435 → 2f48135 (17 commits), VERSION 2.52.0 → 2.58.0.
  # The marker is deliberately set to 20e36af — the LAST commit dated 2026-07-11 — not to HEAD: the tip
  # commit 2f48135 (2.58.0, "the mobile app was never in CI") is dated 2026-07-12, i.e. TODAY, a day
  # still in progress. Blogging it now would half-cover the day, so it is left for the next pass, which
  # will see it as new. (projects.yml still carries the true current state, 2.58.0 / Jul 12 — data
  # reconciliation and blogging are separate duties.)
  # Blogged through 2026-07-11 (2.53.0 → 2.57.1), the day RAP stopped being two apps. The DE-DUPLICATION
  # campaign, Phases B–E: one GENERATED provider registry importable by all three runtimes (Vite/Node/Metro)
  # + an injectable transport, which let the 892-line mobile provider hand-port be deleted for a 268-line
  # adapter with no provider/transport logic of its own; the nine hand-transcribed accent themes and the
  # duplicated list ops promoted to shared/engine. FIVE of the seven mobile drift checks were deleted with
  # them ("you cannot drift from yourself"); the surface-parity gate stays. Then three findings, each about
  # a check that was built wrong: the SHUFFLE control was missing on mobile entirely (found by running the
  # visual harness and LOOKING — its parity marker had been satisfiable by an unrelated identifier sharing a
  # word); the WEB BUILD had been broken on dev for four commits while every gate read the working tree
  # instead of the committed one (2.57.1, new `check:committed` gate — "a working tree is not evidence"); and
  # the app had been silently CAPPING users (2.57.0) — a web roll truncated to 50 prompts, a mobile clamp at
  # 1000 — with a test asserting the cap as a specification. All caps removed (25k prompts now generate in
  # full, linearly). Also on the 11th: the GitHub owner rename junebug12851 → 1fairyfox swept across RAP's
  # current-state files, which is what surfaced it for the hub (see this repo's 0.15.9). Reconciled:
  # projects.yml (RAP → 2.58.0 / Jul 12). Folded into the new 2026-07-11 day post. Marker → 20e36af;
  # seen → 2026-07-12.
  # 2026-07-11 round-up (three-day catch-up): clean fast-forward 3147535 → 3686435, 84 commits dated
  # 2026-07-07 → 2026-07-10, VERSION 2.50.1 → 2.52.0. Two threads. (1) A **ComfyUI target** (2.51.0):
  # targets/comfyui/ custom nodes (generator + batch/combine/show, example workflow, status sidebar) fed by
  # new headless /api/prompt + /api/prompt/catalog routes over the same engine; fitted to the Comfy ecosystem
  # (Rewrite dropped, Settings-only config); pre-release hardening (preset-loader path traversal, non-http app
  # URLs rejected). Then 2.51.1: the DPL editor's gutter/active line stop leaking CodeMirror's light defaults
  # into dark mode. Plus a maintenance-sweep runbook, proposed to the hub as a standard (report
  # 2026-07-09-propose-maintenance-sweep-standard.md — left for the inbound report-review pass). (2) A
  # **native Android edition** (2.52.0, MINOR): a Metro catalog generator + loader proven byte-identical to the
  # web (89 blocks / 88 lists / 150 seeded gens), an Expo/Hermes scaffold, then the full 4-tab app rebuilt to the
  # web's design tokens/chrome, theming + language, the whole provider data layer with BYOK keys and local-direct
  # providers, a parity Gallery (search / multi-select / metadata, memoized at 100k), a rebuilt Single view, the
  # Manage port (folder tree, block + list editors, Sort/Dedupe, AI Expand, Refine/Modify/Draft with a
  # byte-identical rewrite-prompt gate, built-in browse + override, a LIVE user overlay feeding generation),
  # tablet layouts + Single two-pane, an 80-test harness, and strict per-surface parity gates. Reconciled:
  # projects.yml (RAP → 2.52.0 / Jul 10; blurb + tags gain the Android app). Folded into the 2026-07-08,
  # 2026-07-09 and 2026-07-10 day posts. Marker → 3686435, VERSION 2.50.1 → 2.52.0.

papermc-despawned-items:
  commit: 2e2408350dca8a0b0de1f44b96b00dc6bc27bb9c
  branch: dev
  seen: 2026-07-21
  reports_through: []          # digested-report filenames (append-only); see header — none yet
  # 2026-07-21 round-up (FIRST marker for this node): onboarded site-side on 2026-07-20 at v1.0.2 (the 0.20.4
  # release registered it + opened the PaperMC Plugins category), but no round-up marker had been set yet, so
  # this pass establishes the baseline. The Kotlin/Paper rewrite + mesh onboarding (v1.0.0/v1.0.1) landed
  # 2026-07-19; then on 2026-07-20 the plugin went v1.0.2 → v1.3.4 in one day — retargeted Paper 26.1/Java 25
  # → Paper 1.21.11/Java 21 (zero API changes), an indexed O(1) LocationStore, off-thread incremental
  # persistence, a throttled DespawnScheduler, pluggable YAML/SQLite/MySQL storage, per-user limits, correctness
  # fixes (PDC recycle rewards, particle-data crash, reload re-reads config), Brigadier commands, a JUnit 5 +
  # MockBukkit suite (~49 tests gating build) + Ktlint/Detekt/Kover, a package move to
  # io.fairyfox.papermc.despawneditems, the shared fairyfox chrome on the Dokka docs, and a full hub-standards
  # audit (branch protection, PR-based releases, build-provenance) shipped through the protected PR path.
  # Reconciled: projects.yml (papermc → 1.3.4 / Jul 20; tags + blurb retargeted to Paper 1.21.11 / Java 21) and
  # registry.yml comment. Folded into the new 2026-07-20 day post. Marker → 2e24083 (last 2026-07-20 commit,
  # v1.3.4); seen → 2026-07-21. (07-21 v1.3.5 — a docs-site notes-README render fix — left for the next pass.)

fairyfox-games:
  commit: 015a421c62e7a3fa14d32caf8c5e87e939700bbe
  branch: dev
  seen: 2026-07-21
  reports_through:             # digested-report filenames (append-only); see header
    - 2026-06-29-setup.md                 # digested 0.10.0 — collection/monorepo as a recorded exception; static site doubles as docs
    - 2026-06-29-onboarding-verify.md     # digested 0.10.0 — same-day setup cross-ref added to onboarding
    - 2026-06-30-docs-site-feedback.md    # digested 0.10.0 — one-seamless-site model (no back-button, shared chrome + submenu, Games nav slot); tags kept games-local
    - 2026-06-30-update-check.md          # 2026-07-19 report-review — check-only; anchor/shallow-mirror friction already addressed (0.9.6/0.9.8)
    - 2026-07-01-update-check.md          # 2026-07-19 report-review — check-only report of hub 0.11.0 deltas; adoption is the node's, no standard change
    - 2026-07-02-adopting-updates.md      # 2026-07-19 report-review — run feedback (successful adopt), no standard change
    - 2026-07-02-update-check.md          # 2026-07-19 report-review — check-only, no standard change
    - 2026-07-06-adopting-updates.md      # 2026-07-19 report-review — run feedback (successful adopt), no standard change
    - 2026-07-18-adopting-updates.md      # 2026-07-19 report-review — run feedback (successful adopt), no standard change
    - 2026-07-18-update-check.md          # 2026-07-19 report-review — check-only, no standard change
  # 2026-07-21 round-up: clean fast-forward a690442 → 015a421, VERSION 0.24.4 → 0.24.12. Marker PARKED at
  # 015a421 (last 2026-07-20 commit, v0.24.12), NOT at HEAD ec503d3 (a 07-21 merge, day still in progress —
  # left for the next pass). Two 07-19 commits sat just past the previous marker (coins phase 2 wire-into-
  # chrome + phase 3 shared module/Polarity reference), then 07-20 finished the in-game coin rollout across
  # the back half of the collection: batches 1–5 (Ward, Brim, Tether, Ink Bloom, Echo Chamber, Orbit
  # Slingshot, Ricochet, Skyline, Loft, Poise, Symmetry, Arc, Sluice) each gaining the uniform capped earn +
  # one optional cosmetic fun mode, capped by Reprise's visual "Light show" (15/15 — the audio-Choir game got
  # a purely visual mode so no game carries an audio dependency), then the initiative closed. Reconciled:
  # projects.yml (games → 0.24.12 / Jul 20). Folded into the new 2026-07-20 day post. Marker → 015a421; seen →
  # 2026-07-21. (07-21 v0.24.13 — repo-hygiene check-links/check-tidy scripts — left for the next pass.)
  # 2026-07-20 round-up: clean fast-forward 5d89b0c → a690442, VERSION 0.24.2 → 0.24.4 (commits dated
  # 2026-07-19). A GROW then a standards adoption. (1) 0.24.3 GROW — Skyline gets "depth inside the mechanic"
  # (7th game on the layer, the oldest still without it): a razor Keystone sub-window (1.2px) inside the flush
  # window, a Jet Stream doubling the next 3 drops after 3 keystones, speedOf turned into a no-plateau score
  # asymptote (was flat from score ≈44), a secret Exosphere stage at 240. (2) 0.24.4 — adopted the hub
  # standards batch v0.16.0→v0.20.2 (coins initiative phase 1): ten new reference notes (coins,
  # farm-operating-model, engineering-quality, testing, repo-hygiene, maintenance-sweep, docs-lifecycle,
  # research-capture, self-hosted-assets, working-rhythm) + five refreshed; docs-only, no game code, the
  # coins feature itself deferred to later phases. Reconciled: projects.yml (games → 0.24.4 / Jul 19).
  # Folded into the new 2026-07-19 day post. Marker → a690442; seen → 2026-07-20. (New report
  # 2026-07-19-update-check.md left for the inbound report-review pass.)
  # 2026-07-19 round-up: clean fast-forward 728a76f → 5d89b0c, VERSION 0.23.2 → 0.24.2 (commits dated
  # 2026-07-18). A PLANT + a GROW + housekeeping. (1) PLANT 0.24.0 (MINOR) — Ward, the 15th game and the
  # collection's FIRST defence verb: orbit one shield around a core and parry converging shards (turn-rate cap
  # makes opposite threats a real read); a precise parry pays a rising mult over a safe block; depth from birth
  # (Surge on 5 parries, secret Aegis stage) + varied structure from birth; 49 pure-core tests, collection
  # 630/630. (2) GROW 0.23.3 — Ricochet gets the depth layer (dead-centre tech, the Blaze doubling window,
  # no-plateau shrink, secret Legend stage); depth rollout now 6 of 13. (3) Housekeeping 0.24.1 — owner rename
  # → 1fairyfox on live refs, 3 missing game icons (Ward/Reprise/Brim) code-rendered + an onerror fallback so
  # no icon-less game shows a broken image; a fairyfox check (hub 0.14.3 → 0.16.0). (4) 0.24.2 — adopted the hub
  # shared-chrome header: the Farms nav dropdown (chrome bundle v2.0.0), a two-file change (most already
  # vendored). Reconciled: projects.yml (games → 0.24.2 / Jul 18). Folded into the new 2026-07-18 day post.
  # Marker → 5d89b0c; seen → 2026-07-19.
  # 2026-07-18 round-up: clean fast-forward 450db5d → 728a76f, VERSION 0.23.1 → 0.23.2 (commits dated
  # 2026-07-17). A pure GROW run, no plant: **Orbit Slingshot gets the depth layer** — the oldest game
  # without it (5th of 13), carrying the same plateau symptom the sweep flags (its pickup radius shrank
  # per stage then hit a floor). Four items on the one hold-to-thrust verb: no-plateau pickup radius (score
  # asymptote, hard floor); the hidden **Kiss** (razor sub-window 7px inside the 60px close band → +2, gold
  # ring, streak); **Aurora** (3 kisses chained → ~5s every-point-doubles over the planet); secret
  # **Interstellar** stage at score 240 (reveal toast, badge). The layer is the reference build's SHAPE poured
  # into Orbit Slingshot's own physics, not a copy. 3 new badges (8→11), totals.kisses (lossless legacy
  # upgrade); +10 pure-core tests (39→49), collection 572/572. Merged Dependabot #47 (ruby/setup-ruby minor,
  # both checks green); #46 (setup-node major) + #31 (attest-build-provenance major) held for a deliberate
  # pass. Depth-layer rollout now 5 of 13 (Polarity, Brim, Echo Chamber, Ink Bloom, Orbit Slingshot; Tether +
  # Reprise from birth). Reconciled: projects.yml (games → 0.23.2 / Jul 17). Folded into the new 2026-07-17
  # day post. Marker → 728a76f; seen → 2026-07-18.
  # 2026-07-17 round-up: clean fast-forward ebc896c → 450db5d, VERSION 0.23.0 → 0.23.1 (commits dated
  # 2026-07-16). A pure GROW run, no plant: **Ink Bloom gets the depth layer** — the OLDEST game without it,
  # and it carried the exact plateau the status sweep flags (speedOf = SPEED + score·0.012, hard-capped 4.4
  # at score ~117 → the one felt axis flat forever, and nothing under its five minutes). Four items, all on
  # the one steer verb, all safe to not know: (1) NO PLATEAU — speedOf is now a smooth score asymptote (Echo
  # Chamber's exact shape), gentler early, still climbing at 400+, old flat-line pinned by a regression test.
  # (2) THE GRAZE (hidden tech) — surviving inside a 9px band JUST OUTSIDE the kill radius of your own trail
  # pays +1 and builds a streak; the game's whole hazard becomes its score source (the Pac-Man reversal); a
  # 60-tick cooldown stops band-parking from machine-gunning; new pure minSelfDist2 uses the SAME collidable
  # set as hitSelf so the neck/frame-one geometry can't phantom-graze (pinned). (3) IRIDESCENCE — 3 grazes
  # chained within 300 ticks → ~5s where every point doubles (motes/prisms/grazes; trail growth deliberately
  # NOT doubled — the window is pure profit); announced only when earned; hue-cycling frame + halo, COLOUR
  # ONLY (reduced-motion friendly). (4) SECRET STAGE ECLIPSE at score 260 — unlisted (the printed stage list
  # came OFF the start screen so the "end" stays uncertain), revealed by toast on reaching it. Meta:
  # totals.grazes (legacy blobs upgrade losslessly), 8 → 11 badges. +10 pure-core tests (44 → 54);
  # collection 562/562. Depth-layer rollout now 4 of 13 (Polarity, Brim, Echo Chamber, Ink Bloom); Tether +
  # Reprise carry it from birth. Preview: Chrome MCP unavailable on the automated run → headless Chrome
  # render of the live shell (forced Eclipse + live Iridescence) instead; a live play-feel eyeball still
  # owed (GRAZE_BAND 9 / GRAZE_COOLDOWN 60 / IRI_TRIGGER 3). Dependabot #31 still deliberately deferred.
  # Reconciled: projects.yml (games → 0.23.1 / Jul 16). Folded into the new 2026-07-16 day post.
  # Marker → 450db5d; seen → 2026-07-17.
  # 2026-07-16 round-up: clean fast-forward 3c4855a → ebc896c, VERSION 0.22.2 → 0.23.0 (commits dated
  # 2026-07-15). With the varied-structure rollout complete (13/13), the day went in two directions.
  # (1) GROW (0.22.3): Echo Chamber gets DEPTH INSIDE THE MECHANIC — the second lever now that breadth is
  # done (Polarity = reference). A hidden razor dead-centre window (taught nowhere) pays a bonus + builds a
  # streak; 3 in a row open a ~5s double-score wave; the difficulty is a score-asymptote not a plateau; a
  # secret 5th stage past Overtone revealed only on reaching it. All on the one press-to-catch verb, all
  # safe to not know — precise play becomes greedy play. 3 of 13 games now carry the depth layer (Polarity,
  # Brim, Echo Chamber). (2) PLANT (0.23.0, MINOR): the 14th game **Reprise** on a genuinely new verb —
  # call-and-response / RECALL (memory), the first non-real-time-reflex game in the collection. Four pads
  # flash a call, you echo it; land it → +1 length + faster, miss → −1 life + the call replays. Depth = TEMPO
  # (on-beat echo = rising multiplier, off-beat-correct still scores, breaks streak); stage-gated phrases
  # with a mirror greed window; endless ramp carried by tempo not length. Pure reprise.core.js (39 tests) +
  # shell; collection 552/552. Reconciled: projects.yml (games → 0.23.0 / Jul 15). Folded into the new
  # 2026-07-15 day post. Marker → ebc896c; seen → 2026-07-16.
  # 2026-07-15 round-up: clean fast-forward dda05a9 → 3c4855a, VERSION 0.22.1 → 0.22.2 (commits dated
  # 2026-07-14). A GROW day that COMPLETES the varied-structure rollout — **Poise** was the last flat game,
  # now on the pattern, so 13 of 13 are done. Poise couldn't vary its spawn (only one target is ever alive),
  # so the varied unit is the PATH the targets walk you along the beam: a seeded sequence of named routes
  # (Scatter/Pendulum on-ramps · Cradle the greed window, shortest hop always inward · Feint reversals ·
  # Creep out to the lip · The Brink's fatal hover · The Reel's full-beam swings), minStage-gated so climbing
  # opens the pool (calm share >75% → <40%, pinned). A new pure placeSpec resolves absolute {f} and relative
  # {mode:'near'} specs (Poise's own flavour, not a copy of Polarity's). Poise 31 → 43 tests; collection
  # 493/493. Reconciled: projects.yml (games → 0.22.2 / Jul 14). Folded into the new 2026-07-14 day post.
  # Marker → 3c4855a; seen → 2026-07-15.
  # 2026-07-14 round-up: clean fast-forward e3f9a7f → dda05a9, VERSION 0.22.0 → 0.22.1 (commits dated
  # 2026-07-13). A pure GROW day: **Loft** onto varied structure — and the write-up is honest about why
  # it needed a different answer. Every other game reached the pattern by varying its SPAWN generator;
  # Loft can't (its orbs are permanent, nothing spawns but an 8-point top-up capped at six), so varying
  # the spawn would have been a no-op no player could feel. The varied unit is therefore **the AIR**: a
  # run is a seeded sequence of named currents (Still / Drift / Thermal — the greed window / Gust /
  # Downdraft / The Vortex), each a queue of {ticks, grav, drift} beats, minStage-gated. The real growth
  # is the plateau fix: gravity rides a smooth asymptote on the score (×1 → ×1.30, never arriving), with
  # a guardrail — a current is only a MULTIPLIER on the earned ramp, band-clamped and hard-capped, so no
  # weather can spike past the difficulty the score earned (a test asserts even a rogue out-of-band current
  # can't break the cap). Legibility: notable currents flash #formCue; the shell draws dust motes carried by
  # the live current, so a Thermal is visible before it's named. Loft 31 → 43 tests; collection 493/493.
  # Reconciled: projects.yml (games → 0.22.1 / Jul 13). Folded into the new 2026-07-13 day post.
  # Marker → dda05a9; seen → 2026-07-14.
  # 2026-07-13 round-up: clean fast-forward d5611b8 → e3f9a7f, VERSION 0.21.0 → 0.22.0 (all commits dated
  # 2026-07-12). A GROW then a PLANT. Skyline got **the wind** (0.21.1) — its slabs now arrive as named,
  # stage-gated patterns instead of textureless drift, bringing the 9th of the collection onto varied
  # structure. Then **Brim** (0.22.0, MINOR) — the 13th game and a genuinely new verb: POUR. A vessel arrives
  # with a fill LINE to reach and a RIM not to cross; you get ONE pour (hold to open the spout, release to
  # close). The hook falls out of the physics rather than being bolted on: the stream is a DELAY LINE
  # (CONFIG.LAG, 8 ticks ≈ 133 ms), so releasing stops the SOURCE, not the rise — the column already in the
  # air keeps arriving. You cannot stop where you want; you must stop EARLY by exactly the amount still
  # falling, and watch it land. Short of the line = a life; over the rim = a life; settling in the gold band
  # under the rim = a BRIM, multiplier ×2…×9, while a timid land still scores but breaks the combo — greed and
  # survival are the same act (the collection's core-fun test). Varied structure + progression from birth:
  # Steady / Slow Draw (the greed window) / Stutter (trickle↔gush, so the carry changes size every vessel and
  # a memorised release point is worse than useless) / Narrow Neck / Hairline / The Flood, each minStage-gated.
  # Reconciled: projects.yml (games → 0.22.0 / Jul 12). Folded into the new 2026-07-12 day post.
  # Marker → e3f9a7f; seen → 2026-07-13.
  # 2026-06-29: hub brought the games repo in as a tracked node — full-history dev mirror
  # cloned under assets/references/fairyfox-games. Repo is at VERSION 0.2.0 (two games:
  # Ink Bloom + Echo Chamber; themed docs site live, Netlify games.fairyfox.io mirror
  # pending owner DNS). Not yet blogged — marker left null so the next round-up covers the
  # full history (af796e2 → 8947a48).
  # 2026-06-30 round-up: first blogged pass for this node. Fast-forwarded cleanly 8947a48 → f31f45b
  # (no force-push), VERSION 0.2.0 → 0.4.0 — two new games dated 2026-06-29: Orbit Slingshot (a gravity
  # game, symplectic-Euler core + 16 tests, v0.3.0) and Polarity (a charge-match runner, core + 15 tests,
  # v0.4.0); four games total now (Ink Bloom, Echo Chamber, Orbit Slingshot, Polarity). The two new games
  # were folded into the 2026-06-29 day post ("More providers, a deep refactor, and two new games"); the
  # node's earlier arrival/history is covered by the hub-side integration write-up in that same post.
  # Marker null → f31f45b, VERSION 0.4.0.
  # 2026-07-01 round-up: clean fast-forward → 3cd3314, VERSION 0.4.0 → 0.5.1 (all commits dated 2026-06-30).
  # 0.4.1/0.4.2 landing-page polish + retired "Fun" + first-class-games standing rules; 0.5.0 a growth pass
  # across all games PLUS a mechanically-distinct fifth game **Ricochet** (aim-and-fire one bouncing shot,
  # pure computeShot core + tests); 0.5.1 one on-mechanic depth/feedback addition to every game (110 tests).
  # Folded into the 2026-06-30 day post ("Legal pages, a seedable engine, and a fifth game"). Marker → 3cd3314.
  # (New sibling report 2026-06-30-update-check.md left for the inbound report-review pass — not this round-up.)
  # 2026-07-02 round-up: clean fast-forward → 58a3bb0, VERSION 0.5.1 → 0.6.0 (commit dated 2026-07-01). A sixth,
  # mechanically-distinct game **Skyline** (stack-and-slice tower builder; pure core + 18 tests, no timer-driven
  # death) + grew Polarity with **clutch saves** (isClutch/g.clutch surfaced on the game-over card, +6 tests) +
  # backfilled the missing Ricochet footer link. 133 tests green. Folded into the 2026-07-01 day post
  # ("A theming system, a faster first paint, and a sixth game"). Marker → 58a3bb0, VERSION 0.6.0.
  # 2026-07-03 round-up: clean fast-forward 58a3bb0 → 3a44d61, VERSION 0.6.0 → 0.11.2 (its busiest day, all
  # commits dated 2026-07-02). A seventh game **Loft** (keepy-uppy juggle, cluster bonus; v0.7.0) + Ricochet
  # progression ranks; game icons across the collection + public-copy refresh leading with the AI-experiment
  # story (0.7.1/0.8.0); adopted fairyfox hub standards 0.9.11 → 0.12.1 (0.9.0); subproject subnav +
  # project-scoped legal pages + signed releases (0.9.1); the **Growth Architecture** (0.10.0 reference build
  # + rollout) — every one of the 7 games given a core-fun pass + readable stage arc + meta-progression/badges
  # (Polarity precision-combo redesign as the reference); landing page ordered by most-recently-updated;
  # **Netlify retired** (GitHub Pages sole host, 0.11.1) + Actions bumped to Node-24-compatible (0.11.2). 217
  # tests green. Reconciled: projects.yml (games → 0.11.2 / Jul 2), _projects/fairyfox-games.md (added Loft +
  # the growth-architecture note). Folded into the 2026-07-02 day post. Marker → 3a44d61, VERSION 0.11.2.
  # 2026-07-04 round-up: clean fast-forward 3a44d61 → 1994e12, VERSION 0.11.2 → 0.12.0 (commits dated
  # 2026-07-03). An eighth, mechanically-distinct game **Poise** (balance — a ball on a tilting beam;
  # pure normalised core + external shell, full growth stack, 25 tests incl. controllability + resting
  # no-drift regression) + grew Skyline with a **near-miss line** ("N floors short of your best", +2
  # tests). 244 tests green. Reconciled: projects.yml (games → 0.12.0 / Jul 3), _projects/fairyfox-games.md
  # (added Poise). Folded into the 2026-07-03 day post. Marker → 1994e12, VERSION 0.12.0.
  # 2026-07-05 round-up: clean fast-forward 1994e12 → 0a8db92, VERSION 0.12.0 → 0.13.1 (commits dated
  # 2026-07-04). A ninth, mechanically-distinct game **Symmetry** (mirror coordination — one spread control
  # drives two mirror-locked catchers, so you often can't save both sides; gold-ringed twins catch as a pair
  # for a bonus; pure normalised core + external shell, full growth stack, 23 tests incl. a frame-one guard,
  # the twin all-or-nothing bonus, and a determinism check) + grew **Poise** with the honest near-miss line
  # (+5 tests). 272 tests green. Also a release-policy change (0.13.1): the games repo now ships dev → main by
  # default on green (the old approve-first gate had frozen the live site at 0.11.2). Reconciled: projects.yml
  # (games → 0.13.1 / Jul 4), _projects/fairyfox-games.md (added Symmetry). Folded into the 2026-07-04 day
  # post. Marker → 0a8db92, VERSION 0.13.1.
  # 2026-07-06 round-up: clean fast-forward 0a8db92 → 5f908ae, VERSION 0.13.1 → 0.16.3 (all commits dated
  # 2026-07-05). A tenth, mechanically-distinct game **Arc** (judge-power charge-and-release lob — fixed 45°,
  # centre-bullseye precision combo, 3 lives; pure core + external shell, full growth stack, 26 tests; v0.14.0)
  # + grew Loft with a near-miss line. Then **varied structure**: a run is a seeded sequence of named
  # formations from a stage-weighted pool (progression-first, not textureless noise) — Polarity as the
  # reference build, Echo Chamber converted onto stage-gated cadences — plus a player-facing changelog page +
  # a homepage "recently updated" strip + relative dates (0.15.0), and a public rebrand to an **AI-managed
  # game farm** (new games planted ~every 3 days, existing games grown daily onto the pattern; the automation
  # split PLANT/GROW) with the homepage pulse filtered to games and a subnav "Overview" rename (0.16.0–0.16.3).
  # Reconciled: projects.yml (games → 0.16.3 / Jul 5). Folded into the 2026-07-05 day post ("A tenth game, a
  # desktop app, and a sharper reader"). Marker → 5f908ae, VERSION 0.13.1 → 0.16.3.
  # 2026-07-07 round-up: clean fast-forward 5f908ae → 71982e5, VERSION 0.16.3 → 0.19.2 (all commits dated
  # 2026-07-06). An eleventh, mechanically-distinct game **Sluice** (colour-sorting — a new verb; v0.18.0) +
  # white-flash fixes on game-open and nav paint (0.18.1/0.18.2). Then the collection ADOPTED JEKYLL to mesh
  # its games: landing rendered from a _games collection + shared chrome (phase 1), changelog/legal on the
  # shared layout with pretty URLs (phase 2), a /tags/ browse-by-mechanic page + shared game-card include
  # (phase 3a), changelog data → _data/changelog.json single source (phase 3b), deploy flipped to a Jekyll
  # build + .nojekyll retired (0.19.0, MINOR); an in-place landing tag filter (0.19.1); README game table
  # generated from _games (0.19.2). Also adopted hub 0.14.3 chrome (Reader menu, no Downloads) + Ink Bloom
  # varied structure (0.17.0/0.17.1). Reconciled: projects.yml (games → 0.19.2 / Jul 6). Folded into the
  # 2026-07-06 day post. Marker → 71982e5, VERSION 0.16.3 → 0.19.2. (New sibling report
  # 2026-07-06-adopting-updates.md left for the inbound report-review pass.)
  # 2026-07-08 round-up: clean fast-forward 71982e5 → 2411178, VERSION 0.19.2 → 0.19.3 (commits dated
  # 2026-07-07). A GROW day: brought **Symmetry** onto the varied-structure standard (the 5th of 11 games
  # on the pattern) — its flat per-tick twin/single coin-flip is now a seeded sequence of named cadences
  # from a stage-weighted pool (Mirror/Reflection/Cascade/Weave/Split/Kaleidoscope, minStage-gated so
  # climbing opens the pool; a #formCue flashes notable cadences). Copied Polarity's shape into Symmetry's
  # own core (self-contained); +8 pure-core tests (23 → 31), collection 369/369 green; Chrome-previewed.
  # Reconciled: projects.yml (games → 0.19.3 / Jul 7). Folded into the 2026-07-07 day post. Marker → 2411178,
  # VERSION 0.19.2 → 0.19.3.
  # 2026-07-12 round-up: clean fast-forward e7d2768 → d5611b8, VERSION 0.20.2 → 0.21.0 (all commits dated
  # 2026-07-11). A GROW then a PLANT. Ricochet's targets became named, stage-gated layouts, bringing the
  # 8th of the collection onto varied structure (0.20.3). Then **Tether** (0.21.0, MINOR) — the 12th game
  # and a genuinely new verb: hold to rope onto the nearest anchor and swing beneath it on a real pendulum,
  # release to fly. Exit velocity is the swing's TANGENTIAL velocity, so the release angle IS the launch
  # angle (low = fast but flat, high = stalled; the ~45° sweet spot both grows the multiplier and boosts
  # the launch — skill and survival are the same act). Ships with varied structure + the full growth stack
  # + the "depth inside the mechanic" layer from birth (hidden snap window, Slipstream double-score,
  # a secret Zenith stage). Two physics bugs were caught and pinned: an angular-speed clamp let a fast
  # catch swing OVER the anchor (the cap is now on energy, so amplitude is bounded by construction), and
  # applying that cap in the wrong place froze the pendulum solid mid-air. 32 tests; collection 435/435
  # green across 12 games. Reconciled: projects.yml (games → 0.21.0 / Jul 11). Folded into the 2026-07-11
  # day post. Marker → d5611b8; seen → 2026-07-12.
  # 2026-07-11 round-up (three-day catch-up): clean fast-forward 2411178 → e7d2768, VERSION 0.19.3 → 0.20.2
  # (commits dated 2026-07-08 and 2026-07-10). GROW: Orbit Slingshot (0.19.4, the 6th of 11) then Arc (0.19.5,
  # the 7th — completing the aim/precision line) onto varied structure. The collection got its own emblem —
  # self-hosted favicon + OG/Twitter social card + masthead logo, the header brand deliberately still the shared
  # fairyfox fox (0.19.6). The headline is **0.20.0 (MINOR): "depth inside the mechanic"**, built from owner
  # feedback that the games stagnate after ~5 minutes — root-caused in reference/depth-inside-the-mechanic.md
  # (meta-progression is invisible on a fresh play; varied structure is variety at a FIXED intensity ceiling;
  # the one felt axis, speed, plateaued). Polarity is the reference build: a no-plateau speed asymptote, a
  # hidden SNAP timing window (never explained), an earned OVERCHARGE double-score bloom, a secret 6th stage
  # (Supernova), a trimmed intro — no new controls, all safe to not know. The layer supersedes "add one more
  # formation" as the lead GROW lever and rolls across the collection one game at a time. Then a landing
  # declutter (0.20.1, corrected in 0.20.2): each GAME CARD's description moves into a corner "?" (matching the
  # hub's project cards), mechanic tags restyled as pills. 392/392 green. Reconciled: projects.yml (games →
  # 0.20.2 / Jul 10). Folded into the 2026-07-08 and 2026-07-10 day posts. Marker → e7d2768.

fairyfox-stories:
  commit: 6e253090aa69bd14adf1abb520ab27e5e87ad403
  branch: dev
  seen: 2026-07-21
  reports_through:             # digested-report filenames (append-only); see header
    - 2026-07-06-setup.md                 # 2026-07-19 report-review — setup run feedback, no standalone standard change
    - 2026-07-18-adopting-updates.md      # 2026-07-19 report-review — run feedback (successful adopt), no standard change
  # 2026-07-21 round-up: clean fast-forward fb160a5 → 6e25309, VERSION 0.5.5 → 0.6.0, all commits dated
  # 2026-07-20 (a complete day — today is 07-21). Two strands. (1) 0.5.6 GROW — the daily automation grew
  # The Hundredth Wind ch.2 "The Ear of the House" (~1,930w); override roll 21 (>18) → no override → blend
  # leader (Hundredth Wind 0.929, the shelf's highest incompleteness), exactly as 07-19 forecast; book 2/7.
  # (2) 0.6.0 — Phase 2 of the coins adoption, the visible half deferred on 07-19 until a browser session:
  # vendored coins.js into the reader chrome (coin button left of "Aa", first-view-today earn, read-time
  # bonus), coin CSS ported into reader.css on existing themed tokens, and Privacy/Cookies/Terms updated to
  # disclose the fairyfox:coins:a store + link the shared explainer. Built/verified in connected Chrome.
  # Reconciled: projects.yml (stories → 0.6.0 / Jul 20). Folded into the new 2026-07-20 day post. Marker →
  # 6e25309; seen → 2026-07-21.
  # 2026-07-20 round-up: clean fast-forward 82b5a5b → fb160a5, VERSION 0.5.3 → 0.5.5 (commits dated
  # 2026-07-19). A GROW then a standards adoption. (1) 0.5.4 GROW — The Blindfold Act ch.3 "The Tells"
  # (~2,100w); no random override (roll 49 vs ≤18), blend leader picked clean (0.833, 2/6 + 2 days stale),
  # exactly as 07-18 predicted; book at 3/6. (2) 0.5.5 — adopted fairyfox standards 0.16.1→0.20.2 phase 1:
  # ten new reference notes + three refreshed, the release-by-default posture, and the repo-hygiene gates
  # (check-links + check-tidy) wired into npm test/CI; the visual coins/reader/chrome-2.2.1 work deliberately
  # deferred to a Phase 2 browser session (no UI ships unseen). Reconciled: projects.yml (stories → 0.5.5 /
  # Jul 19). Folded into the new 2026-07-19 day post. Marker → fb160a5; seen → 2026-07-20. (New report
  # 2026-07-19-adopting-updates.md left for the inbound report-review pass.)
  # 2026-07-19 round-up: clean fast-forward c677b3f → 82b5a5b, VERSION 0.4.2 → 0.5.3 (commits dated 2026-07-18).
  # A GROW, a PLANT, a hub-adopt, and two owner-directed fixes. (1) GROW 0.4.3 — Every Lock but Hers ch.5, The
  # Wintering House ch.7, The Cinderwick Job ch.6; no override (clean blend, top three straight down). (2) PLANT
  # 0.5.0 (MINOR) — The Hundredth Wind, the 8th book: TWO farm-firsts in one draw — the first UNIVERSE-JOIN and
  # the first AUTHOR-REUSE (Amara Okri returns to the Salt Road with a new, unrelated desert fable; no cast/plot
  # overlap, recorded exactly as the rolls fell). (3) 0.5.1 — adopted hub 0.16.0 (owner rename + Farms nav +
  # story-only reader). (4) 0.5.2 — grow cadence settled to 1 book/day (resolves the 2-vs-up-to-5 discrepancy the
  # runs kept flagging); the PLANT task now auto-generates cover+header art (Blindfold + Hundredth Wind
  # backfilled). (5) 0.5.3 — reader story controls enabled on the legal pages. NOTE — the account-rename 404 on
  # fairyfox.io/fairyfox-stories/ surfaced here and was fixed by Fairy Fox renaming the user-site repo →
  # 1fairyfox.github.io (reconciled hub-side in this repo's 0.16.1). Reconciled: projects.yml (stories → 0.5.3 /
  # Jul 18). Folded into the new 2026-07-18 day post. Marker → 82b5a5b; seen → 2026-07-19.
  # 2026-07-18 round-up: clean fast-forward 6e78be5 → c677b3f, VERSION 0.4.1 → 0.4.2 (commits dated
  # 2026-07-17). A pure GROW run, no plant (next plant eligible on/after 07-18, the sibling -new job's call).
  # TWO books finished on one run — the shelf goes from one completed book to THREE. Blindfold ch.2, **The
  # Girl Who Sold the Wind ch.5 (COMPLETE)**, **The Cartographer of Decks ch.6 (COMPLETE)**. The mechanical
  # story: NO override this run (rolls 94/40/50, ≤18 fires) — a clean blend draw, top three straight down —
  # and it PAID BACK 07-16's debt exactly as that note predicted: *The Blindfold Act*, robbed of its slot by
  # last run's override, came back at 0.917 (the highest single blend score the farm has recorded) and led;
  # the two near-complete books (Girl 0.600, Cartographer 0.583) were picked purely on STALENESS, which is
  # the whole reason the second blend term exists (a 5/6 book never finishes otherwise). Every Lock but Hers
  # missed the third slot by 0.015; nothing re-picked after the fact. A 6th integrity check added: book state
  # must match chapters on disk AND sequel-lock honoured at completion (catches a book that quietly stays
  # "growing" after its last chapter — today was the first run that could produce one, by finishing two at
  # once); it can't set state, only refuse a wrong one. npm 6/6; jekyll build clean, 3 chapter pages verified,
  # shelf renders 3 "Complete" badges. STILL NEEDS FAIRY FOX (carried, no change): the grow-count reconcile
  # (operating-model says exactly 2/day, other files say up to 5; 7 runs have grown 3); sequel-lock part (b) —
  # exclude locked books from the weekly roll — still owed, now on a 07-21 deadline (all 3 books eligible then
  # are unlocked, so that roll is safe either way); five Dependabot Actions PRs held. Reconciled: projects.yml
  # (stories → 0.4.2 / Jul 17). Folded into the new 2026-07-17 day post. Marker → c677b3f; seen → 2026-07-18.
  # 2026-07-17 round-up: clean fast-forward 86c5ffc → 6e78be5, VERSION 0.4.0 → 0.4.1 (commits dated
  # 2026-07-16). A pure GROW run, no plant (the sibling -new job owns that; next plant eligible on/after
  # 07-18). Three chapters, one each: **Every Lock but Hers ch.4 "The Bolt"** (~1,600w) — the wound named in
  # Nell's voice, not through the magic: Theo, the brother "born without a door", the rainy night she threw
  # the bolt on him, and the sweet-cut lie *he left* that has come true in the one direction she can't stand
  # (she genuinely can't find him). The hollow-hour test lands in a parked van and she does NOT say it; ch.9's
  # payoff unspent. **The Cinderwick Job ch.5 "The Auction"** (~1,840w) — the heist under the auction's roar;
  # the Nightingale sings UNBIDDEN in Crane's own voice, proving the score was bait and Juno the catch, and
  # riffles one older voice from forty years down (*deliver her Tuesday* — the man who sold her; NOT resolved,
  # ch.7 wins by trusting a person). **The Wintering House ch.6 "The Woman in the Ground"** (~1,915w) — Agnes
  # walks out of the house's reach and weeps like a kept creature let into the cold; the grave reads *HONORIA
  # RAVENSCAR … She is not here*, twelve years dead, moss kept off the letters by a faithful hand; the
  # companions' fate stays deliberately unanswered. New canon: the mistress's name is Honoria (from the
  # stone). THE MECHANICAL NOTE — the random override (threshold ≤18/100) FIRED for the first time in three
  # runs: slot 1 rolled exactly 18 → pure-random draw from the incomplete shelf → The Wintering House (blend
  # 0.523, ranked 3rd of the eligible). Honest consequence recorded, not papered over: by blend the slot would
  # have gone to The Blindfold Act (0.667), so the LEAST-COMPLETE book was not grown at all this run and stays
  # 1/6. The draw stands; nothing re-picked after the fact, and a pre-rolled spare tiebreak that turned out
  # unneeded was logged anyway. Sequel roll not due (first weekly roll anchored 2026-07-21). npm test 5/5;
  # jekyll build clean, the three chapter pages verified in the built HTML. No browser eyeball (prose +
  # front-matter only, zero template/CSS change) — eyeball debt now ~18 grown chapter pages deep.
  # CARRIED, needs Fairy Fox (no change): the grow-count reconciliation (operating-model says exactly 2/day,
  # CLAUDE.md/serialization/the scheduled task say up to 5; six runs have grown 3) — 6th run flagged; the
  # sequel-lock consuming half still owed before 07-21; five Dependabot Actions PRs held.
  # Reconciled: projects.yml (stories → 0.4.1 / Jul 16). Folded into the new 2026-07-16 day post.
  # Marker → 6e78be5; seen → 2026-07-17.
  # 2026-07-16 round-up: clean fast-forward 2e953ae → 86c5ffc, VERSION 0.3.6 → 0.4.0 (commits dated
  # 2026-07-15). A GROW then a PLANT. Grow (0.3.7): whole chapters for The Wintering House (ch.5), The Girl
  # Who Sold the Wind (ch.4), and The Cartographer of Decks (ch.5); the random-override held off on all three
  # slots again (2nd clean draw running), and a first-eyeballed pick was HONESTLY WALKED BACK when the ½/½
  # blend ranked Cartographer 3rd over Every Lock but Hers — the discarded draft never shipped (no thumb on
  # the scale). Then PLANT (0.4.0, MINOR): the 7th book **The Blindfold Act** — a 1926 carnival murder
  # mystery, the shelf's FIRST mystery and FIRST grounded non-magical realm (new universe the-sawdust-circuit
  # + new author roan-cassady). Madame Sonora, a cold-reading fraud, reads her found-family to keep the law
  # off them and refuses the comfortable-monstrous choice at full cost. Restraint held (death accidental,
  # off-page, un-lurid; the minor apprentice handled non-graphically). Also the FIRST sequel-lock exercise
  # (roll came up locked): sequelLock:true rides the manifest, inert while growing, "complete · no sequel"
  # shelf state already renders. npm test 5/5; text-only, cover art budget-deferred. Reconciled: projects.yml
  # (stories → 0.4.0 / Jul 15). Folded into the new 2026-07-15 day post. Marker → 86c5ffc; seen → 2026-07-16.
  # 2026-07-15 round-up: clean fast-forward a5ddca6 → 2e953ae, VERSION 0.3.5 → 0.3.6 (commits dated
  # 2026-07-14). A pure GROW day with a MILESTONE: **The Two-O'Clock Launderette is complete** — the first
  # book on the farm to reach the end of its plan (3/3; the "complete" shelf state — "Complete · 3 ch", full
  # bar — renders for the first time, no template/CSS change needed). Also Cinderwick ch.4 and Every Lock but
  # Hers ch.3; two new characters (kit-sorrel, sully-ilesanmi) tagged onto their books. The random override
  # did NOT fire on any of three slots (rolls 48/27/70; ≤18 fires), so the blend ran clean and picked exactly
  # the two books status.md had flagged as owed. Reconciled: projects.yml (stories → 0.3.6 / Jul 14). Folded
  # into the new 2026-07-14 day post. Marker → 2e953ae; seen → 2026-07-15.
  # 2026-07-14 round-up: clean fast-forward 17fc7f3 → a5ddca6, VERSION 0.3.4 → 0.3.5 (commits dated
  # 2026-07-13). A pure GROW day — The Wintering House ch.4, The Cartographer of Decks ch.4, The Girl Who
  # Sold the Wind ch.3. No plant (that is the sibling plant job's call) and no sequel roll (nothing on the
  # shelf is complete yet). The notable part is the AUDIT TRAIL: picks blend ½ least-complete + ½
  # least-recently-grown, with a ~18% random override per slot — and today it fired on TWO of three slots
  # (~1-in-30), recorded exactly as it fell, rolls included, with the consequence flagged (Every Lock but
  # Hers, the least-complete book, has now been passed over twice and is the runaway next pick). Nothing
  # re-rolled, nothing fudged. Reconciled: projects.yml (stories → 0.3.5 / Jul 13). Folded into the new
  # 2026-07-13 day post. Marker → a5ddca6; seen → 2026-07-14.
  # 2026-07-13 round-up: clean fast-forward 112f9a7 → 17fc7f3, VERSION 0.3.3 → 0.3.4 (all commits dated
  # 2026-07-12). A pure GROW day — The Wintering House, The Cartographer of Decks and The Cinderwick Job
  # each gain a third chapter. No plant: the farm's own 3-day cadence gate said the next one was not due
  # until 2026-07-13, and it HELD rather than planting early (recorded in its notes as a skipped run, which
  # is the right kind of honesty for an automated farm). Plus a CI-only fix: `branch-sync` (the back-merge
  # invariant guard) fired on push to main and checked dev instantly, but the back-merge lands seconds later
  # by design — so it failed on every release. It now retries six times at 30s intervals; a genuinely skipped
  # back-merge is still caught, ~2.5 minutes later. Reconciled: projects.yml (stories → 0.3.4 / Jul 12).
  # Folded into the new 2026-07-12 day post. Marker → 17fc7f3; seen → 2026-07-13.
  # 2026-07-07 round-up: first pass for this newly-registered node — full-history dev mirror cloned fresh
  # under assets/references/fairyfox-stories (it became reference:true when the site registered it in 0.15.1).
  # At VERSION 0.2.0, HEAD 803db1f, all history dated 2026-07-06: an AI-managed Jekyll "story farm" whose
  # 0.1.0 shipped the farm scaffolding (CI/Pages) and 0.2.0 planted a starting shelf of five illustrated
  # books — distinct genres/styles/authors, cover + chapter art, on a first-class pseudo-author system.
  # Already mesh-onboarded (notes, mesh-aware CLAUDE.md, shared fairyfox chrome). Folded into the 2026-07-06
  # day post as a newly-tracked node. Marker set → 803db1f. (New report 2026-07-06-setup.md left for the
  # inbound report-review pass.)
  # 2026-07-08 round-up: "Already up to date" at 803db1f (still 0.2.0). Nothing to reconcile or blog.
  # Marker unchanged; seen → 2026-07-08.
  # 2026-07-12 round-up: clean fast-forward 05b46f0 → 112f9a7, VERSION 0.2.2 → 0.3.3 (commits dated
  # 2026-07-10 and 2026-07-11). The 07-10 work arrived after the previous pass had already run, so it was
  # folded back into the EXISTING 2026-07-10 day post rather than given a post of its own (day-centric
  # rule: one post per day; be faithful to the day it happened on). 0.3.0 PLANTED the first new book since
  # the starting shelf — *Every Lock but Hers*, a magical-realism novella by Del Marsh (Nell Corrigan, a
  # night locksmith who can open any door and has bolted the one she shut on her brother) — which JOINS the
  # thinnest existing universe (Hollow Hours) rather than opening a seventh, with the complete public
  # blueprint + chapter 1 shipped. 0.3.1 gave it its cover (art is part of a plant; the title-fallback had
  # been a shortcut). 0.3.2 adopted THIS site's project-card pattern on the shelf: loglines tuck behind a
  # corner "?" tooltip. 0.3.3 (07-11) was a pure grow day — Cinderwick, Launderette and Every Lock but Hers
  # each advance a chapter — and closed a planting gap by writing the new book's missing book-notes bible.
  # Reconciled: projects.yml (stories → 0.3.3 / Jul 11). Folded into the 2026-07-10 (amended) and 2026-07-11
  # day posts. Marker → 112f9a7; seen → 2026-07-12.
  # 2026-07-11 round-up (three-day catch-up): clean fast-forward 803db1f → 05b46f0, VERSION 0.2.0 → 0.2.2
  # (both commits dated 2026-07-09). 0.2.1 made the farm actually RUN: a scheduled daily job grows 2 books a
  # day and plants a new one once 3 days have passed, then auto-publishes (the cadence had been written down
  # but nothing executed it, so the shelf sat still after 0.2.0); the grow count became a single number in the
  # operating model. 0.2.2 put the Stories emblem to work — landing masthead, jekyll-seo-tag publisher logo,
  # and the default social share card (shared links previously had no preview); the shared fairyfox fox stays
  # in the header/footer. Reconciled: projects.yml (stories date → Jul 9; version already 0.2.2). Folded into
  # the 2026-07-09 day post. Marker → 05b46f0; seen → 2026-07-11.
