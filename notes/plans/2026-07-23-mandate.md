# Mandate — project-detail/badge defaults, phased work, web-interface enforcement (2026-07-23)

Owner directive transcribed verbatim per clause ([`mandate-ledger`](../../hub/standards/mandate-ledger.md)).
Completion claims cite rows; a green release mid-mandate says "rows remaining: N".

| # | Owner clause (verbatim / condensed) | Interpretation | Status |
|---|-------------------------------------|----------------|--------|
| 1 | "projects in fairyfox need to by default have the project details like description and tags and other stuff not social image i get you cant do that automated but this needs to be a default for targets onboarded unless an exception is made on the onboarding end specifically from the user never from the ai itself." | Onboarded projects must have their **project details** (registry/`_data/projects.yml`: blurb/description, tags, lifecycle, version, activity, links, accent, icon…) populated **by default**. The **social/preview image is the one manual exemption** (can't be auto-made). Governance: **omission only by an explicit exception the USER grants at onboarding — never the AI's own judgment.** | done — rows added to both runbooks' Register step + Verify (project-details completeness + user-only-exception + social-image exempt) |
| 2 | "So these are the badges on the README, all of them are required in this order and equiv ones need to be swapped out or added/expanded like for different projects like not all will have netlify and some may have published packages. Contributors Stars Forks Watchers Last commit Commits Version Node CI Engine coverage Code quality Quality gate Tech debt OpenSSF Scorecard Docs Netlify Open issues Closed issues Open PRs Closed PRs License." | The full **20-badge set is REQUIRED BY DEFAULT, in this fixed order**. Per-project you **swap the equivalent** (version tag↔package-json; CI/coverage/deploy variants) or **add/expand** (distribution badges for published packages) — you do **not** drop. Same governance as #1: omit only on a **user-granted** onboarding exception. ("Node CI" = the CI badge, "Engine coverage" = the coverage badge — the runtime is the qualifier, not two extra badges.) | done — `badges.md` rewritten to default-required + fixed order; `README-badges.md` reordered; compliance/runbooks reframed |
| 3 | "The compliancy checks should also check for that so if its not there it should come up as would everything else." | The compliance audit + Verify sections must catch a missing badge / missing project-detail the same as any other gap. | done — `badges.md` `## Verify` + `compliance.md` badges row + runbook Verify rows updated |
| 4 | "It needs to be enforced in CLAUDE.md and notes for projects to break any request or task or whichever up into as many phases as needed for planning for research for implementations of different phases. It really needs to be broken up into as many phases as needed on those different levels by default. Research often needs to be done locally and/or online this doesnt need to be skipped or rushed, planning needs to happen often whenever needed and its mainly for the ai. These are important things for projects to always follow." | **Phase-by-default:** decompose any non-trivial request into as many phases as needed across three levels — **research** (local + online; not skipped/rushed; primary sources), **planning** (as often as needed; primarily for the executor/AI), **implementation** (itself phased). Enforce in `planning.md`, the CLAUDE template's Default Workflow, and this repo's `CLAUDE.md`. | done — `planning.md` phased section + Verify; template + this `CLAUDE.md` updated |
| 5 | Web interface still poorly implemented; language leaves vagueness. Sub-problems below. | Harden the docs-site standard so a chrome-wearing site is right the first time. | done — see 5a–5g |
| 5a | "fighting with the default styling if the tool has it is common like default footers meshing in with the chrome footer" | The tool's **own default chrome must be removed/replaced, not left alongside** the fairyfox chrome (double footers/headers/sidebars/theme). Generalized beyond generators to any stack. | done — `06` + `12` + `08` |
| 5b | "sometimes it doesnt even know the chrome styling or a chunk of it should be used" | The chrome bundle is **all-or-nothing and mandatory** — copy the whole bundle verbatim (all four HTML parts + all JS + CSS + fonts); **read the bundle files, don't guess**. | done — `12` + `chrome/README` posture reinforced in `12` |
| 5c | "coins is a common thing determined not needed even though its mandatory" | The **coin counter/button (shared `coins.js`) is MANDATORY** on every chrome-wearing page — NOT a project's judgment call to drop. Only project-**added** coin moments are optional. | done — `coins.md` (loud mandatory note) + `08` + `12` |
| 5d | "the subnav bar is often the poorest done often missing all the buttons it should have and all the structure is totally different… useless right out of the box… i sometimes have to verbatim copy the nav links on random-ai-prompt" | Firm **subnav baseline** (Overview · Project Notes + section doors · Changelog · API when API docs exist · Download when downloads · Legal · Repository↗·Notes↗). **No bare/near-empty subnav** — building the expected pages is the default. Point at a **worked example to copy**, not hand-invent. | done — `05` subnav section strengthened |
| 5e | "It gets dividers wrong" | Explicit **zone-divider** rule in the subnav. | done — `05` |
| 5f | "API pages often get littered on every page… theres often not even a link for it on the navbar it often leaves the sidebar full of defaults" | **API output confined to the API section only**; an **API subnav link is required when API docs exist**; the generator's **default sidebar/footer pruned to API-only** (no leaked defaults). | done — `06` + `08` (already partly present; made firmer + link-required) |
| 5g | "One project literally rolled out the bog standard defacto default built site with the only change being 2 colors and a single mention of fairyfox.io in the footer… then said this is completely all done exactly to the specs." | Explicit **anti-pattern**: a default-theme tool build with cosmetic-only changes (a couple colors + one footer mention) is **`missing`, not `partial`, not done** — look at the actual page; ties to checklists-are-contracts honesty. | done — `08` + onboarding/setup Row 6 |
| 5h (mid-turn) | "the notes section often needs a total rework… disconnected set of links… Notes should never be an external link to github… always a fully navigable online interface with reader settings only in the content pane… a sidebar that has different categories with a way to navigate back out to allow navigating nested categories… actually look good and polished." | Notes = **on-site, fully-navigable, polished interface, never a GitHub link**: root note + jump-off cards, a **scrolling sidebar by category with nested drill-in *and* back-out**, `data-read` note pages whose reader controls reflow the content pane, designed look. | done — `06` Notes section + `08` + `05` (Project Notes on-site) |
| 5i (mid-turn) | "reader settings are applied wrong or not at all." | Reader controls must **actually work** on readable pages (content pane reflows); landing/index/sidebar omit `data-read`. | done — `06` + `08` |
| 5j (mid-turn) | "projects still often pick a different button to highlight like 'Docs' instead of 'Projects'." | Restated the **active primary-nav = `Projects` always** rule where it's most missed. | done — `05` + `08` already carried it; `05` restated |
| 5k (mid-turn) | "default styling… links and stuff often get overriden by the default styles… fix often in 1-3 phases." | Universal rule: **replace the tool's stylesheet from scratch, don't override** — the chrome's links/type/palette must render, not the tool's defaults. | done — `12` (universal) + `06` Case B already |
| 6 (mid-turn) | "the legal stuff projects do them wrong… no idea what to do, how to maintain them, what to include… footer links sometimes get derailed." | Broke legal-docs into **what each page covers**, **how to maintain**, and **where the links live (don't derail the vendored footer)**; added Verify rows + an `08` check. | done — `legal-docs.md` + `08` |

**Release shape:** MINOR — `1.3.0 → 1.4.0` (mesh-wide behavior change across many standards).
Branch `dev`; `hub/standards/CHANGELOG.md` `## 1.4.0`; regenerate `VERIFY-INDEX.md`; build + link-gate.
Hold the protected-`main` PR for an explicit go-ahead (report-first).

**Rows remaining after this pass:** 0 (all fixed in-spec; the node-side *behavior* change lands as siblings re-adopt on their next fairyfox pull).

## Follow-on items (later)

- **Docker standard — DONE (1.5.0).** Owner: use Docker whenever possible; Linux-only build/test/
  setup runs locally (Docker-on-Windows), not deferred to online CI; fix Docker issues, don't
  write them off. New `hub/standards/docker.md` + compliance row + CLAUDE-template Build/Run rule.

- **Retire `fairyfox.io` → `1fairyfox.github.io` — DECIDED (retire entirely), timing HELD.**
  Owner chose to retire the custom domain for the free github.io URL; keep the org/package names
  the same; the **replacement domain + contact email come later "with the free email."**
  - **Scope:** 341 `fairyfox.io` refs across 103 files, but ~30 are **dated `_posts` that must NOT
    be rewritten** ([`docs-lifecycle`](../../hub/standards/docs-lifecycle.md)) and much of the rest
    is brand *text*, not URLs. Real URL surface: `CNAME` (remove), `_config.yml` `url`,
    `_data/projects.yml` `docs:`/`doc_url:`, the **badges docs link** (just set to `fairyfox.io/<key>/`),
    the chrome `head/header/footer`, and the `docs-site/10-domain-and-publishing` + `deployment`
    standards; plus the live Pages custom-domain setting (repo settings / DNS).
  - **Recommendation (why held):** the live-domain teardown is **irreversible-ish and has taken the
    site fully dark before** (the account-rename 404), and a replacement domain is imminent — so
    doing it **now and again** when the replacement lands = migrating twice. Recommend **one
    migration, executed when the replacement domain + email are ready.** Await owner timing (or an
    explicit "do the github.io interim now anyway").
  - **Replacement contact email:** swap the `…@fairyfox.io` contact across the legal templates +
    pages when provided (arrives "with the free email").
