# 2026-07-23

_Newest entry on top._

## Continuation — residual items from the report-review (Phases A + B)

After 0.21.0 shipped, worked the residuals I'd disclosed.

**Phase A — retroactive claims-vs-tree spot-check (the S4 follow-up).** Ran the new
report-review step 3a across all five node clones (read-only; anti-recursion held). **Every
node's adoption claims hold against its tree** — the one blemish is a cosmetic path typo in
the games report (`games/_shared/…` vs the real `games/shared/coins-earn.core.js`; artifact
present). despawned-items' S1/S6/S7 reference implementations are genuinely present. Recorded
as an addendum in `fairyfox-reports/2026-07-23-report-review.md`.

**Phase B — self-host the fonts (0.22.0).** Remediated the fonts half of the hub's
`self-hosted-assets` exception (pse2's standing ask). Vendored the three OFL variable woff2
(from the stories clone's verified set) into `assets/fonts/` + a local `fonts.css`; rewired
`_includes/head.html` + the chrome bundle `head.html` (bundle → 2.3.0) off Google Fonts;
kept `legal/{privacy,cookies,terms}.md` accurate (only the cdnjs/Font-Awesome icon request
remains, dated July 23). Verified: Jekyll build green, **0** `googleapis`/`gstatic` refs in
`_site`, all three woff2 + `fonts.css` shipped, `@font-face` family names exact-match main.css's
`--display/--sans/--mono` tokens (so type binds, not falls back). VERSION `0.21.0 → 0.22.0`.

**Still deferred (evidence-backed):** Font Awesome / cdnjs self-hosting — the FA files aren't
in any clone (siblings carry no FA) and can't be fetched in this environment; the ~22 project-card
icons include brand marks, so hand-authored SVGs risk inaccurate glyphs. Recorded as the open
remainder of the self-hosted-assets exception with a clear remediation path (vendor the FA subset,
or inline-SVG the set, then drop the cdnjs link).

**Phase C — generated Verify-index (0.22.1).** Built `hub/standards/build-verify-index.mjs`
(zero-dep) and generated `hub/standards/VERIFY-INDEX.md` — every standard's `## Verify` in one
page (27 indexed), a derived artifact that keeps the standards canonical; compliance.md points at
it. Closes the standards-audit's last ask.

**Phase D — self-host Font Awesome (0.22.2).** On the owner's steer ("use PowerShell to download
it — it's what you've always done"), downloaded Font Awesome Free 6.5.2 via `Invoke-WebRequest`
(the sanctioned local tool, same class as the git/gh/npm calls) and vendored it under
`assets/vendor/font-awesome/`; rewired `_includes/head.html` off cdnjs. With fonts already
self-hosted, the hub now makes **no third-party presentation request** — the `self-hosted-assets`
exception is fully closed, and the legal pages updated to match. My earlier "can't fetch binaries
here" was wrong: local PowerShell downloading an openly-licensed asset is ordinary vendoring, not
the restricted web-content path. Verified: build green, 0 cdnjs/googleapis/gstatic refs in `_site`.

## Process-report review pass + fold-back → standards 0.21.0

Ran the inbound **report-review pass** (`hub/standards/process-reports.md`) on an explicit
*fairyfox*-named request with an upfront go-ahead to carry the fold to completion.

**Check + read.** Refreshed all five reference clones (pse2 + random-ai-prompt already current;
papermc-despawned-items → `f3a2d01`/v1.5.0, fairyfox-games → `898a6de`/0.25.2, fairyfox-stories →
`ecc53f9`/0.7.1). Read **15 reports** past their `reports_through` markers. Hub clean on `dev` at
0.20.7 before starting — no concurrent live session.

**Synthesis.** The dominant, owner-confirmed pattern: "whole checklists get skipped while marked
done" — the mesh's "done = artifact exists" vs. the standards' "done = every `## Verify` passes",
with `compliance.md` opt-in and never invoked. Same disease in adoption, owner-mandates, and testing.
Plus cross-node patterns: no standards/bundle version stamp (3 nodes), coins-inside-chrome coupling
(3), check-links false positives (2), agent-tooling gotchas (3), a JVM/Gradle/Dokka blind spot.
Full itemized ledger in [`plans/2026-07-23-report-review-foldback.md`](../../plans/2026-07-23-report-review-foldback.md).

**Fold (0.21.0, MINOR).** 26 items across the standards: 2 new standards
(`checklists-are-contracts`, `mandate-ledger`), the adoption-manifest enforcement pulse
(manifest template + adopt/onboard record Verify + release reads the manifest + report-review
spot-check), the ship contract, supply-chain/dependencies/testing hardening, the adaptive docs-site
subnav + Dokka adapter + chrome CHANGELOG, and a batch of coins/legal/repo-hygiene/agent-tooling/
onboarding clarifications + `check-standards.mjs` + a `branch-sync.yml` race fix + a
`hub/standards/CHANGELOG.md`. See the 0.21.0 changelog entry for the file-by-file breakdown.

**Markers + report.** Advanced every digested report in `hub/.last-seen.yml reports_through`
(including papermc-despawned-items' first eight). Wrote the hub's own process report
[`fairyfox-reports/2026-07-23-report-review.md`](../../fairyfox-reports/2026-07-23-report-review.md)
— fairyfox.io is a node too.

**Deferred (honest not-done):** self-hosting the docs fonts *inside* the chrome bundle (pse2's
standing ask — a bundle-content change deserving its own pass, not this standards fold); an
auto-generated one-page index of every `## Verify` table (a stretch beyond the `standards/CHANGELOG.md`
that shipped). Both recorded in the plan as `gap(next pass)`.
