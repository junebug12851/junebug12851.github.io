# 2026-07-06

_Newest entry on top._

## Maintenance round-up — reconcile to RAP 2.43.1 + Games 0.16.3, blog the 5th (0.14.4)

Scheduled maintenance pass over the whole site surface (data files + nodes + docs), not a
blog-only pass.

**References refreshed (all clean fast-forwards on `dev`):**

- **random-ai-prompt** `5dcd621 → 8f56153`, VERSION `2.42.1 → 2.43.1` (all commits dated
  2026-07-05). Turned outward: a **pre-built desktop edition** on Tauri (thin native shell
  running the unmodified local app + Node backend as a bundled sidecar; user data preserved
  across upgrades) and an "every edition ships pre-built" reframing — hosted online build, a
  self-hostable copy of it, and the desktop installers all interchangeable (2.43.0, MINOR).
  Plus a full **docs-site rebuild** — hub header + organized subnav, self-hosted fonts,
  centred reading column, softer light/sepia themes, a reader menu, full SEO/social/sitemap,
  a WCAG 2.1 AA pass (0 axe violations), sidebar confined to the API pages, then adopting the
  hub's shared chrome + reader menu directly on the `fairyfox:reader:b` key (2.43.1); README +
  CONTRIBUTING rewritten.
- **fairyfox-games** `0a8db92 → 5f908ae`, VERSION `0.13.1 → 0.16.3` (all commits dated
  2026-07-05). A tenth game **Arc** (judge-power charge-and-release lob, centre-bullseye
  precision combo, 26 tests; v0.14.0) + a Loft near-miss line; then **varied structure** — a
  run is a seeded sequence of named formations from a stage-weighted pool, progression-first
  (Polarity the reference build, Echo Chamber converted onto cadences) with a player-facing
  changelog page + a homepage "recently updated" strip + relative dates (0.15.0); and a public
  rebrand to an **AI-managed game farm** (plant a new game every few days, grow one daily; the
  automation split PLANT/GROW) with the homepage pulse filtered to games and a subnav
  "Overview" rename (0.16.0–0.16.3).
- **pokered-save-editor-2** — "Already up to date" at `1bae326`, still `0.14.2-alpha`. Nothing
  to reconcile or blog.

**Components reconciled:**

- `_data/projects.yml` — RAP → `2.43.1` / Jul 5 (blurb now notes the pre-built + desktop
  editions; added a `Desktop` tag); Games → `0.16.3` / Jul 5; site meta row → `0.14.4` / Jul 6.
  PSE2 and the predecessor left as-is (accurate).
- `_data/pulse.yml` — refreshed the `changes` ticker to the genuinely recent 2026-07-05
  highlights (Arc / varied structure / game-farm; RAP desktop + docs; the site reader/theme
  work), re-dated `when` relative to today, and shifted the `bars` window by one day.
- `hub/registry.yml` — no change needed (carries no version fields; branches/flags accurate).
- Node pages — none to touch (the `/projects/<key>/` node pages were retired in 0.13.0).
- `_data/downloads.yml` — none (the Downloads section + data were removed in 0.13.1).
- Docs library (`_docs/` + `_data/doc_categories.yml`) and the About page — reviewed, already
  accurate (no hub-standard or docs-location change this cycle).

**Blogged:** wrote `_posts/2026-07-05-a-tenth-game-a-desktop-app-and-a-sharper-reader.md` —
a day-centric post across all three threads (tags `fairyfox-games`, `random-ai-prompt`,
`site`, `update`), covering Arc + varied structure + the game-farm framing, RAP's desktop
edition + docs rebuild, and the site's own reader-panel/theme rework that shipped 2026-07-05.

**Markers advanced** in `hub/.last-seen.yml`: RAP → `8f56153`, Games → `5f908ae`, PSE2 seen →
2026-07-06 (marker unchanged). Untracked inbound report files under
`notes/fairyfox-reports/inbound/` were left alone (report-review pass, not this one).

**VERSION:** `0.14.3 → 0.14.4` (PATCH — maintenance reconcile + blog). Changelog entry rides
in the commit.

**Build:** `JEKYLL_ENV=production bundle exec jekyll build` — _(result recorded below)._
