# Session — 2026-07-11

_Newest on top._

## Maintenance round-up (0.15.8) — three days of siblings, five standards into the docs library

Scheduled maintenance pass. PowerShell + Read/Write/Edit only (no bash).

**Starting state.** `dev` clean and in sync with `origin/dev` at `980c6ac` (0.15.7), except for
an **uncommitted `_data/projects.yml`** left behind by the previous pass — it had refreshed the
references and started the reconciliation while the 2026-07-09/10 live card session was running,
then held off shipping (the correct concurrency posture). Its edits were stale by two days, so
they were re-derived from scratch against the refreshed mirrors rather than trusted. The
long-standing untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings were left untouched.

**References refreshed** — all four clean fast-forwards, no force-pushes:

| Node | Marker | VERSION |
|------|--------|---------|
| pokered-save-editor-2 | `acc09b9` → `43effcf` | 0.14.2-alpha → **0.14.6-alpha** |
| random-ai-prompt | `3147535` → `3686435` (84 commits) | 2.50.1 → **2.52.0** |
| fairyfox-games | `2411178` → `e7d2768` | 0.19.3 → **0.20.2** |
| fairyfox-stories | `803db1f` → `05b46f0` | 0.2.0 → **0.2.2** |

**Reconciled the data-driven components.**

- `_data/projects.yml` — all four sibling rows to their real current release and date; RAP's blurb
  and tags gain the **Android** app (alongside the ComfyUI node and the CLI); Stories' date corrected
  to Jul 9 (the previous pass had it a day ahead); the site's own meta row → 0.15.8 / Jul 11.
- `_includes/project-card.html` — `Android` (fa-android) and `Mobile` (fa-mobile-screen) added to the
  tag → icon map, both in the `platform` group.
- `_data/pulse.yml` — ticker rewritten to the genuinely recent changes across all four nodes; bars
  shifted three days; each project given its own colour (Games had been sharing RAP's green).
- `hub/registry.yml` — checked, already accurate (no change).
- Node pages / `downloads.yml` — n/a: retired in 0.13.0/0.13.1.

**Reconciled the docs library.** `hub/standards/` has shipped **supply-chain-hardening**,
**dependencies**, **legal-docs**, **agent-tooling** and **badges** since 0.12.0, but `_docs/` never
surfaced them — the library was five standards behind the hub it documents. Added all five under
**Shared standards** (orders 15–19). `_data/doc_categories.yml` needed no change.

**Blogged three days** (day-centric, per `notes/reference/blogging-workflow.md`):

- **2026-07-08** — RAP's ComfyUI custom-node target (2.51.0) + the headless `/api/prompt` routes
  behind it, the dark-mode DPL editor fix (2.51.1); Games: Orbit Slingshot onto varied structure.
- **2026-07-09** — RAP starts a native **Android** edition, built on a loader proven byte-identical
  to the web catalog; Stories' farm automation finally runs (grow 2/day, plant every 3rd day) + its
  emblem; PSE2's single-type save-file fix; this site's card "?" (0.15.4).
- **2026-07-10** — RAP 2.52.0 mobile parity (Manage port, live overlay, strict per-surface gates,
  tablet layouts, an 80-test harness); Games' **depth inside the mechanic** (Polarity as the
  reference build — snap, overcharge, a secret sixth stage); PSE2's rebuilt trainer card + the
  debug automation harness; this site's icon tech row (0.15.5–0.15.7).

Markers advanced for all four nodes. Their pending process reports were left for the inbound
report-review pass — that is a separate procedure, not this one.

Verified with a local production `jekyll build` and a headless Chrome screenshot of the home page
and `/docs/` before shipping.
