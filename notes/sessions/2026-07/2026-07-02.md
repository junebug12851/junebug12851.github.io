# 2026-07-02 — Session log

_Newest on top._

## Fold RAP's proposals into six new/updated standards (0.12.0)

Owner asked to comprehensively review Random AI Prompt and adopt as much as possible into
mandatory mesh-wide standards (plus its README badges). This is the hub's inbound
report-review duty: RAP had **six undigested `notes/fairyfox-reports/` proposals** past its
`reports_through` marker, all node → hub standard proposals. Read all six + RAP's
status/README + the existing hub standards; the automated inbound briefing
(`INBOX-2026-07-02-0202.md`) had already synthesized the same themes (good cross-check).

Reported findings and got the go-ahead in-session, with two owner scope calls via
AskUserQuestion: **branch protection mandatory for all repos**, **legal-docs mandatory for
all repos**. Wrote a plan (`notes/plans/2026-07-02-adopt-rap-standards.md`), then executed
as one MINOR release:

- **Fixed** `git-workflow.md` — the back-merge defect (`dev` must contain `main` after every
  release) + never-commit-on-`main` + feature-branches-unCI'd + a PR-based release path for
  branch-protected repos; new `branch-sync.yml` guard.
- **New standards:** `supply-chain-hardening.md`, `dependencies.md`, `legal-docs.md`,
  `agent-tooling.md`, `badges.md` — each with a `## Verify` slice + a `compliance.md` row.
- **Edited** `process-reports.md` — named the node → hub `propose-standard` report shape.
- **New templates:** `project.gitattributes`, `SECURITY.md`, `dependabot.yml`,
  `branch-sync.yml`, `README-badges.md`, `legal/*.html`.
- **Establishment:** `compliance.md`, the `CLAUDE.md` template, all three lifecycle runbooks,
  and both hub READMEs updated; RAP's `reports_through` advanced past the six reports.

PowerShell + Read/Write/Edit only (no bash). Jekyll build green. Process report written to
`notes/fairyfox-reports/2026-07-02-fold-rap-proposals.md`. Out of scope this pass: the
fairyfox-games setup-mirror gap (a different node's report), and actually enabling branch
protection on each node's repo (node-side adoption).

## Scheduled maintenance round-up — reconcile to RAP 2.38.1 + Games 0.6.0, blog the 1st (0.11.2)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree was clean on `dev`
beforehand — the only untracked files were the `notes/fairyfox-reports/inbound/INBOX-*.md` briefings
from the automated inbound report-review procedure, left untouched (not this pass's job).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `c38373d → 2ed5787`, VERSION `2.35.1 → 2.38.1`. All commits
  dated 2026-07-01. No force-push (plain ff under the 0.9.6 model).
- **fairyfox-games** — clean fast-forward `3cd3314 → 58a3bb0`, VERSION `0.5.1 → 0.6.0`. A sixth game
  (Skyline); commit dated 2026-07-01.

**What the siblings shipped (2026-07-01).**

- **RAP 2.35.2–2.38.1.** A day of foundations. A floor-up **CSS overhaul + theming framework** (Phases
  0–7): the ~4,600-line `styles.css` monolith split into a modular `styles/` tree on a two-tier token
  system + explicit cascade layers, then a **System/Dark/Light** theme engine, **9 accent presets**, a
  header **Appearance picker**, built-in themes restructured as a folder of one-JSON-per-theme files, and
  **user theme import/export** (strict validation, device-stored). A **web-perf** pass moved the ~430 KB
  prompt corpus off the first-paint path (2.37.1) and then had the online build **prerender its first
  paint to static HTML + hydrate** (2.38.0) — mobile Lighthouse Performance mid-50s → 80s, A11y/BP/SEO
  stay 100, returning visitors' settings survive hydration. A **SonarCloud tech-debt sweep** cleared all
  **70 code smells** (incl. 7 ReDoS regex rewrites closing a real super-linear parser class + 7
  cognitive-complexity refactors), resolved the last bug and two vulnerabilities as documented
  suppressions, and fixed the quality-gate coverage measurement — board now a clean zero. Plus a full
  dependency upgrade (2.35.2), a stylelint CSS gate (2.35.3), and a git-workflow back-merge (main→dev)
  fix + branch-sync guard.
- **Fairy Fox Games 0.6.0.** A mechanically-distinct sixth game **Skyline** (stack-and-slice tower
  builder — drop each sliding slab, only its overlap with the one below survives, a flush drop pays
  double; pure core + 18 tests, notably no timer-driven death), **Polarity** grown with **clutch saves**
  (a last-moment-flip match tallied by the core and surfaced on the game-over card), and a backfilled
  Ricochet footer link. 133 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.38.1 / Jul 1; games → 0.6.0 / Jul 1; the
fairyfox.io meta card → 0.11.2 / Jul 2), `_data/pulse.yml` (ticker re-led with the 07-01 highlights,
`when` rolled, bars rolled), `_projects/random-ai-prompt.md` (themeable interface + online-prerender
note), `_projects/fairyfox-games.md` (added Skyline as the sixth game).

**Verified accurate, left untouched:** `_data/downloads.yml` (RAP source/run-only — no new pinned
binaries; PSE2/PSE1 releases unchanged; games has no downloads), `hub/registry.yml` (no version fields),
`_data/doc_categories.yml`, the `_docs/*` per-project pages (version-agnostic), `about.md`
(lifecycle-based, no hardcoded versions/counts), header/footer nav (registry-driven).

**Blogged.** `_posts/2026-07-01-a-theming-system-a-faster-first-paint-and-a-sixth-game.md` (tags
`random-ai-prompt, fairyfox-games, update`), day-centric. The site's own 07-01 work was just the routine
maintenance bump (0.11.1), not a notable feature — no `site` tag this day. Markers advanced in
`hub/.last-seen.yml` (RAP → `2ed5787`; games → `58a3bb0`; PSE2 unchanged; all `seen` → 2026-07-02).
`reports_through` left alone — the new sibling process reports (RAP ×3) are for the inbound report-review
pass, not the round-up.

**Notes/version.** July changelog entry added atop `notes/version/2026-07.md`; `VERSION` `0.11.1 →
0.11.2`; this session log; `status.md` version line + sibling version numbers refreshed. Build-checked
before shipping.
