# Session — 2026-07-08

_Newest on top._

## Maintenance round-up — reconcile to RAP 2.50.1 + Games 0.19.3, blog the 7th (0.15.3)

Routine scheduled "usual stuff" pass over the whole surface. PowerShell + Read/Write/Edit
only (no bash). Tree clean on `dev` beforehand except the pre-existing untracked
`notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched — inbound
report-review's job). No sign of a concurrent live session (branch up to date with
`origin/dev`, no unexpected staged files).

**References refreshed** — all clean fast-forwards on `dev`, no force-push:

- **random-ai-prompt** `5ea0761 → 3147535`, `2.49.6 → 2.50.1` (commits dated 2026-07-07).
  The **`prompt` CLI** (`targets/cli/`, `2.50.0`, MINOR) — a full args-and-flags command-line
  edition covering every feature, reusing the same engine/providers/settings/store as the app,
  with `--help`, colours, and bash/zsh/fish/PowerShell completion. Then `2.50.1`: retired the
  pre-DPL `chaos` knob (byte-identical output) and extracted a shared engine-owned
  `engine/promptRun.js`. ComfyUI target planned, not built.
- **fairyfox-games** `71982e5 → 2411178`, `0.19.2 → 0.19.3` (commits dated 2026-07-07). A GROW
  day: Symmetry onto varied structure (5th of 11 games) — seeded cadence sequence from a
  stage-weighted pool; +8 pure-core tests (369/369).
- **pokered-save-editor-2** — up to date at `acc09b9`, still `0.14.2-alpha`.
- **fairyfox-stories** — up to date at `803db1f`, still `0.2.0`.

**Reconciled:**

- `_data/projects.yml` — RAP → `2.50.1` / Jul 7 (blurb editions list now names the command-line
  tool); Games → `0.19.3` / Jul 7; site meta → `0.15.3` / Jul 8. PSE2, Stories, predecessor
  left accurate.
- `_data/pulse.yml` — ticker re-aged (previous "1d" → "2d", etc.), re-led with the three
  2026-07-07 highlights (RAP CLI, RAP chaos/prompt-run, Games Symmetry cadences); dropped the
  three tail entries that would have aged to "4d"; `bars` advanced one day (dropped the oldest,
  appended a plausible count).
- Node pages (retired `0.13.0`), `downloads.yml` (removed `0.13.1`), the `_docs/` library,
  About, and the header/footer nav — all checked, none carry sibling version specifics, all
  already accurate. `hub/registry.yml` — no versions, accurate.

**Blogged:** `_posts/2026-07-07-a-command-line-prompt-tool-and-a-fifth-game-onto-varied-structure.md`
(tags `[random-ai-prompt, fairyfox-games, update]`). PSE2 + Stories had no 2026-07-07 work and no
`[site, update]` change shipped in this repo, so no other posts.

**Markers advanced** in `hub/.last-seen.yml`: RAP → `3147535`, Games → `2411178`; PSE2 + Stories
`seen` → 2026-07-08 (markers unchanged). Notes maintained (this log, changelog entry, VERSION
`0.15.2 → 0.15.3`, status version line). Build-checked with `jekyll build`; released `dev → main`.
