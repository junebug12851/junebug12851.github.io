# 2026-07-04 — Session log

_Newest on top._

## Maintenance round-up: reconcile to Games 0.12.0, blog the 3rd (0.12.3)

Routine scheduled "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean
on `dev` beforehand except the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings
(left untouched — inbound report-review's job). No unexpected staged files or commits — no live
session collision.

**References refreshed (git-ignored mirrors, on `dev`).**

- **pokered-save-editor-2** — `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change.
- **random-ai-prompt** — clean fast-forward `43f4c2b → cdfaf7c`, VERSION unchanged at `2.41.0`
  (all commits 2026-07-03). No force-push.
- **fairyfox-games** — clean fast-forward `3a44d61 → 1994e12`, VERSION `0.11.2 → 0.12.0` (commits
  2026-07-03). No force-push.

**Siblings' 2026-07-03 work.** Games: an eighth game **Poise** (a balance game — a ball on a
tilting beam, momentum through each catch, gravity ramping by stage; pure core + growth stack,
25 tests) plus a **near-miss line** grown onto Skyline; 244 tests. RAP: CI/tooling only — the
`2.41.0` release had failed at keyless signing and never published, fixed by pinning cosign 2.x
then modernizing onto **cosign 3.x Sigstore bundles** (`<asset>.sigstore.json`), plus an in-range
Vite bump. No product change, no version bump.

**Reconciled (changed):** `_data/projects.yml` (games `0.11.2 → 0.12.0` / Jul 3; site meta
`0.12.3` / Jul 4); `_data/pulse.yml` (ticker + `when` + bars rolled — Poise, Skyline near-miss,
and the RAP signing fix at the top); `_projects/fairyfox-games.md` (added Poise to the game list).

**Left untouched (verified accurate):** `_data/downloads.yml` (RAP source-only; the signing change
touches no download link; PSE2/PSE1 unchanged), `hub/registry.yml`, `_data/doc_categories.yml`, all
`_docs/*`, `about.md`, header/footer nav.

**Blogged.** `_posts/2026-07-03-an-eighth-game-and-a-modernized-release-signature.md`
(tags `fairyfox-games, random-ai-prompt, update`) — day-centric on the siblings' 2026-07-03 work.
Markers advanced in `hub/.last-seen.yml` (RAP → `cdfaf7c`, games → `1994e12`, PSE2 unchanged; all
`seen` → 07-04); `reports_through` left for the inbound report-review pass.

**Notes/version.** Changelog entry atop `notes/version/2026-07.md`; `VERSION` `0.12.2 → 0.12.3`;
`status.md` version line refreshed. Build-checked before shipping.
