# Session — 2026-07-10

_Newest on top._

## Tech icons grouped by kind (0.15.6)

Follow-up in the same live session. Grouped the card's tech icons into language /
framework / platform buckets with a faint hairline divider between groups (`.tech-group`
in the include + a `border-left` in CSS; empty groups dropped; group order stable
regardless of tag authoring order). Kept the per-icon hover label + accent. Rebuilt and
screenshotted (headless Chrome, 2×) — the three groups read clearly and the dividers are
suitably light. Committed on `dev` (no release), matching the prior finalize choice.

## Project cards: icon tech row + Updated to the bottom (0.15.5)

Live session (Fairy Fox directing), continuing the card work from 2026-07-09.
PowerShell + Read/Write/Edit only (no bash). Git clean on `dev` and in sync with
`origin/dev` beforehand (`3e046c1`); the pre-existing untracked
`notes/fairyfox-reports/inbound/INBOX-*.md` briefings left untouched.

Moderate UI pass on the standardized project card:

- **Tags → icons.** Replaced the text tag chips with a compact row of Font Awesome
  platform/language icons, mapped from each tag in `_includes/project-card.html`
  (brand marks for JS / Node / Angular / GitHub; solid stand-ins for the rest —
  `gem`=Ruby, `flask`=Jekyll, `atom`=Electron, `cubes`=Qt 6, `code`=C++/TypeScript,
  `paintbrush`=QML, `globe`=Web, `desktop`, `palette`=Canvas; `tag` fallback). Icons
  are muted, lift to the project accent on hover, and each reveals a small label chip
  so nothing is ambiguous. Kept per-tag `aria-label` for screen readers.
- **Updated moved to the bottom**, below the Docs/Repository links, behind a hairline
  divider (`.proj-bottom .updated` restyled).
- **Font Awesome 6** loaded from cdnjs in `_includes/head.html` (with a preconnect),
  alongside the existing Google Fonts.
- Styles added: `.tech` / `.tech-item` / `.tech-label` in `assets/css/main.css`
  (tech row sits above the card's stretch-link overlay so the hover labels work).

Shared include ⇒ home and `/projects/` both change. Site meta bumped to `0.15.5` /
Jul 10 in `_data/projects.yml`.

Verified locally: `bundle exec jekyll build` green; served `_site` and took headless
Chrome screenshots — a full-page default grid and a 2× forced-hover crop. Inspected both:
all icons render (atom, paintbrush, gem, flask, cubes, brand marks), hover labels and
accent colour work, and the Updated line sits cleanly at the card's base.
