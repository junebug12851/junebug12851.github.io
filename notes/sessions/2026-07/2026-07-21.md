# 2026-07-21

## Maintenance round-up — the 20th blogged; coins-finish + plugin-modernization reconciled (0.20.5)

A scheduled maintenance pass. Started clean on `dev` at 0.20.4, tree clean and up to date with
`origin/dev` (no concurrent live-session collision to work around).

**References.** All five sibling mirrors fast-forwarded cleanly (no force-pushes). **Random AI
Prompt** was unchanged (`35b1370`, still `2.60.1`, last commit 07-19). The other four moved, every
new commit dated **2026-07-20** — one complete day (today is 07-21) — so one day post:

- **DespawnedItems** `5?/1.0.2 → 1.3.4` (baseline established this pass — no prior marker). The Paper
  plugin, onboarded site-side the day before at v1.0.2, was rebuilt in a single day: retargeted
  Paper 26.1/Java 25 → **Paper 1.21.11 / Java 21** (zero API changes), indexed O(1) `LocationStore`,
  off-thread incremental persistence, a throttled `DespawnScheduler`, pluggable YAML/SQLite/MySQL
  storage, per-user `despi.limit.<n>` caps, correctness fixes (PDC recycle rewards, particle-data
  crash, reload re-reads config), Brigadier commands, a JUnit 5 + MockBukkit suite (~49 tests gating
  build) + Ktlint/Detekt/Kover, a package move to `io.fairyfox.papermc.despawneditems`, the shared
  fairyfox chrome on the Dokka docs, and a full hub-standards audit (branch protection, PR-based
  releases, build provenance). Marker `→ 2e24083` (last 07-20 commit, v1.3.4); 07-21's v1.3.5 (a
  docs-render fix) left for next pass.
- **Fairy Fox Games** `a690442 → 015a421`, `0.24.4 → 0.24.12`. Two 07-19 commits sat just past the
  old marker (coins phase 2 chrome + phase 3 shared module/Polarity), then 07-20 finished the in-game
  coin rollout across the back half of the collection — batches 1–5 + Reprise's visual **Light show**
  (15/15; the audio-Choir game got a visual mode so no game carries an audio dependency), initiative
  closed. Marker parked at 015a421 (last 07-20); 07-21's v0.24.13 (repo-hygiene scripts) left for next
  pass.
- **Fairy Fox Stories** `fb160a5 → 6e25309`, `0.5.5 → 0.6.0`. The visible half of the coins adoption
  deferred on 07-19 — coins.js into the reader chrome + coin CSS on themed tokens + legal disclosure,
  built in a connected-Chrome session — plus a daily GROW: *The Hundredth Wind* ch.2 "The Ear of the
  House".
- **Pokered Save Editor 2** `5ae9dad3 → b3128652`, unchanged at `0.43.6-alpha`. A de-personalization
  pass (copyright/credits/first-person notes → Fairy Fox attributions) + a docs-domain TLD typo fix.
  Docs/identity only, no engine change.

**Blogged.** New day post `_posts/2026-07-20-the-coins-finish-their-rounds-and-a-plugin-grows-up.md`
— the reading coin reaches the last game and the story reader; DespawnedItems grows from a two-file
port into a tested, pluggable, mesh-standard project; PSE2 takes its name off its credits; and the
site's own 0.20.4 work (onboarding + the first project category) is the [site] strand. Tags
`[papermc-despawned-items, fairyfox-games, fairyfox-stories, pokered-save-editor-2, site, update]`.
RAP not tagged (no 07-20 commits).

**Reconciled.** `_data/projects.yml` — DespawnedItems `→ 1.3.4 / Jul 20` (tags + blurb retargeted to
Paper 1.21.11 / Java 21), Games `→ 0.24.12 / Jul 20`, Stories `→ 0.6.0 / Jul 20`, PSE2 date `→ Jul 20`
(version held at 0.43.6), meta card `→ 0.20.5 / Jul 21`. `_includes/project-card.html` — retargeted
the `Java 25` / `Paper 26.1` tag-glyph cases to `Java 21` / `Paper 1.21.11`. `hub/registry.yml` —
corrected the DespawnedItems comment. `_data/pulse.yml` — ticker advanced a day (07-20 as "today"),
bars advanced; added a `--sky` accent token (`assets/css/main.css` root + sepia) for the plugin's
ticker colour + legend. Docs library / About / header/footer — checked, accurate, no change (per-project
docs pages retired; all standards pages present). `hub/.last-seen.yml` — four markers advanced, a **new
baseline marker created for papermc-despawned-items**, five round-up notes added; new sibling report
files left for the inbound report-review pass.

**Ship.** Build-checked, committed on `dev`, released to `main`. VERSION `0.20.4 → 0.20.5`.
