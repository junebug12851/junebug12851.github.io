# 2026-07-19

## Home/projects listing reshape + farm icons (0.16.2)

Fairy Fox asked (in one live session, across several follow-ups) to take the **farms off the
home page** and give them their own section near the bottom of `/projects/`, drop the **(meta)
home card** from the home grid, wire in the farms' **own icons** (which now exist in the sibling
repos), and swap the hero's **Documentation** button for a **GitHub-profile** link.

- **`index.html`** — projects grid now loops only the standalone tier (`meta != true and
  integrated != true`); the `integrated` and `meta` loops are gone. Hero secondary button →
  `https://github.com/1fairyfox` ("GitHub profile ↗").
- **`projects.md`** — top grid drops the `integrated` loop (keeps standalone + meta); a new
  **Farms** `.prose` heading + a 3-col grid of the two farm cards sits below the "How the
  projects connect" prose.
- **Icons** — `assets/references/fairyfox-stories/assets/icon.png` and
  `.../fairyfox-games/assets/icon.png` copied to `assets/icons/{stories,games}.png`; `icon:`
  set on both farm entries in `_data/projects.yml`.
- Build-checked green; visually verified home + `/projects/` locally in Chrome (farm cards
  carry the new glyphs). PATCH release `0.16.1 → 0.16.2`.

## Maintenance round-up — the 18th blogged; the hub-repo rename reconciled (0.16.1)

A full maintenance pass. All four sibling mirrors fast-forwarded cleanly (no force-pushes);
three moved, Random AI Prompt was unchanged for a seventh quiet day. Every new commit on all
three moving siblings sat on **2026-07-18** — one complete day (today is 07-19), so one day
post: [`2026-07-18-one-name-one-header-one-colour-table.md`](../../../_posts/2026-07-18-one-name-one-header-one-colour-table.md),
tags `[pokered-save-editor-2, fairyfox-games, fairyfox-stories, site, update]`.

### What moved

- **Pokered Save Editor 2** `0.42.13-alpha → 0.43.4-alpha` (5 commits): the map states go
  live (a progression picker that rolls a map forward/back a stage; "map script" → "map
  state"), then a consistency overhaul of the map screen — one canonical ink table, hidden
  sprites always shown as recognisable ghosts, one "useless edits" toolbar gate, event flags
  grouped by stage, the "Map Storage" panel renamed **World**, outline-only door/sign boxes.
  Marker `1679d2f0 → 4d2ffc8c`.
- **Fairy Fox Games** `0.23.2 → 0.24.2` (Ward, the 15th game and the collection's first
  defence verb; Ricochet's depth layer → 6/13; owner rename + 3 code-rendered game icons +
  a fallback; adopted the hub shared-chrome Farms header). Marker `728a76f → 5d89b0c`.
- **Fairy Fox Stories** `0.4.2 → 0.5.3` (grow ch.5/7/6; planted **The Hundredth Wind**, the
  8th book — the farm's first universe-join and first author-reuse, Amara Okri back on the
  Salt Road; adopted hub 0.16.0; grow cadence settled to 1 book/day; auto-art on plant;
  reader controls on the legal pages). Marker `c677b3f → 82b5a5b`.
- **Random AI Prompt** — "Already up to date" at `2f1459f` (still `2.60.1`); nothing to
  reconcile or blog. `seen → 2026-07-19`.

### The site's own notable change — the hub repo is renamed

Surfaced through the Stories run and confirmed against `gh`: Fairy Fox renamed the user-site
repo **`junebug12851.github.io` → `1fairyfox.github.io`** on 2026-07-18, closing a site-wide
404 (`fairyfox.io/fairyfox-stories/` had gone dark after the account rename). This closes the
open item that `status.md` and `decisions/architecture.md` had carried as "Fairy Fox's call".
Reconciled the hub's own **current-state** references to the new name (dated history left
intact): `_data/projects.yml` (meta card repo/notes) + `hub/registry.yml` (hub repo);
`_docs/overview.md`, `_docs/site-deployment.md`, and the docs-site standard
`10-domain-and-publishing.md`; the clone URLs in `hub/README.md`, `adopting-updates.md`,
`docs-site/chrome/README.md`, `new-project-setup.md`, `onboarding-existing-project.md`,
`templates/README.md`; `CLAUDE.md`; `status.md`; and a new `decisions/architecture.md` entry.
The site's own 0.16.0 (Farms nav + story-only reader, shipped 07-18) is written up in the day
post's site section alongside the rename. The local git remote was left pointing at the
redirecting old URL (harmless); each sibling repoints its vendored chrome-pull URL as it
re-adopts.

### Components reconciled

- `_data/projects.yml` — PSE2 `→ 0.43.4 / Jul 18`, Games `→ 0.24.2 / Jul 18`, Stories
  `→ 0.5.3 / Jul 18`, meta card `→ 0.16.1 / Jul 19` + renamed repo/notes URLs; RAP unchanged.
- `_data/pulse.yml` — ticker refreshed (the 18th's work as "today", the site's Farms-nav +
  rename added as coral entries, prior days shifted down); bars advanced one day.
- `hub/registry.yml` — hub repo URL renamed; branches/flags already accurate.
- `_docs/*` + `hub/standards/docs-site/*` + About/header/footer — checked; only the repo-name
  references needed changing (no hub standard moved this pass — RAP, the standards-bearer, was
  unchanged; the 0.16.0 chrome/docs-site update was already mirrored in the human 07-18 session).
- `_projects/` remains retired (empty) as designed; no `downloads.yml` (removed 0.13.1).
- `hub/.last-seen.yml` — three markers advanced, RAP `seen` bumped, four round-up notes added.

### Verify / ship

Production `jekyll build`; committed on `dev`, released to `main`. VERSION `0.16.0 → 0.16.1`.
