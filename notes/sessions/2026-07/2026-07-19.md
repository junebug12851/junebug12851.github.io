# 2026-07-19

## Coins/reader polish on owner review (0.18.1)

After shipping 0.18.0, Fairy Fox reviewed the coins UI/copy. Fixes: monochrome **vector** coin icon
(concentric-circle token, `currentColor`) replacing the gold filled coin; coin-pop on the accent
palette not gold; simpler panel copy ("Navigating around and using fairyfox.io lets you earn and use
coins"); footer hint → "Saved & shared across Fairy Fox"; a **"Clear my data"** link on both the coins
and reader panels (each clears its own store, resets live). Wording softened per a firm owner rule —
**never promise no data loss**: "strive to keep across site updates", "only saved in your browser",
coins "mainly a fairyfox.io feature", "Fine print" → "Details"; durability standard reframed as a
strive-to-persist engineering discipline (not a guarantee). Chrome `2.1.0 → 2.1.1`, VERSION `→ 0.18.1`.
Released `dev → main` (PATCH, direct).

## Deep-read the siblings → promoted nine cross-brand standards (0.18.0)

Owner asked me to use PowerShell, then to **manually read every sibling `CLAUDE.md` + non-log notes
file** for anything standardizable across the brand (session logs excluded; reports become the
ongoing channel after).

- **PowerShell now in use.** Build-checked 0.17.0 green (`jekyll build`, 5.1s); verified the built
  output (legal pages render, `data-read` on legal + absent on index/docs, coins.js loaded). Refreshed
  the four dev mirrors (games/stories/pse2 advanced, RAP unchanged).
- **Deep-read.** Read all four `CLAUDE.md` in full + the convention-dense notes (context/principles,
  reference conventions, RAP's `shareable-systems-roundup`). Categorized out domain knowledge and
  already-adopted hub copies. Findings recorded in
  [`notes/fairyfox-reports/2026-07-19-standards-deep-read.md`](../../fairyfox-reports/2026-07-19-standards-deep-read.md).
- **Promoted (owner: all nine, phased).** Plan:
  [`notes/plans/2026-07-19-standards-promotion.md`](../../plans/2026-07-19-standards-promotion.md).
  Eight new `hub/standards/` (testing, engineering-quality, repo-hygiene [+ two template scripts],
  docs-lifecycle, research-capture, working-rhythm, self-hosted-assets, farm-operating-model), each
  with a `## Verify` + a `compliance.md` row + a `hub/README.md` entry; **release posture** set to
  RAP's CI-gated PR gate in `git-workflow.md`. VERSION `0.17.0 → 0.18.0`.
- **Held:** authoring is hub-side only; nodes adopt on-request. `main` release still held for a
  go-ahead (0.17.0 ships first, then 0.18.0). One inconsistency surfaced beyond the nine — the hub's
  own Google-Fonts/cdnjs hot-link — recorded as a self-hosted-assets exception to remediate.

## Fairy Fox coins + a brand/chrome/quality standards pass (0.17.0)

Fairy Fox asked (one live session, many follow-ups) for a subtle **coins** feature — a
reading-engagement counter beside the reader button — and, as it grew, a broad set of
brand/chrome/quality standards around it. MINOR milestone.

**Decisions (confirmed via AskUserQuestion + follow-ups):** wallet is **per-origin native**
(one wallet across everything under the fairyfox.io origin; Netlify apps + sub-domains keep
their own — "I know it doesn't work that way"); earn feedback is **playful** (coin-pop + glow);
click opens a **mini panel**. Coins must stay **subtle, never overused, never a gate**; must
**never lose data**; and must **never detract from the brand**.

**Built (site).**
- `assets/js/coins.js` — origin-wide `fairyfox:coins:a` wallet; first-view +1 (10% → +2);
  repeat-view 1% +1 capped 10/day; day-keyed seen-set rolls at midnight, balance carries.
  Injects the coin button just left of the "Aa" button; mini panel (balance · today · lifetime
  · how-it-works + "How coins work →"); playful pop; `window.FairyFoxCoins`
  (`get`/`earnedTotal`/`earnedToday`/`onChange`/`spend`/`reward`) + `fairyfox:coins` event.
  Load added after `reader.js` in `_layouts/default.html`.
- `assets/css/main.css` — `.ff-coin-*` button/panel/pop (reduced-motion guard); `.footer-legal`;
  `.subnav-home`/`.subnav-repo` zones; `.ff-coin-more`.
- **Hub legal pages (new)** `legal/{privacy,terms,cookies,coins}.md` at `/legal/…`, accurate to
  the code (static Jekyll, no accounts/server data, localStorage for reader prefs + coins,
  Google Fonts + cdnjs IP flagged, Pages processor, `fairy@fairyfox.io`). Closes a standing gap
  (the hub served none). `/legal/coins/` is the plain-language transparency page.
- **Footers** — hub bar gains Privacy · Terms · Cookies · Coins; shared chrome "This project"
  column gains the project's Privacy/Terms/Cookies.
- `projects.md` — "How the projects connect" moved **below** Farms.
- `index.html` — Documentation section curated (top few per category + "+N more →"); hero button
  → **GitHub**.

**Standards (flow outward to siblings on re-adopt).** New `hub/standards/coins.md` (earning model
+ API + subtle/never-a-gate philosophy + **durability mandate**) + `compliance.md` row; reader
key guidance in `02-design-tokens.md` fixed to "migrate, never orphan". Docs-site: **canonical
project subnav** three-zone shape (`05` + `chrome/subnav.html` + `reference/chrome.html`);
**sub-projects mark only `Projects`** active; **Downloads** page + perma-latest requirement (`06`);
**page quality / no-orphan / keep-current** (`06`); **API-docs-only-in-API** + **generated-docs
hygiene** (sidebar only API — audit; kill the generated footer, use chrome footer; **Notes** one
item, fully navigable) (`06` + doxygen adapter); all folded into the `08` checklist. `legal-docs.md`
gains the **brand minimum**. Chrome bundle `VERSION 2.0.0 → 2.1.0` (coins.js added to the pulled
set + adapters + `04`/`08`/`09`/`12`).

**Verify / ship.** Static self-review of `coins.js` + edited Liquid/HTML done. **Local `jekyll
build` + visual check still to run, and the `main` release held** — Fairy Fox was iterating live,
so the work sits on the working tree pending a go-ahead (and a concurrency check for any live
commits). VERSION `0.16.2 → 0.17.0` staged.

## Home/projects listing reshape + farm icons (0.16.2)

Fairy Fox asked (in one live session, across several follow-ups) to take the **farms off the
home page** and give them their own section near the bottom of `/projects/`, drop the **(meta)
home card** from the home grid, wire in the farms' **own icons** (which now exist in the sibling
repos), and swap the hero's **Documentation** button for a **GitHub-profile** link.

- **`index.html`** — projects grid now loops only the standalone tier (`meta != true and
  integrated != true`); the `integrated` and `meta` loops are gone. Hero secondary button →
  `https://github.com/1fairyfox` ("GitHub profile ↗").
- **`projects.md`** — top grid drops the `integrated` loop (keeps standalone + meta); a new
  **Farms** `.prose` heading + a 3-col grid of the two farm cards sits below the "How the
  projects connect" prose.
- **Icons** — `assets/references/fairyfox-stories/assets/icon.png` and
  `.../fairyfox-games/assets/icon.png` copied to `assets/icons/{stories,games}.png`; `icon:`
  set on both farm entries in `_data/projects.yml`.
- Build-checked green; visually verified home + `/projects/` locally in Chrome (farm cards
  carry the new glyphs). PATCH release `0.16.1 → 0.16.2`.

## Maintenance round-up — the 18th blogged; the hub-repo rename reconciled (0.16.1)

A full maintenance pass. All four sibling mirrors fast-forwarded cleanly (no force-pushes);
three moved, Random AI Prompt was unchanged for a seventh quiet day. Every new commit on all
three moving siblings sat on **2026-07-18** — one complete day (today is 07-19), so one day
post: [`2026-07-18-one-name-one-header-one-colour-table.md`](../../../_posts/2026-07-18-one-name-one-header-one-colour-table.md),
tags `[pokered-save-editor-2, fairyfox-games, fairyfox-stories, site, update]`.

### What moved

- **Pokered Save Editor 2** `0.42.13-alpha → 0.43.4-alpha` (5 commits): the map states go
  live (a progression picker that rolls a map forward/back a stage; "map script" → "map
  state"), then a consistency overhaul of the map screen — one canonical ink table, hidden
  sprites always shown as recognisable ghosts, one "useless edits" toolbar gate, event flags
  grouped by stage, the "Map Storage" panel renamed **World**, outline-only door/sign boxes.
  Marker `1679d2f0 → 4d2ffc8c`.
- **Fairy Fox Games** `0.23.2 → 0.24.2` (Ward, the 15th game and the collection's first
  defence verb; Ricochet's depth layer → 6/13; owner rename + 3 code-rendered game icons +
  a fallback; adopted the hub shared-chrome Farms header). Marker `728a76f → 5d89b0c`.
- **Fairy Fox Stories** `0.4.2 → 0.5.3` (grow ch.5/7/6; planted **The Hundredth Wind**, the
  8th book — the farm's first universe-join and first author-reuse, Amara Okri back on the
  Salt Road; adopted hub 0.16.0; grow cadence settled to 1 book/day; auto-art on plant;
  reader controls on the legal pages). Marker `c677b3f → 82b5a5b`.
- **Random AI Prompt** — "Already up to date" at `2f1459f` (still `2.60.1`); nothing to
  reconcile or blog. `seen → 2026-07-19`.

### The site's own notable change — the hub repo is renamed

Surfaced through the Stories run and confirmed against `gh`: Fairy Fox renamed the user-site
repo **`junebug12851.github.io` → `1fairyfox.github.io`** on 2026-07-18, closing a site-wide
404 (`fairyfox.io/fairyfox-stories/` had gone dark after the account rename). This closes the
open item that `status.md` and `decisions/architecture.md` had carried as "Fairy Fox's call".
Reconciled the hub's own **current-state** references to the new name (dated history left
intact): `_data/projects.yml` (meta card repo/notes) + `hub/registry.yml` (hub repo);
`_docs/overview.md`, `_docs/site-deployment.md`, and the docs-site standard
`10-domain-and-publishing.md`; the clone URLs in `hub/README.md`, `adopting-updates.md`,
`docs-site/chrome/README.md`, `new-project-setup.md`, `onboarding-existing-project.md`,
`templates/README.md`; `CLAUDE.md`; `status.md`; and a new `decisions/architecture.md` entry.
The site's own 0.16.0 (Farms nav + story-only reader, shipped 07-18) is written up in the day
post's site section alongside the rename. The local git remote was left pointing at the
redirecting old URL (harmless); each sibling repoints its vendored chrome-pull URL as it
re-adopts.

### Components reconciled

- `_data/projects.yml` — PSE2 `→ 0.43.4 / Jul 18`, Games `→ 0.24.2 / Jul 18`, Stories
  `→ 0.5.3 / Jul 18`, meta card `→ 0.16.1 / Jul 19` + renamed repo/notes URLs; RAP unchanged.
- `_data/pulse.yml` — ticker refreshed (the 18th's work as "today", the site's Farms-nav +
  rename added as coral entries, prior days shifted down); bars advanced one day.
- `hub/registry.yml` — hub repo URL renamed; branches/flags already accurate.
- `_docs/*` + `hub/standards/docs-site/*` + About/header/footer — checked; only the repo-name
  references needed changing (no hub standard moved this pass — RAP, the standards-bearer, was
  unchanged; the 0.16.0 chrome/docs-site update was already mirrored in the human 07-18 session).
- `_projects/` remains retired (empty) as designed; no `downloads.yml` (removed 0.13.1).
- `hub/.last-seen.yml` — three markers advanced, RAP `seen` bumped, four round-up notes added.

### Verify / ship

Production `jekyll build`; committed on `dev`, released to `main`. VERSION `0.16.0 → 0.16.1`.
