# 2026-07-18

## Maintenance round-up pass — reconcile + blog the 17th (0.15.15)

A scheduled maintenance pass. Refreshed all four sibling references, reconciled the data-driven
components against reality, blogged the one complete day found (2026-07-17), and shipped a PATCH.

**References refreshed** (all clean fast-forwards, no force-pushes, no re-clones):

- **pokered-save-editor-2** `cb8b1d79 → 1679d2f0`, VERSION `0.41.8 → 0.42.13-alpha` — 37 commits, all
  dated **2026-07-17**, a complete day (today is the 18th). The project's single biggest day yet, and a
  day about naming/measuring the right thing. **Map states shipped as data** — 98 per-map progression
  blueprints computed off pret's own scripts (a stage = script byte + absolute event set + missable
  visibility + badges, *not* the byte alone; the disassembly has no format for this, so it's a genuinely
  new artefact); the map screen's "map script" renamed to "map state". The **16f interaction model**
  briefed and settled by reusing the **layers panel's own visible order** for click priority, with a
  click-and-drag colour tab per layer as the escape hatch — the answer that invents nothing and retires
  the buried-object risk rather than mitigating it. **The units renamed**: there are three (tile 8×8,
  half-block 16×16, block 32×32), and the walk grid the editor boxes live on is the half-block — the
  boxes were the right size, the word was wrong. **One Qt everywhere** (6.11, kit == container == CI):
  the Windows install failure was an aqtinstall repo-layout reorg already fixed in a merged-but-unreleased
  PR (pinned via `aqtsource`) — "the latest release can't" ≠ "can't"; and the container's three emu-test
  "failures" were a path-exists availability gate (checks a *file*, not whether the interpreter *runs*).
  With both nets restored, **CI went green for the first time since `0.29.0`** and the unblocked gates
  found a **heap-use-after-free in the music parser** (a by-ref label into a growing list) that twelve
  releases had carried, plus a null deref, a dead check hiding a missing bounds test, and a needless copy.
- **random-ai-prompt** — unchanged at `2f1459f` (`2.60.1`); no commits dated 2026-07-17. Six quiet days.
  As the mesh's only standards-bearing sibling, an unchanged RAP means no hub standard could have moved.
- **fairyfox-games** `450db5d → 728a76f`, VERSION `0.23.1 → 0.23.2` — a pure grow run: **Orbit Slingshot**
  gets the depth layer (5th of 13, the oldest without it, carrying the same radius plateau the sweep
  flags). A no-plateau pickup radius; a hidden **Kiss** window inside the close pass; an **Aurora**
  doubling wave chained from three kisses; a secret **Interstellar** stage past the visible end. The
  layer is the reference build's *shape* poured into Orbit Slingshot's own physics, not a copy.
- **fairyfox-stories** `6e78be5 → c677b3f`, VERSION `0.4.1 → 0.4.2` — three chapters grown, no plant, and
  **two books finished on one run** (shelf: one complete → three). The blend **repaid 07-16's debt**: no
  override this run, so *The Blindfold Act* — robbed of its slot last run — came back at a record 0.917
  and led; the two near-complete books were picked purely on staleness, which is exactly why that half of
  the formula exists. A sixth integrity check added (book state must match chapters on disk + sequel-lock
  honoured), turning a standing promise into something shipping-the-wrong-state can't get past.

**Components reconciled:**

- `_data/projects.yml` — PSE2 → `0.42.13` / Jul 17 (blurb gains in-game trades + completion milestones);
  games → `0.23.2` / Jul 17; stories → `0.4.2` / Jul 17; RAP left at `2.60.1` / Jul 12; the meta
  fairyfox.io row → `0.15.15` / Jul 18.
- `_data/pulse.yml` — bars shifted one day (appended the **real** 2026-07-17 commit count, 44, counted
  across the four references + this repo; dropped the oldest); ticker rebuilt so the 2026-07-17 work is
  "today" and older entries aged out (the RAP command-injection entry to `5d`).
- `_data/downloads.yml`, `_projects/` node pages — still retired (do not exist); N/A.
- `hub/registry.yml` (no version fields), the `_docs/` library, `_data/doc_categories.yml`, and
  `about.md` — checked, accurate, no change. Verified directly: no sibling's diff since its marker
  touches `hub/`, `standards`, or `fairyfox-reports/`.
- `hub/.last-seen.yml` — markers advanced to `1679d2f0` (PSE2) / `2f1459f` (unchanged) / `728a76f`
  (games) / `c677b3f` (stories); all `seen` → 2026-07-18, each with a round-up note.

**Blogged:** `_posts/2026-07-17-the-word-was-wrong-not-the-size.md` — one day-centric post (tags
`pokered-save-editor-2`, `fairyfox-games`, `fairyfox-stories`, `update`). RAP absent (no work that
day). The site itself shipped only this maintenance PATCH, which the round-up post + changelog already
cover, so no separate `[site, update]` post was warranted.

**Version:** `0.15.14 → 0.15.15` (PATCH). Build-checked; shipped dev → main.

_Note: untracked `notes/fairyfox-reports/inbound/INBOX-*.md` files remain present in the working tree
and were left untouched again — not this pass's concern, and staging was file-specific. Still worth a
deliberate decision from Fairy Fox about whether they should be tracked, ignored, or removed._
