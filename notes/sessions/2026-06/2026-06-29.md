# 2026-06-29 — Session log

_Newest on top._

## Fairy Fox Games adopts_hub -> true (0.9.11)

fairyfox-games landed its themed docs site (and a Netlify second home at
games.fairyfox.io), so flipped its adopts_hub flag to true in hub/registry.yml
and bumped its version to 0.1.2 in _data/projects.yml. PATCH release.
## Scheduled maintenance pass — reconcile to RAP 2.14.0, blog the 28th (0.9.9)

Routine "usual stuff" round-up. PowerShell + Read/Write/Edit only (no bash). Tree was clean on `dev`
beforehand (only the three untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings, which belong
to a different procedure — left untouched).

**References.**

- **pokered-save-editor-2** — `git fetch` + `merge --ff-only origin/dev` → "Already up to date" at
  `1bae326` (still `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `e491232 → ba5b65a`, 33 commits all dated 2026-06-28,
  VERSION `2.10.4 → 2.14.0`. No force-push (a plain ff, as the 0.9.6 model expects).

**What RAP shipped (2.10.5–2.14.0).** Online build went fully static with browser-direct generation for
CORS-friendly providers (2.11.0); the **Manage tab** — an in-app, disk-backed catalog editor
(blocks/lists/folders) with live hot-apply, virtualized list editing, restore-default, ghost pills, and an
external-edit watch (2.12.0; probe hardened in 2.12.1 so it never mounts on a static host); the **focus
dial** as a sibling of the intensity dial (`{#name i25% f80%}`, mandatory `i`/`f` prefixes, the `$` keyword
sigil) plus global layer auto-merge (2.13.0); and list Dedupe/Sort, a Manage Insert bar, AI Expand, and a
live DPL-validity lint (2.14.0).

**Reconciled (changed):**

- `_data/projects.yml` — RAP `2.10.4 → 2.14.0` (updated Jun 28); fairyfox meta `0.9.6 → 0.9.9` (the meta
  row had lagged two patch releases behind — 0.9.7/0.9.8 never updated it).
- `_data/pulse.yml` — ticker re-led with the five 2026-06-28 highlights (Manage tab, focus dial, list
  quick-wins, restore/ghost, the hub's 0.9.8 fold), prior batch re-aged 1d→2d / 2d→3d / 3d→4d, bars rolled
  forward one day (dropped oldest, appended a busy day).
- `_projects/random-ai-prompt.md` — now describes twin intensity/focus dials and the in-app Manage tab.
- `_docs/random-ai-prompt.md` — DPL bullet → twin dials.

**Verified accurate, left untouched:** `_data/downloads.yml` (RAP source/run-only + hosted-demo note;
PSE2 still `v0.14.2-alpha`), `hub/registry.yml` (branches `dev`, flags correct), `about.md` (prose-level),
`_data/doc_categories.yml`, header/footer nav.

**Blogged.** `_posts/2026-06-28-an-editor-for-the-catalog-and-a-second-dial.md` (tags
`random-ai-prompt, fairyfox-io, site, update`) — RAP's 2.10.5–2.14.0 + the hub's 0.9.8 standards fold,
day-centric. Markers advanced in `hub/.last-seen.yml` (RAP `e491232 → ba5b65a`, VERSION → 2.14.0; PSE2
unchanged, `seen` → 2026-06-29).

**Notes/version.** Changelog entry atop `notes/version/2026-06.md`; `VERSION` `0.9.8 → 0.9.9`;
`notes/status.md` version line + the RAP onboarded-summary refreshed. Build-checked before shipping.
