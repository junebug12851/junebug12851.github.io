# 2026-06-30 — Session log

_Newest on top._

## Submenu nav + advance digested report markers (0.11.0)

Site-code follow-up to the 0.10.0 standards (owner: "go ahead"). On
`feature/one-site-frontend` off `dev`.

- **Submenu (`.subnav`)** — new `_includes/subnav.html` + `.subnav` CSS, rendered below
  the primary nav by the base layout. Auto-enables for **Projects** (All Projects + each
  project from `_data/projects.yml`) and **Docs** (All Docs + doc categories → `/docs/#slug`;
  added section `id`s). Lighter pills than the primary nav; wraps/scrolls on mobile.
- **docs-site master copy kept in step** — `.subnav` mirrored into `reference/main.css`,
  `11-measurements`, `reference/README.md`, `05`.
- **Markers advanced** — `hub/.last-seen.yml` `reports_through`: fairyfox-games ×3 + RAP
  `2026-06-28` (feedback folded in 0.10.0).
- **Verified** — local `jekyll build` green; served + screenshotted `/projects/` and
  `/docs/` (active states right, primary nav unchanged, theme consistent). Server cleaned
  up after.
- **Pending (node-side):** games at `fairyfox.io/games/` needs the games repo renamed
  `fairyfox-games → games` + its base path updated, then retire the hub `/games/`
  redirect — not done from the hub session (hub doesn't edit a node). See
  `plans/one-site-and-deployment.md`.

## Standards milestone — one seamless site, deployment + planning standards, shallow-clone cleanup (0.10.0)

Live session off the back of the automated 2026-06-30 inbound report-review
(`notes/fairyfox-reports/inbound/INBOX-2026-06-30-0208.md`). Planned first
(`notes/plans/one-site-and-deployment.md`), then executed on
`feature/one-site-standards` off `dev`. Owner decisions, several overriding the
briefing:

- **One seamless site.** Nodes appear as a **page of fairyfox.io** (shared chrome +
  the fixed global nav incl. Games), the **back-button is retired** (brand/Home is the
  way home), and a recommended **`.subnav`** submenu pattern + a copy-paste
  `reference/chrome.html` snapshot were added. Rewrote docs-site `01/04/05/06/08/11`,
  `reference/README.md`, and `_docs/docs-site.md`.
- **Deployment standard (new).** static→Pages on the shared domain, built apps→Netlify;
  **games is the recorded exception** (Pages `/games/`, no Netlify/subdomain).
- **Collection/monorepo** = deliberate, recorded exception (not default); onboarding
  reads a same-day setup report first.
- **Planning standard (new).** plan-before-execute by default for non-trivial work.
- **Tags** stay games-local (no mesh-wide tag model).
- **Shallow-clone purge** across standards/runbooks/refs/_docs/templates/README — just
  ordinary clones now; history files left as record.
- Compliance matrix gains `deployment` + `planning` rows; `ai-context`, the CLAUDE
  template, and root `CLAUDE.md` Default Workflow updated.

No site code yet — serving games at `/games/` on Pages and building the submenu nav is
the planned follow-up. PowerShell + editor tools only; build-checked before release.

## Add a Games link to the nav (0.9.14)

Added "Games" to the header nav (after Projects), linking `/games/`. The games already
have a live, self-maintaining landing at `/fairyfox-games/` (four games — Ink Bloom, Echo
Chamber, Orbit Slingshot, Polarity, confirmed live via fetch), so `/games/` is a
lightweight redirect (`games.html`, `layout: none`, meta-refresh + canonical) to it rather
than a duplicate page — keeps one source of truth and dodges a daily-sync burden.

**Concurrency note.** A scheduled round-up landed commit `e09684d` (0.9.13, reconcile RAP
2.28.18 + Games 0.4.0) and released it to `main` mid-session. Detected the collision (my
in-progress notes had also grabbed 0.9.13), reverted my two stray note edits with
`git checkout -- <file>`, re-versioned this work to **0.9.14**, and moved the session entry
into today's file (`2026-06-30.md`) where it belongs. PowerShell + editor tools; build green.

## Scheduled maintenance round-up — reconcile to RAP 2.28.18 + Games 0.4.0, blog the 29th (0.9.13)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree was clean on `dev`
beforehand — the only untracked files were the four `notes/fairyfox-reports/inbound/INBOX-*.md`
briefings from the automated inbound report-review procedure, left untouched (not this pass's job,
and not a live-session collision).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326`
  (still `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `ba5b65a → 2b6b8e6`, VERSION `2.14.0 → 2.28.18`. RAP's
  biggest single day — all commits dated 2026-06-29. No force-push (plain ff under the 0.9.6 model).
- **fairyfox-games** — clean fast-forward `8947a48 → f31f45b`, VERSION `0.2.0 → 0.4.0`. Two new
  games dated 2026-06-29. This is the node's first *blogged* round-up (it was brought in 06-29).

**What the siblings shipped (2026-06-29).**

- **RAP 2.15.0–2.28.18.** SPA internationalization (react-intl + FormatJS, 2.15.0); the single-image
  view's actions — re-roll/variation + tracked ancestry + Markdown export (2.16.0), then inline
  actions/live strips/resize/View Raw (2.17.0); a large provider expansion (2.18–2.28.0) — an AI
  upscale/enhance framework + NSFW soft-lock, a dedicated Upscaler/Enhancer row, a run of
  upscale/enhancer providers (DeepAI, Picsart, Segmind, local SD A1111/Forge/SD.Next/ComfyUI,
  Clipdrop, Venice, WaveSpeed, Claid, Deep-Image, neural.love, VanceAI, Leonardo, Replicate) plus
  text providers to 18 total; then the Phase 4–6 **refactor** (floor-up modular decomposition with a
  test net landed alongside each extraction), a Lighthouse polish (SEO+A11y 100, Perf 97) + SEO meta
  + CI gate, and a loader **memoization** (~280× faster `generate()`, no behavior change).
- **Fairy Fox Games 0.3.0 / 0.4.0.** Orbit Slingshot (a gravity game, symplectic-Euler core + 16
  tests) and Polarity (a charge-match runner, core + 15 tests); four games total now.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.28.18 / Jun 29; games → 0.4.0),
`_data/pulse.yml` (ticker re-led + bars rolled), `_projects/random-ai-prompt.md` (upscale/enhance +
single-view ancestry + i18n), `_projects/fairyfox-games.md` (names Orbit Slingshot + Polarity),
`about.md` (added the games entry), `_docs/fairyfox-games.md` (**new** per-project doc entry point —
the library was missing one), `_docs/overview.md` (fixed stale "shallow" clone wording — full-history
since 0.9.6).

**Verified accurate, left untouched:** `_data/downloads.yml` (RAP source/run-only; PSE2 still
`v0.14.2-alpha`; games play-in-browser), `hub/registry.yml`, `_data/doc_categories.yml`,
`_docs/random-ai-prompt.md`, header/footer nav (registry-driven).

**Blogged.** `_posts/2026-06-29-more-providers-a-deep-refactor-and-two-new-games.md` (tags
`random-ai-prompt, fairyfox-games, fairyfox-io, site, update`), day-centric. Markers advanced in
`hub/.last-seen.yml` (RAP → `2b6b8e6`; games null → `f31f45b`; PSE2 unchanged; all `seen` → 2026-06-30).

**Notes/version.** Changelog entry atop `notes/version/2026-06.md`; `VERSION` `0.9.12 → 0.9.13`;
status.md version line + the RAP/games summaries refreshed. Build-checked before shipping.
