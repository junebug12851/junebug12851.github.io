# 2026-06-28 — Session Log

## Scheduled maintenance round-up — reconcile to Random AI Prompt 2.10.4 (0.9.7)

Automated "usual stuff" pass (owner not present). `main` at `v0.9.6`, tree clean on `dev`
apart from two untracked inbound report-review briefings
(`notes/fairyfox-reports/inbound/INBOX-2026-06-26-1803.md`, `…-2026-06-27-0202.md`) — those are
from the **inbound report-review** procedure, not this round-up; left untouched, not a live-session
collision. PowerShell + the editor tools throughout (no bash).

**References.** Both git-ignored mirrors refreshed on `dev`.

- **random-ai-prompt** — `1868b58 → e491232`. The `merge --ff-only` refresh hit an `incorrect old
  value provided` fetch anomaly mid-operation; per the 0.9.6 full-history-mirror standard the mirror
  was **re-cloned fresh** (`clone --branch dev --single-branch`, not shallow), giving a clean
  full-history tree at `e491232`. **32 commits, all dated 2026-06-27**, VERSION `2.7.25 → 2.10.4`.
  The 2.7.26–2.10.4 line built out the engine-v3 web app: CodeMirror DPL editors (2.7.26), sidecar
  `priority` + `nsfw` hard-hide (2.7.27), result/gallery polish (2.7.28), the **stripped generate-only
  online build** (2.7.29; 2.10.4 shows local-only features disabled with a full-version link, and the
  Netlify build was pointed at `engine-v3/gui` and deployed to **prompt.fairyfox.io**), keyword tooling
  + a DPL insert toolbar (2.8.0), the **provider-header redesign** (2.9.0 — Providers dropdown +
  per-provider BYOK keys + Prompt/Negative switch), and the headline **DPL intensity dial** (2.10.0 —
  `{#name NN%}`, intensity conditions in the weight slot, auto-scaling counts/gates, `{intensity}`
  keyword) with a **content refactor across all five categories** (de-scatter, typo fixes, render-farm
  filler stripped, `anime-irl` → `anime-realism`, default wrapper cleaned of `masterpiece/best-quality`).
- **pokered-save-editor-2** — no new commits past `1bae326` (still `0.14.2-alpha`). Nothing to
  reconcile or blog. `seen` advanced to 2026-06-28; commit marker unchanged.

**Reconciled.** `_data/projects.yml` (RAP `2.7.25 → 2.10.4`, updated Jun 27 / `2026-06-27`; fairyfox
meta row `0.9.5 → 0.9.6`), `_data/pulse.yml` (ticker re-led with the seven genuinely-recent 2026-06-27
highlights — online build, intensity dial, provider header, keyword tooling, CodeMirror editors, the
content refactor, and the site's 0.9.6 force-push fix — the prior provider/gallery batch re-aged to 2d/3d;
bars rolled forward one day with a high newest value for the heavy 2026-06-27), the RAP node page
(`_projects/random-ai-prompt.md` — intensity dial in the DPL line, a sentence on the matured editing
surface + the hosted online build), `_docs/random-ai-prompt.md` (intensity in the DPL bullet), and
`_data/downloads.yml` (RAP note now mentions the hosted demo at prompt.fairyfox.io).

**Verified already-accurate, left untouched.** `_data/downloads.yml` artifact links — P2's
`v0.14.2-alpha` GitHub release and all six assets confirmed present via `gh` (a bare `gh release view`
returns "not found" only because the release is a pre-release and `latest` skips it); predecessor and
P2 entries unchanged. `hub/registry.yml` (no versions; `adopts_hub`/`notes_system` flags accurate).
The other three node pages, `_data/doc_categories.yml`, the rest of `_docs/`, the About page project
list, and header/footer.

**Blogged.** New 2026-06-27 day post —
`_posts/2026-06-27-a-dial-for-the-prompt-language-and-a-home-online.md`, tags
`[random-ai-prompt, fairyfox-io, site, update]` — covering RAP's full day and the site's own 0.9.6
force-push root-cause fix in one day-centric post (the 0.9.6 work happened 2026-06-27 but had not yet
been written up). Neutral voice; the SD-capable providers stay framed as pluggable backends, not RAP's
identity; no booster tags. Round-up markers advanced in `hub/.last-seen.yml` (RAP `1868b58 → e491232`;
P2 `seen` only).

**Not touched.** The two inbound report-review briefings and their findings (CI-owned tagging as a
first-class path, force-push-emphasis wording, etc.) — those belong to the inbound report-review
procedure and a live fold-in session, not this round-up; `reports_through` markers left as-is.

Bumped `VERSION` + the meta row + `status.md` to `0.9.7`. Build-checked; shipped PATCH `0.9.6 → 0.9.7`
direct `dev → main`.
