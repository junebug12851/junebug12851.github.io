# 2026-06-25 — Session Log

## New hub standard: process reports — the system's feedback loop (0.7.0)

Live session with Fairy Fox. New standing rule requested: **whenever a project runs a
fairyfox system procedure, it should produce a full report** about the whole process —
what went wrong, suggestions, feedback, how it went — and the site should regularly
review those reports to improve the system. Reports to live in a notes folder
(`fairyfox-reports`).

Confirmed the architecture against the existing decoupled model rather than inventing a
new path: reports live in **each node's own repo** (`notes/fairyfox-reports/`, committed
there), and the hub reads them through the **same read-only `assets/references/` inbound
clones** it already pulls for round-ups. So the pull-only / one-directional / on-request
anti-recursion model is fully preserved — writing a report pushes nothing; the hub reads
read-only on request; acting on reports edits the **hub's own** standards, and nodes adopt
the improvements later. Trigger scope per Fairy Fox: **any** fairyfox system interaction
(all three runbooks + the hub-side passes), check-only runs included.

Built it end to end:

- **New standard** `hub/standards/process-reports.md` — why, the one rule, when to write
  one, where reports live + naming, what they contain, the hub's review pass, anti-recursion,
  verify.
- **Template** `hub/templates/fairyfox-report.md` + **skeleton folder**
  `hub/templates/notes-skeleton/fairyfox-reports/README.md`; the hub's own
  `notes/fairyfox-reports/README.md`.
- **Wired into the runbooks** — report step + checklist/audit/verify row in
  `new-project-setup` (also fixed a duplicate step number), `onboarding-existing-project`
  (new audit row #8), and `adopting-updates` (incl. the check-only path).
- **Hub-side review** — `reports_through` marker added to `hub/.last-seen.yml`; pointers in
  `cross-project-sync` (both copies), `blogging-workflow`, `hub/README`, `templates/README`,
  the notes-skeleton README, this repo's `notes/README` + `systems/overview`, and the
  hub-review duty added to the root `CLAUDE.md` + the `CLAUDE.md` template's mesh block.

Build-checked green; MINOR `0.6.6 → 0.7.0` via a `release/0.7.0` branch (new cross-project
standard = milestone). No process report written for *this* session — authoring a standard is
ordinary hub development, not a procedure run.

## Maintenance pass — both siblings fully onboarded; reconciled master → main (0.6.6)

Later scheduled pass (after the `0.6.5` ship earlier today). Tree clean on `dev`, `main` =
`origin/main`. Refreshed both sibling references: **both `dev` branches had been force-pushed**
since the `hub/.last-seen.yml` markers, so `--ff-only` aborted and I re-cloned both fresh —
`pokered-save-editor-2` → `f88bbfd`, `random-ai-prompt` → `a3cfd15`. Neither cut a release
(pse2 `0.14.2-alpha`, rap `2.6.1`), but both had **notable mesh work** past the markers:

- **pokered-save-editor-2** — finished project-side onboarding (mesh-aware `CLAUDE.md`,
  adopted `cross-project-sync.md`, ignored `assets/references/`) and **themed its Doxygen docs
  site** to the fairyfox design system (Case B: theme the generator — a CSS var-repoint layer +
  customised header/footer with the way-home link + pink accent). Now the second project fully
  onboarded; project-side complete pending the next `main` Pages deploy.
- **random-ai-prompt** — adopted the full **git-flow** standard and **renamed `master` → `main`**
  (`ci.yml`/`pages.yml`/`release.yml` repointed, git-workflow docs rewritten). First project to
  do the rename. (`main` and `master` both currently resolve on the remote; default-branch flip +
  `origin/master` delete are owner GitHub-side steps still pending.)

**Blogged** (day-centric, folded into the existing 2026-06-25 post): added the
`pokered-save-editor-2` tag, a new "Pokered Save Editor 2 joins too" section, and a note that
rap adopted git-flow + renamed its branch; updated the title/subtitle to "the projects join the
mesh". Advanced both markers.

**Reconciled the live data for rap's rename** (everything else checked and already accurate):
`_data/projects.yml` (rap `notes:` `tree/master` → `tree/main`; pse2 `updated` Jun 23 → Jun 25,
its genuine latest activity), `_data/downloads.yml` (rap source zip `master.zip` → `main.zip`),
`_docs/random-ai-prompt.md` notes link, and the `2026-06-21` DPL post's two `blob/master` deep
links — all → `main`. Refreshed `_data/pulse.yml` ticker (pse2 themed docs + rap rename lead;
dropped the stale "shipped to master"; bars left as-is — same calendar day, already rolled).
`downloads.yml` binaries, both node pages, `doc_categories.yml`, the About page, header/footer:
already accurate, untouched. Bumped `VERSION` + `projects.yml` (meta) + `status.md` to `0.6.6`.
PATCH `0.6.5 → 0.6.6`.

## Maintenance pass — reconciled, nothing to ship

Scheduled maintenance pass ran. Refreshed both sibling references
(`assets/references/`): `pokered-save-editor-2` and `random-ai-prompt` both came back
*already up to date*, sitting exactly on their `hub/.last-seen.yml` markers
(`005a0cb` / `96ee2ad`), so no new sibling commits to blog. Reconciled the data-driven
surface against reality and found it all current from the earlier `0.6.5` pass:
`_data/projects.yml` (pse2 0.14.2 · rap 2.6.1 · pse 2.0.1 · site 0.6.5),
`_data/downloads.yml` (pse2 pinned to `v0.14.2-alpha`, rap source-only),
`_data/pulse.yml`, `hub/registry.yml`, the four `_projects/*` nodes, and the `_docs/`
library (incl. `docs-site.md` and the `0.6.4`-reconciled `git-workflow.md`) — no stale
SD framing or booster tags, no hardcoded stale versions. Tree clean, `main` = `origin/main`
(the `v0.6.5` merge `d676cf8`). No release, nothing unblogged → no content commit; VERSION
unchanged at `0.6.5`. Recording this note only.

## Hub round-up + site refresh, shipped from the working tree (0.6.5)

A parallel session produced a 2026-06-25 hub round-up and left it uncommitted in the
working tree; FF told me the other process was done and to handle it. Reviewed the whole
batch (all coherent): new day post (nodes-can-check-the-hub, docs-site standard, full
git-flow + `master → main`, RAP onboarding + SFW toggle), `hub/.last-seen.yml` markers
advanced, `_data/pulse.yml`/`projects.yml`/`downloads.yml` + `notes/status.md` refreshed
(HTTPS now live; RAP→2.6.1; RAP zip → `master`), and a new `_docs/docs-site.md` summary.
**Reconciled the one lag:** the parallel pass synced `_docs/git-workflow.md` only to the
`0.6.3` framing, so I brought that page **and** the post's git section up to the `0.6.4`
SemVer release rule (PATCH direct `dev → main`; MINOR/MAJOR via `release/*`). Bumped
VERSION + `projects.yml` + `status.md` to `0.6.5` for consistency. Per FF's "follow normal
procedure, ignore the other work" steer this shipped as a normal PATCH (direct `dev →
main` + tag `v0.6.5`); staged the files explicitly (no `add -A`). PATCH `0.6.4 → 0.6.5`.

## Tie release branches to the SemVer level (0.6.4)

FF's compromise on release ceremony: **`release/*` branches are for MINOR and MAJOR
only; PATCH increments release directly `dev → main` (`--no-ff` + tag).** Replaces the
fuzzy "routine vs substantive" latitude with an objective, self-explaining rule —
ceremony proportional to the change. Updated `hub/standards/git-workflow.md`: the
`main` bullet + `release/*` bullet now state the rule; "Cutting a release" split into
PATCH (direct) and MINOR/MAJOR (`release/X.Y.0` branch) flows; the "Solo latitude"
section trimmed to just the trivial-change-on-`dev` point (release path is no longer
latitude); merging + VERSION notes reconciled (every merge into `main` is a tagged
release; PATCH VERSION bump happens on `dev`). Mirrored into
`notes/reference/git-workflow.md`; `CLAUDE.md` step 3 split by SemVer level. PATCH
`0.6.3 → 0.6.4` (so this very release dogfoods the direct path).

## Full git-flow is the model, not a thin core (0.6.3)

Corrected a framing miss from 0.6.2. FF clarified that "paper-thin" was his word for the
*old* `dev → main` system being a thin shadow of git-flow — git-flow is the meatier,
better foundation, and he wants **full git-flow adopted as much as makes sense and is
possible**, NOT a minimized core with feature/release/hotfix as rare "expansions".
Rewrote `hub/standards/git-workflow.md` accordingly: full git-flow is *the* model —
`feature/*` is the normal unit of work, `release/*` is the release mechanism (main is
updated **only** via release/hotfix, never `dev` merged straight in), `hotfix/*` for
urgent prod fixes; added explicit feature/release/hotfix command flows. Kept canonical
`--no-ff` throughout + tags on main, and the `master → main` mandate + safety rules.
Demoted the solo simplifications to a clearly-secondary **"Solo latitude"** note
(trivial change → commit on `dev`; routine release → direct `dev → main` when a
`release/*` branch would buy nothing) — latitude *within* the model, not a lighter
model. Mirrored into `notes/reference/git-workflow.md`; updated `CLAUDE.md` Default
Workflow steps 2–3 (feature branch for features; release via `release/*` or direct for
routine). PATCH `0.6.2 → 0.6.3`.

## Reframe the git standard as git-flow (0.6.2)

FF's call: **all projects use git-flow procedures** — the model and policies, not the
`git flow` scripts (he wants this upheld intelligently by the AI in each repo, not
automated). Per his steer the everyday flow stays a **paper-thin shallow git-flow**
(commit on `dev`, release on `main`) and git-flow's `feature/` `release/` `hotfix/`
branches are documented as *expansions on top*, used only when a change warrants them.
Decisions: keep the integration branch named **`dev`** (mapped to git-flow's `develop`
role — no churn to mesh-sync/registry refs); adopt git-flow's **`--no-ff`** merge style
(he deferred to the git-flow default). Rewrote `hub/standards/git-workflow.md` around
this (core + expansions + branch-naming `type/` prefix), kept the `master → main`
mandate and the safety rules (noting `--no-ff` merge commits are additive, not a
rewrite). Mirrored into `notes/reference/git-workflow.md`. Updated `CLAUDE.md` Default
Workflow step 3 from `merge --ff-only` to the git-flow release ritual
(`merge --no-ff dev` + annotated `vX.Y.Z` tag matching `VERSION` + push `--tags`).
**This reverses the prior FF-only-to-`main` rule** for releases — flagged for FF in
case he prefers tag-only. PATCH `0.6.1 → 0.6.2`.

## Mandate master→main across the mesh (0.6.1)

FF reversed the earlier branch stance: `master` should be MANDATED to become `main`. So `main`
is now the required stable branch mesh-wide. Added the mandate + safe rename procedure (rename,
set default, fix Pages source branch / CI-release on:push filters / tree/master URLs — never a
history rewrite) to `hub/standards/git-workflow.md`. Reversed `onboarding-existing-project.md`
step 2 ("adopt the branch model — rename master"), its gotcha, and audit row 3 (stable=main
required). Updated `registry.yml` comment: `branch` is the dev work branch (unaffected by the
rename); stable branch is main. Mirrored into `notes/reference/git-workflow.md`. RAP is still on
master → owes the rename (its job; also its projects.yml `notes:` tree/master URL updates then).
PATCH `0.6.0 → 0.6.1`.

## Standards update from the RAP onboarding report — generator docs + relaxed branding (0.6.0)

The fixed runbook worked: a second session onboarded random-ai-prompt properly (mesh-aware
CLAUDE.md + a themed docs site live at fairyfox.io/random-ai-prompt/) and filed a detailed
report. Acted on its findings against the standards (FF's instruction: improve the docs, the
project fixes are its own job):

- **Generator-based docs = first-class path** (report P0). `docs-site/06` now splits generated
  docs into (A) boundaried zone inside a hand-built site vs (B) **the generator IS the site**.
  For B, documented the technique the session proved: replace the generator stylesheet from
  scratch (NOT override — defaults hard-code light bg, overrides leak), inject brand + way-home
  into the generator's own sidebar (don't overlay a bar that fights its fixed layout), and make
  the build COPY theme assets into output (docdash references-but-doesn't-copy → CI 404s). Wired
  into onboarding step 6 + new-project-setup + `05` + `01`.
- **Relaxed branding/back-links** (asked FF; both answers = looser). Shared theme is the tie, not
  brand-precedence: project leads with its OWN brand (Fairy Fox mark may be replaced); the one
  required back-link is a persistent "← Back to Fairy Fox"; footer/breadcrumb/node-link now
  recommended. Retired the strict rules in `05`/`08`. RAP's project-forward + way-home-only is now
  compliant.
- **Hub-side hygiene.** Clarified registry `branch` = sync-tracking work branch (dev default),
  NOT the repo default — the report (and the earlier bad session) misread it; RAP's `dev` is
  correct, kept it. Added honesty rule (don't pre-set adopts_hub/notes_system true before the work
  exists). Blessed held-`master` verification. RAP entry needs no value change (flags now honest).

`branch`-change recommendation from the report was declined with reason. MINOR `0.5.4 → 0.6.0`.

## Fix the onboarding audit — "registered ≠ integrated" (0.5.4)

FF flagged that a separate session's onboarding-verification of random-ai-prompt missed "the
website thing and probably more." Confirmed from the hub side: RAP is hub-registered (both
registries, node page, docs page, 5 blog posts) but project-side it's barely integrated — its
`CLAUDE.md` has zero mesh content (no fairyfox/hub/cross-project/check-for-updates), and the live
`fairyfox.io/random-ai-prompt/` is raw **default-theme JSDoc** (grepped the fetched page: ~1 stray
fairyfox marker across 1365 lines — no Fraunces, no main.css, no site-header, no "Fairy Fox", no
back-links). So it fails the docs-site standard entirely.

FF: don't fix the project (that's its job) — fix the **document** so a retry catches this. Root
cause was in `onboarding-existing-project.md`: it (a) conflated hub registration with integration,
(b) never made the `CLAUDE.md` mesh-awareness block a checkable item (step 4 just said "fold the
structure in, keep landmines"), and (c) had a soft docs-site check ("moving toward… full or
honestly-partial") that an unthemed JSDoc dump passes. Fixes: step-4 row now **requires** the mesh
block with a callout that it's the easiest thing to skip on a mature CLAUDE.md; step 6 adds a hard
"raw generated docs alone = missing, not partial — look at the served page" warning; replaced the
soft Verify with a **"Registered ≠ integrated"** section + honest-partial clause + a **7-row
completeness audit** (report done/partial/missing per row; never say "fully onboarded" with any
gap). Mirrored the CLAUDE.md-mesh check + "default-theme generator output is a miss" into
`new-project-setup.md`. Memory updated with the lesson. PATCH `0.5.3 → 0.5.4`.

## Wire docs-site into the lifecycle runbooks + keep-current trigger (0.5.3)

FF asked whether the docs-site work is reflected in the workflow files. It wasn't — grep
confirmed none of the three runbooks referenced the docs-site standard, so it was orphaned.
Wired it into all three: new-project-setup (new step 6 "build the themed docs site" + checklist
row + verify bullet; Register/Commit renumbered 7/8 conceptually), onboarding-existing-project
(new step 6 reconcile-not-clobber docs — re-skin existing/Doxygen, partial-ok; renumbered Commit
to step 8 + verify bullet), adopting-updates (new "Design system" row in the apply-by-category
table → `docs-site/` incl. measurements + `reference/main.css` + compliance checklist).

Then FF: "keep all this updated by default" → strengthened the `CLAUDE.md` design-system
maintenance trigger to fire on `assets/css/main.css`/layouts/includes edits and to keep the spec
prose + `11-measurements-reference.md` + the bundled `reference/main.css` snapshot all in step
(master-copy exemption unchanged). Updated the docs-site memory. PATCH `0.5.2 → 0.5.3`.

## Docs-site standard — exact measurements + bundled stylesheet (0.5.2)

FF asked whether the style/theme files carry exact measurements and placement. Honest answer:
tokens + shell were exact (~109 values) but some component internals were qualitative. FF chose
the full pass + main.css reference. Wrote `11-measurements-reference.md` pinning every element's
exact padding/gap/size/position + the full breakpoint/motion lists, and bundled the real
stylesheet read-only at `reference/main.css` (hash-verified faithful copy) with an annotation
README. Wired into the docs-site README index + a pointer atop `04`.

**Bash-mount hazard confirmed:** my earlier sandbox read of `main.css` truncated at 204 lines —
the real file is 455. Re-read with the file tool (got cards/badges/doc-sidebar/footer/node
styles that the truncated read was missing), so the measurements are from the true source.
Reinforced the never-use-bash-here rule in memory. PATCH `0.5.1 → 0.5.2`.

## Docs-site standard — domain & publishing (0.5.1)

FF asked how a project actually joins the `fairyfox.io` domain on GitHub Pages — the infra under
the standard's one-domain model. It's already working for the two siblings (per status.md), and
the mechanism is auto-inherit: `fairyfox.io` is the custom domain on the **user site**
(`junebug12851.github.io`), so every other repo's Pages site serves at `fairyfox.io/<repo>/`
automatically. Wrote `hub/standards/docs-site/10-domain-and-publishing.md`: how it works, DNS is
once at the apex (projects need none), per-project steps (enable Pages, set base path `/<repo>`,
verify HTTPS, set registry `docs:`), and gotchas (NO CNAME in the project repo — it conflicts;
base path must equal the slug or the theme breaks; HTTPS; themed 404). Wired into the docs-site
README index + the one-domain section of `05`. PATCH `0.5.0 → 0.5.1`.

## Docs-site design-system standard (0.5.0)

FF wants every project's docs site to match fairyfox.io's theme/colours/style/structure and
to transition seamlessly both ways, preferring a custom theme per project. Clarified scope with
two questions: FF wants **a spec folder, not code** — comprehensive written files that AI-driven
projects read and implement in their own stack to stay compliant; the projects' differing
tooling (Doxygen, JS, etc.) doesn't matter since they can follow instructions. Mid-task FF added
a hard constraint: **fairyfox.io is the master copy and is exempt from auto-applied theme/style
updates** (separate manual-review process).

Authored `hub/standards/docs-site/` (10 files): README (front door + compliance model + the
master-copy exemption) · 01 overview/principles (one-domain model, match-don't-link) · 02 design
tokens (exact dark+light palette pulled from `assets/css/main.css`, Fraunces/Inter/JetBrains,
spacing/radius/shadow/motion/meta) · 03 layout/structure (64px sticky shell, `.wrap`/bands, the
docs sidebar+content+crumbs layout, responsive 820px/60rem) · 04 components (brand, nav, buttons,
cards, badges, code, callout, tables, footer) · 05 navigation/cross-linking (the seamless model:
parent→child links already on the site, child→parent header brand→fairyfox.io + locator→node
page + footer; sub-brand rule; no-jump checklist) · 06 content/organization (two doc surfaces,
recommended page set, API-docs boundary) · 07 accessibility (WCAG 2.1 AA) · 08 compliance
checklist · 09 adopting/maintaining (master-copy exemption authoritative here; adopt via the
check-for-updates flow; record deviations).

Grounded it in the real theme: read `_config.yml`, layouts (`doc`/`project`/`default`), includes
(head/header/footer/project-card), all of `main.css`, `doc_categories.yml`, `projects.yml`.
Wired into `hub/README.md` (standards table) and a new `CLAUDE.md` maintenance trigger (with the
manual-review-only caveat). MINOR `0.4.12 → 0.5.0`. All under `hub/` (site-excluded), no render
impact. Not done: the projects themselves implementing it (read-only here) — rolls out via the
adopting flow; and any main-site cross-link enhancements (already mostly in place).

## Maintenance pass — reconcile docs/data + blog the mesh-machinery days (0.4.12)

Scheduled "usual stuff" pass. Both git-ignored shallow clones (`pokered-save-editor-2`,
`random-ai-prompt`) fetched clean and were **already up to date** at the
`hub/.last-seen.yml` markers (`005a0cb` / `8fd02ab`) — no new upstream commits, so **no
sibling round-ups** and no marker advance. (Note: git/file state on the mount read stale
early in the pass — HEAD appeared at `0.4.9` with the later work uncommitted — but settled
to the real baseline of `0.4.11` once refreshed; built on that.)

Reconciled the whole data-driven surface against the now-shipped `0.4.7–0.4.11` work and
found three genuine drifts (the rest already accurate):

- **`_data/projects.yml`** — the `fairyfox.io` meta row was stale at `0.4.5`/Jun 24;
  brought to `0.4.10`/Jun 25. Sibling rows (PSE2 `0.14.2`/Jun 23, RAP `2.6.0`/Jun 22,
  predecessor) unchanged and correct.
- **`_data/pulse.yml`** — the ticker still led with the Jun-24 design increments as
  "today". Refreshed it: the update-command layer and the three lifecycle runbooks now
  surface first, the design work aged to `1d`, the sibling highlights re-aged (`2d`/`3d`/
  `5d`/`6d`); rolled the `bars` window forward one day.
- **`_docs/random-ai-prompt.md`** — the docs-library page still described the project as a
  **Stable Diffusion** prompt generator (the de-SD pass had updated projects.yml/About/node
  but missed this page). Rewritten to the DPL framing the rest of the site uses.

Surfaced the three new shared standards that had no library page —
`_docs/new-project-setup.md`, `onboarding-existing-project.md`, `adopting-updates.md`
(standards group, orders 7–9, readable summaries pointing at the canonical `hub/standards/`
copies; public "hub" voice). `downloads.yml`, the other nodes, `doc_categories.yml`, the
About page, and header/footer were checked and **already accurate** — left untouched.

Blogging: the two mesh-machinery days were unblogged (the prior 0.4.6 pass predated them).
Added a new `2026-06-25` day post for the update-command / check-for-updates layer, and
folded the lifecycle-runbook strand into the existing `2026-06-24` day post. Brought
`notes/status.md`'s version line current. PATCH `0.4.11 → 0.4.12`.

## Update command — make the word "fairyfox" mandatory in the gate (0.4.11)

FF caught that my 0.4.10 qualifying list still allowed handles with no "fairyfox" in them
("the hub", "the mesh", "the standards", "the sync doc", a runbook name). Per FF the gate is
the **two words "fairyfox system"** — exception allowed for "fairyfox standards". So the rule
is now: the request must literally carry the word **fairyfox** ("fairyfox system" / "fairyfox
standards" / "fairyfox.io") paired with an update intent; the generic handles and a bare
"system" explicitly do NOT qualify. Corrected all three command spots
(`adopting-updates.md`, `hub/templates/CLAUDE.md`, `CLAUDE.md`). `VERSION` → 0.4.11.

## Update command — gate on a named fairyfox reference; "fairyfox system" naming (0.4.10)

FF refinements to how the check-for-updates flow is triggered:

- **The gate is a fairyfox-ecosystem reference, not the word "update".** "Update" alone is
  ambiguous (OS / deps / a file), and FF noted a bare "system" could mean anything too. So the
  flow fires only when the request **names the fairyfox ecosystem** — "fairyfox" / "fairyfox.io"
  / "the fairyfox system", "the hub", "the mesh", "the (shared) standards", or a named
  standard/runbook — *paired with* an update/sync intent (check for updates · what changed ·
  sync · refresh · pull the latest · get the newest · adopt the latest). Full qualifying list
  written into `adopting-updates.md`; condensed into the two `CLAUDE.md` spots.
- **Naming split.** AI/command layer calls it **"the fairyfox system"**; the public **website
  keeps "hub"** (FF: shorter/nicer). Site content untouched; structural paths (`hub/`, registry)
  keep "hub". Bridged once per doc.

Touched `hub/standards/adopting-updates.md`, `hub/templates/CLAUDE.md`, `CLAUDE.md`. `VERSION`
→ 0.4.10. Still on the table (offered): a broader hub→system rename across registry roles /
standards — not done, to avoid breaking things.

## Update-command layer — nodes can check the system for updates (0.4.9)

Continuation of the lifecycle-runbook work. Fairy Fox wanted each project to behave like a
node that knows how to check the system for updates — say "check system updates" (or anything
like it) and the AI knows to get the newest version and pull the relevant file — with a level
of hub integration that's real but won't break things unasked.

Decided (FF's call) on **check-and-report, then wait**: the safest autonomy level — discover
and explain, change nothing without a clear go-ahead. Also corrected course mid-task: FF noted
this shouldn't be a rigid keyword/function-call list — AI is natural language — so it's framed
as an *intent to recognize in plain conversation*, not magic phrases.

Changes:

- `hub/standards/adopting-updates.md` — new opening section "Checking for updates — what to do
  when asked": recognize the update intent naturally, run refresh → diff → **report → stop**,
  apply only on an explicit go-ahead. Plus the "why bounded this way" rationale tying it to the
  anti-recursion rules.
- `hub/templates/CLAUDE.md` — expanded the cross-project section into a standing instruction so
  every adopting node inherits the check-for-updates behavior and its guardrails (on-request,
  read-only, git-ignored clone, reconcile-not-clobber, no apply/history-rewrite without go-ahead).
- `CLAUDE.md` (this repo) — added the hub-side reading: the hub never adopts from itself, so
  "check for updates" here = scan siblings for round-ups + internal consistency, same
  report-first posture.

`VERSION` → 0.4.9. All under `hub/` + root docs (site-excluded), no render impact.
