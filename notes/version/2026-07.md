# Changelog — July 2026

Newest on top. See [`../version.md`](../version.md) for the inline rule (each
entry ships inside the commit it describes).

### 2026-07-10 — Project-card tech icons: light grouping by kind (0.15.6)

Follow-up to 0.15.5. The card's tech icons are now grouped by kind — language, then
framework/runtime, then platform — with a faint hairline divider between groups, so a glance
separates "what it's written in" from "with what" from "for where". Grouping is driven off a
per-tag category in `_includes/project-card.html` (`.tech-group` wrappers; empty groups are
dropped) and styled with `.tech-group` + a `border-left` divider in `assets/css/main.css`.
Group order is stable regardless of how the tags are authored in `_data/projects.yml`.
Verified with a local `jekyll build` + a headless Chrome screenshot (2×).

### 2026-07-10 — Project cards: icon tech row + Updated moved to the bottom (0.15.5)

A second, moderate pass on the project card. The text tag chips (`JavaScript`, `Qt 6`,
`Desktop`, …) are replaced by a compact row of Font Awesome platform/language icons — each
mapped from its tag in `_includes/project-card.html` (brand marks where they exist: JS, Node,
Angular, GitHub; solid stand-ins elsewhere: `gem` = Ruby, `flask` = Jekyll, `atom` = Electron,
`cubes` = Qt, `code` = C++/TypeScript, `paintbrush` = QML, `globe` = Web, `desktop`, `palette`
= Canvas). Each icon greys by default, lifts to the project accent on hover, and reveals a
small label chip so the exact tech is never ambiguous. The `Updated` date moved to the very
bottom of the card, under the Docs/Repository links, set off by a hairline divider. Font
Awesome 6 is now loaded from cdnjs in `_includes/head.html` (alongside the existing Google
Fonts). Styles: `.tech` / `.tech-item` / `.tech-label` in `assets/css/main.css`. Verified with
a local `jekyll build` + headless Chrome screenshots (default + hovered icon, 2×) — every glyph
renders and the labels read cleanly.

### 2026-07-09 — Project cards: description moves into a "?" tooltip (0.15.4)

The project card no longer shows its description as an always-on line. Each card now
carries a small "?" help button in the top-right corner, and the blurb appears as a
hover/focus tooltip beside it — leaving the card's top block (name · lifecycle · version
· activity), the Updated date, tags, and the two links (Docs · Repository) as the only
always-visible content. Every blurb in `_data/projects.yml` was tightened to a single
tooltip-sized sentence. It's the shared `_includes/project-card.html`, so the home page
and `/projects/` change together; styles live in `assets/css/main.css` (`.proj-help` /
`.proj-tip`, plus `padding-right` on `.proj-top` so the name clears the icon). Verified
with a local `jekyll build` + headless screenshots of the default and hover states.

### 2026-07-08 — Maintenance round-up: reconcile to RAP 2.50.1 + Games 0.19.3, blog the 7th (0.15.3)

Routine "usual stuff" pass over the whole surface. PowerShell + Read/Write/Edit only (no bash).
Tree clean on `dev` beforehand bar the pre-existing untracked `notes/fairyfox-reports/inbound/INBOX-*.md`
briefings (left untouched — inbound report-review's job, not this pass's).

**References refreshed (all clean fast-forwards on `dev`, no force-push):**

- **random-ai-prompt** `5ea0761 → 3147535`, VERSION `2.49.6 → 2.50.1` (commits dated 2026-07-07). A new
  **command-line target** — `targets/cli/`, the `prompt` CLI (`2.50.0`, MINOR): a traditional args-and-flags
  tool covering every feature as a subcommand (generate/list/config/keys/rewrite/upscale/completion), with
  `--help`, colours, and bash/zsh/fish/PowerShell completion, reusing the **same engine, providers,
  settings, and on-disk store** as the web+desktop app. Then `2.50.1`: disposed of the pre-DPL `chaos`
  knob for good (byte-identical output) and lifted the duplicated seed/re-roll logic into an engine-owned
  `engine/promptRun.js` the web facade + CLI now share. A ComfyUI target is planned, not yet built.
- **fairyfox-games** `71982e5 → 2411178`, VERSION `0.19.2 → 0.19.3` (commits dated 2026-07-07). A GROW day:
  **Symmetry** brought onto the varied-structure standard (5th of 11 games) — its flat twin/single coin-flip
  is now a seeded sequence of named cadences (Mirror/Reflection/Cascade/Weave/Split/Kaleidoscope, minStage-gated
  so climbing opens the pool). +8 pure-core tests (369/369 green).
- **pokered-save-editor-2** — "Already up to date" at `acc09b9`, still `0.14.2-alpha`. Nothing to reconcile.
- **fairyfox-stories** — "Already up to date" at `803db1f`, still `0.2.0`. Nothing to reconcile.

**Components reconciled:**

- `_data/projects.yml` — RAP → `2.50.1` / Jul 7 (blurb editions list gains "a command-line tool"); Games →
  `0.19.3` / Jul 7; site meta → `0.15.3` / Jul 8. PSE2, Stories, and the predecessor left accurate as-is.
- `_data/pulse.yml` — ticker re-aged and re-led with the 2026-07-07 highlights (RAP's CLI + chaos/prompt-run,
  Games' Symmetry cadences); the three now-4-day-old tail entries dropped; `bars` advanced one day.
- Node pages — none exist (retired `0.13.0`); `_data/downloads.yml` — none exists (removed `0.13.1`);
  the `_docs/` library + About + header/footer nav carry no sibling version specifics — all already accurate.
  `hub/registry.yml` carries no versions — accurate.

**Blogged:** the 2026-07-07 day post ([`_posts/2026-07-07-a-command-line-prompt-tool-and-a-fifth-game-onto-varied-structure.md`]) —
tags `[random-ai-prompt, fairyfox-games, update]`. Last-seen markers advanced (RAP → `3147535`, Games →
`2411178`; PSE2 + Stories `seen` → 2026-07-08, markers unchanged). Nothing site-notable shipped in this
repo beyond the reconcile, so no `[site, update]` post this cycle.

### 2026-07-07 — Maintenance round-up: reconcile to RAP 2.49.6 + Games 0.19.2, onboard the Stories mirror, blog the 6th (0.15.2)

Routine "usual stuff" pass over the whole surface. PowerShell + Read/Write/Edit only (no bash).
Tree clean on `dev` beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md`
briefings (left untouched — inbound report-review's job, not this pass's).

**References refreshed (all clean fast-forwards on `dev`, no force-push):**

- **random-ai-prompt** `8f56153 → 5ea0761`, VERSION `2.43.1 → 2.49.6` (all commits dated
  2026-07-06 — a big day). Auto-updating (check-and-notify banner + desktop auto-install, 2.44.0);
  a gallery composer with live placeholders, multi-select delete, a11y + SEO (2.45.0); a repo-root
  `user/` content overlay beside `data/` (2.46.0); a restructure into `engine/` + `targets/` with
  `targets/desktop → web-shell` (2.47.0) and the **"dynamic prompts" → "blocks"** rename everywhere
  (2.48.0); then an **interactive DPL editor** — hover intensity/focus dials, per-line actions, line
  numbers, active-line highlight — plus a Manage AI-refine toolbar + draft-from-description (2.49.0–2.49.6).
- **fairyfox-games** `5f908ae → 71982e5`, VERSION `0.16.3 → 0.19.2` (all commits dated 2026-07-06).
  An eleventh game **Sluice** (colour-sorting, a new verb; 0.18.0) + white-flash fixes; then the
  collection **adopted Jekyll** to mesh its games — a `_games` collection landing + shared chrome, a
  `/tags/` browse-by-mechanic page + shared card include, `_data/changelog.json` single source, a
  Jekyll-build deploy (0.19.0), an in-place landing tag filter (0.19.1), and a generated README table (0.19.2).
- **pokered-save-editor-2** `1bae326 → acc09b9`, still `0.14.2-alpha` — its first change in ~10 days,
  docs-site only (no VERSION bump): the Doxygen docs adopted the hub 0.14 chrome + shared reader,
  flattened to the reference (muted rose accent, centred prose, framed screenshots), rebuilt the
  Overview to the hub's exact measures, and added a themed `/screenshots/` gallery.
- **fairyfox-stories** — new mesh node, **cloned fresh** under `assets/references/` (it became
  `reference: true` when 0.15.1 registered it). At `0.2.0` (2026-07-06): an AI-managed Jekyll "story
  farm" that planted a starting shelf of five illustrated books on a first-class pseudo-author system.

**Components reconciled:**

- `_data/projects.yml` — RAP → `2.49.6` / Jul 6; Games → `0.19.2` / Jul 6; PSE2 `updated` → Jul 6
  (real docs-site commits that day; version unchanged); site meta → `0.15.2` / Jul 7. Stories was
  already accurate (`0.2.0` / Jul 6, registered in 0.15.1); the predecessor left as-is.
- `_data/pulse.yml` — ticker re-led with the genuinely recent 2026-07-06 highlights (RAP's editor +
  restructure + auto-update, Sluice + the Jekyll move, Stories' first shelf, the chrome bundle, PSE2's
  docs reskin), `when` re-dated, and the `bars` window rolled forward one day.
- `_docs/docs-site.md` — the live summary was stale: the primary-nav enumeration was missing **Stories**
  (added 0.15.0), and the page predated the **shared-chrome bundle**. Fixed the nav and added a paragraph
  noting the chrome is now a vendored, pull-at-build exact copy (the one reimplement-don't-copy exception).
- `hub/registry.yml`, `_data/doc_categories.yml`, `about.md`, header/footer nav — reviewed, already
  accurate (registry-driven; carry no versions). No node pages / downloads data (both retired earlier).

**Blogged.** `_posts/2026-07-06-auto-updating-prompts-an-eleventh-game-and-a-shelf-of-stories.md`
(tags `random-ai-prompt, fairyfox-games, fairyfox-stories, pokered-save-editor-2, site, update`) —
day-centric across all four threads plus the site's own 0.14.5/0.15.0/0.15.1 chrome-and-Stories work.
Project links use each project's own on-domain page (`/<key>/`); the retired `/projects/<key>/` node
paths are avoided.

**Markers advanced** in `hub/.last-seen.yml`: RAP → `5ea0761`, Games → `71982e5`, PSE2 → `acc09b9`
(all `seen` → 2026-07-07), and a new `fairyfox-stories` block set to `803db1f`. New sibling process
reports (RAP/Games/PSE2/Stories) left for the inbound report-review pass — `reports_through` untouched.

VERSION → 0.15.2 (PATCH — maintenance reconcile + blog).

### 2026-07-06 — Register Fairy Fox Stories in the project registries (0.15.1)

Followed the `0.15.0` nav slot by registering the project it points at. Pulled the real
metadata from GitHub (`junebug12851/fairyfox-stories`): a Jekyll **AI-managed story
farm** — a growing library of short, pre-planned interactive books set in living,
cross-linked universes, at `v0.2.0`, live at `fairyfox.io/fairyfox-stories/`, and already
a mesh node (notes system, mesh-aware `CLAUDE.md`, the shared fairyfox chrome, badges,
`SECURITY.md`, `CONTRIBUTING.md`). Added it to both `_data/projects.yml` (human/rendered)
and `hub/registry.yml` (machine/sync), kept in step: lifecycle **alpha**, active,
accent `#b06ad0`, `integrated: true` so it pins in the integrated tier alongside Games
(both are nav-promoted), `reference: true`/`adopts_hub: true`/`notes_system: true` per
the visible onboarding. This closes the follow-up the `0.15.0` note deferred.
VERSION → 0.15.1 (PATCH).

### 2026-07-06 — A vendored shared-chrome bundle + a Stories nav slot (0.15.0)

Two things, one release.

**Stories in the primary nav.** Added a **Stories** item immediately left of Games,
wired exactly like Games: a `/stories/` redirect stub (`stories.html`) that
meta-refreshes to `/fairyfox-stories/`, carrying the same reader-resolved early-paint
so it never flashes white. Propagated the new slot to every place the fixed mesh-wide
nav set is written out (`_includes/header.html` + the docs-site standard's `01`/`04`/
`05`/`08` and `reference/chrome.html`). Registering fairyfox-stories as a *tracked
project* (cards, `projects.yml`/`registry.yml`, integrated tier) is a deliberate
follow-up — it needs the project's real metadata, and the registry honesty rule forbids
seeding fabricated version/lifecycle.

**The chrome is now a vendored bundle, not a reimplementation.** The header/primary-nav,
submenu, footer, reader/theme menu, and palette are the one part of the design system
that's a fixed, framework-agnostic shell — so reimplementing it per project (the old
"reimplement, don't copy a CSS file" rule) is exactly what made every project's bar/
reader/footer drift. New standard **[`docs-site/12-shared-chrome.md`](../../hub/standards/docs-site/12-shared-chrome.md)**
(with a `## Verify`) plus the bundle itself at **`docs-site/chrome/`**: the static
`head`/`header`/`subnav`/`footer` HTML, per-generator adapters (Jekyll · Doxygen ·
static/SPA), a `VERSION`, and the git-pull contract. CSS + JS are **pulled from the live
master paths** (`assets/css/main.css`, `assets/js/{reader,nav}.js`) so there's one source
of truth and no snapshot lag; projects copy it verbatim and **pull over git at build
time** — still decoupled (vendored copies, never a runtime hot-link), just byte-identical
instead of paraphrased. Narrowly revised the "reimplement, don't copy" framing across the
docs-site `README`, `01`/`04`/`05`/`09`, folded a chrome-bundle row into the `08`
checklist and the mesh `compliance.md` docs-site row, and added a keep-the-bundle-in-step
trigger to `CLAUDE.md`. Tokens/layout/page-components are unchanged — still spec-and-
reimplement. Hub-side only; siblings adopt on request. VERSION → 0.15.0 (MINOR).

### 2026-07-06 — Fix: the `/games/` redirect stub flashed white en route to Games (0.14.5)

The "Games" nav link points at `/games/` — a redirect stub (`games.html`, `permalink: /games/`)
that meta-refreshes to `/fairyfox-games/`. The stub carried **no styling**, so the browser painted
it plain **white** for the instant before the refresh fired: a visible white flash every time you
opened Games from anywhere on the site (the rest of the site and the destination are dark, so it
stood out). Gave the stub the site's own early-paint treatment — a tiny inline script sets
`<html>`'s background/colour to the **reader-resolved theme** (`#181017` dark / `#efe4d1` light /
`#f1e3c2` sepia, via the shared origin-wide `fairyfox:reader:b` key) before the refresh, with a
static dark fallback + `theme-color` metas. The redirect itself is unchanged; the stub now matches
the theme instead of flashing white. Verified with a Jekyll build + Chrome (computed `<html>`
background `rgb(24,16,23)`, no white frame). VERSION → 0.14.5 (PATCH).

(Context: reported against Fairy Fox Games, but the flash was never in that repo — the stub lives
here in the hub. The fairyfox-games pages got defensive early-background hardening too, v0.18.1/2.)

### 2026-07-06 — Maintenance round-up: reconcile to RAP 2.43.1 + Games 0.16.3, blog the 5th (0.14.4)

Routine maintenance pass. Refreshed the sibling mirrors (clean fast-forwards): Random AI
Prompt `2.42.1 → 2.43.1` (a pre-built Tauri desktop edition + a rebuilt, hub-themed docs
site) and Fairy Fox Games `0.13.1 → 0.16.3` (Arc, its tenth game; varied progression-first
structure; an AI-managed game-farm framing with a player-facing changelog). Pokered Save
Editor 2 unchanged at `0.14.2-alpha`. Reconciled `_data/projects.yml` (RAP → 2.43.1 / Jul 5
with a Desktop tag; Games → 0.16.3 / Jul 5) and refreshed the `_data/pulse.yml` ticker to the
newest micro-changes. Wrote the 2026-07-05 day post ("A tenth game, a desktop app, and a
sharper reader") covering both siblings and the site's own reader/theme work. Advanced the
round-up markers. No downloads data (removed in 0.13.1) and no node pages (retired in 0.13.0)
to touch; the docs library and About page were already accurate.

### 2026-07-05 — Blog secondary tags: coloured chips, de-duplicated (0.14.3)

Follow-up to the blog colour fix: the **other** (non-primary) tags on every post now render
as proper chips. Each secondary project carries its **own accent** (a coloured dot), the
primary project is no longer repeated as a chip (it's already the meta dot + name), and the
structural `site`/`update` tags are hidden — on the blog list and the post page alike.

### 2026-07-05 — Theme icons, a classic sepia, curated accents, blog tag colours (0.14.2)

- **Theme control = weather/time icons.** The four preview tiles are replaced by a
  segmented row of icon buttons — **sun** (Light), **sunset** (Sepia), **moon** (Dark) —
  with an **Auto** toggle in the section header. Clearer and lighter than the tiles.
- **A proper sepia.** The muddy tan was reworked into a classic warm **parchment** —
  golden cream ground, raised near-white panels, near-ink warm-brown text — and pulled
  clearly away from the neutral Light theme.
- **Curated accents + a clean default.** The rainbow "default" swatch became a tidy
  **reset** (slashed-circle) chip; the preset dots are now a distinct, refined set (coral,
  ochre, green, teal, blue, indigo, rose) with the old double-orange removed. The dark
  theme's default accent was **deepened** so it isn't a neon orange.
- **Blog colours follow the primary tag.** A post's colour + label now come from its
  **first project tag** (authored most-prominent-first) instead of whichever project
  happened to sort last — so a Random-AI-Prompt-led round-up reads green, not the site's
  orange. Post tags render as clean chips (project chips carry the project's accent dot;
  the structural `update` tag is hidden).
- Standard synced (02/04/11 + `reference/main.css`) for the theme control, sepia, accents.

### 2026-07-05 — Reader panel rebuilt as a proper appearance menu (0.14.1)

The reader panel itself was redesigned to match well-made appearance menus (Apple Books /
Kindle), after feedback that it looked bad and unclear:

- **A real panel**: a sticky title bar ("Reading settings" + close), grouped sections
  divided by hairlines, and a footer with a **Reset**.
- **Theme = preview tiles**: four tiles each rendering "Aa" in *that theme's own colours*
  (Auto shows a light/dark split), so you see what you're picking; the active tile gets an
  accent ring. Far clearer than text buttons.
- **Text size = a slider** flanked by a small and a large "A" — the two-button stepper is
  gone. Still scales the document root font-size live.
- **Accent = colour dots**, now clearly visible with a multi-colour "Default".
- The reader button's hover/active tint now follows `--accent` (so it matches a picked
  accent instead of a fixed hue).

Verified functional (theme / accent / slider / spacing / width / reset) and screenshotted
open in dark, light and sepia. Standard synced (04/11 + `reference/main.css`).

> Note: 0.13–0.14 all shipped in quick succession; if the panel still looks like the old
> one, it's a cached asset — a hard refresh loads the current build (assets are
> cache-busted per deploy since 0.13.2).

### 2026-07-05 — Reader redesign: contrast pass, accent picker, real text scaling (0.14.0)

A MINOR milestone reworking the reader + theming after the first cut read flat and the
size/spacing controls didn't visibly do anything.

- **Contrast pass on all three themes.** Dark, light and sepia were retuned for real
  surface separation (panels raised a clear step off the ground, stronger hairlines) and
  brighter/darker secondary text, so nothing looks washed-out. **Sepia** in particular was
  rebuilt into a proper warm-paper look with near-ink brown text. Tuned to WCAG 2.1 AA.
- **Text size now scales the whole page.** The reader's size lever sets the document
  **root font-size** (`html`), so the entire rem-based UI resizes — it visibly works on
  every page, not just prose. The confusing `A−`/`A+` pair is replaced by a **5-step
  segmented "A" scale** that shows the current level. Line spacing drives `body`
  line-height; width still caps the reading measure.
- **Accent colour picker.** A row of colour circles (plus a "Default" that reverts to the
  theme accent) lets the visitor recolour the site accent. A picked hex overrides
  `--accent`/`--violet` and derives readable `--accent-ink`/`--link` by mixing toward
  `--text`, so it stays legible in every theme.
- **A little more colour** where it helps: an accent bar on section headings, an accent
  italic hero ampersand, and a fixed stray hard-coded button-hover purple.
- **New storage key `fairyfox:reader:b`.** The schema changed (accent + root-scale size),
  so the key is versioned with a `:b` suffix — it won't collide with the older
  `fairyfox:reader` a sibling may still use until it adopts this model.
- **Standard synced** (`hub/standards/docs-site/` 02/04/11 + `reference/main.css`): the new
  key/model, the accent picker, the 5-step size scale, and the retuned palettes.

### 2026-07-05 — Card CTA → "Docs ↗" + asset cache-busting (0.13.2)

- Project card CTA relabelled **"Open →" → "Docs ↗"** (external glyph) — the card opens the
  project's own documentation site, so the label now says so.
- **Cache-busting** on the stylesheet + scripts: `main.css`, `nav.js`, `reader.js` now carry a
  `?<build-time>` query (`site.time`), so a new deploy always loads fresh assets instead of a
  stale cached copy. (This is why the right-aligned reader button from 0.13.1 could still look
  centered on a hard-cached browser — the old `reader.js`/`main.css` were being reused.) The
  button placement itself was already correct at 0.13.1; verified far-right on a fresh build.

### 2026-07-05 — Trim to the projects: reader button right-aligned, Downloads + duplicated docs gone (0.13.1)

Immediate follow-up refinements to the 0.13.0 shape, all toward a leaner site:

- **Reader button moved to the far right** of the header (past "About"): `reader.js` now
  inserts it after `.nav`, `margin-left:auto` → `.5rem`.
- **Projects submenu no longer auto-enables** — with a handful of projects it only
  complicated the UX; the row is kept in code, dormant, reserved for future project
  categories (opt-in via `subnav: projects`).
- **On-site per-project documentation removed** — the four `_docs/<project>.md` pages and
  the "Project documentation" doc category. Projects link to their own docs from their card;
  the hub no longer duplicates them (the `/docs/` page's per-project block is gone too).
- **Downloads section removed** — `downloads.md`, `_data/downloads.yml`, and the nav +
  footer links. The fixed global nav is now **Home · Projects · Games · Docs · Updates ·
  About**. Keep the site to the projects.
- **Duplicated projects list dropped from `about.md`** (it just repeated `/projects/`); the
  footer's Projects column now points at each project's own page (it still pointed at the
  retired node URLs — fixed).
- **Standard + on-site docs updated in step**: the new nav order (01/04/05/08 +
  `reference/chrome.html`), the far-right reader placement (04/11 + snapshot), and the
  projects-submenu-reserved note.

### 2026-07-05 — Shared "Aa" reader menu + project cards go straight to the project (0.13.0)

A **MINOR** milestone in two moves, both toward one seamless site.

**The shared Kindle-style reader.** Random AI Prompt's refreshed docs theme shipped an
**"Aa" reader menu** (Theme: Auto/Light/Sepia/Dark · text size · line spacing · reading
width), saved under an **origin-wide `fairyfox:reader` localStorage key** so the choice
carries across every same-origin `fairyfox.io` site. fairyfox.io now has the same control:

- `assets/css/main.css` — theming moved to a `data-theme` model (the OS light query
  guarded with `:not([data-theme])` so a manual pick wins), a real **Sepia** theme added,
  `--reading-fs/-lh/-width` vars applied to the reading surfaces, and the `.ff-reader-btn` /
  `.ff-reader-panel` / `.ff-seg` styles.
- `assets/js/reader.js` — the button + panel, with the **same key + constants** as RAP so a
  choice is interchangeable. A tiny inline no-flash apply lives in `head.html`.

**Node pages retired; cards link straight through.** The hub-local `/projects/<key>/` node
pages re-stated each project's blurb/status/connections — cached detail that goes stale and
breaks the seamless feel. Removed `_projects/*.md`, the `project` layout, and the `projects`
collection. A project **card now links directly to the project's own page** on the domain
(`fairyfox.io/<key>/` — wearing the shared chrome, so it looks like this site with its own
sub-navbar), repo as the fallback; the projects submenu and `about.md` follow suit.

**Standard updated in step** (`hub/standards/docs-site/`): the reader is now a required shared
component with the origin-wide key + Sepia palette (02, 04, 05, 08, 11), node pages recorded as
retired, and the `reference/main.css` + `reference/chrome.html` snapshots refreshed.

### 2026-07-05 — Maintenance round-up: reconcile to RAP 2.42.1 + Games 0.13.1, blog the 4th (0.12.4)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean on `dev`
beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched —
inbound report-review's job, not this pass's).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `cdfaf7c → 5dcd621`, VERSION `2.41.0 → 2.42.1` (all commits
  dated 2026-07-04). No force-push.
- **fairyfox-games** — clean fast-forward `1994e12 → 0a8db92`, VERSION `0.12.0 → 0.13.1` (commits dated
  2026-07-04). No force-push.

**What the siblings shipped (2026-07-04).**

- **Random AI Prompt 2.42.1** (from `2.41.0`, all Jul 4). The headline **2.42.0** is a large-scale performance
  pass to an explicit ceiling (100k gallery / 1000 prompts / 10k images / 100k catalog, all at once): the gallery
  **virtualized** to a bounded window, the results list bounded, **placeholder-first chunked** image generation
  with per-provider image/text concurrency limiters (a new folder-based shared-settings system), and a perf test
  suite asserting scroll quality at the max load. Around it: an explicit **Random-seed control** (2.41.2) replacing
  a `-1` magic-number sentinel that had pinned every roll to one prompt — free-text, reproducible seeds; seed-preview
  independence + zero SonarCloud debt (2.41.3); a mobile settings-gear backdrop fix + a standing "every fix ships a
  regression test" rule (2.41.1); and a **dark-mode DPL autocomplete contrast fix** plus an automated per-release
  screenshot/GIF pipeline (2.42.1).
- **Fairy Fox Games 0.13.1.** A ninth, mechanically-distinct game **Symmetry** (mirror coordination — one *spread*
  control drives two mirror-locked catchers, so you often can't save both sides; gold-ringed twins catch as a pair
  for a bonus; pure core + external shell, full growth stack, 23 tests) + grew **Poise** with the honest near-miss
  line (+5 tests). 272 tests green. Also a release-policy change (0.13.1): the repo now ships `dev → main` by default
  on green (the old approve-first gate had frozen its live site at 0.11.2).

**Reconciled (changed):** `_data/projects.yml` (RAP `2.41.0 → 2.42.1` / Jul 4; games `0.12.0 → 0.13.1` / Jul 4; site
meta `0.12.4` / Jul 5); `_data/pulse.yml` (ticker + `when` + bars rolled — Symmetry, RAP's scale pass + seed control,
the dark-mode fix at the top); `_projects/fairyfox-games.md` (added Symmetry to the game list).

**Left untouched (verified accurate):** `_data/downloads.yml` (RAP is source-only, PSE2/PSE1 unchanged, no games
binaries), `_projects/random-ai-prompt.md` (the seedable/deterministic + gallery prose already covers the day's
work), `hub/registry.yml` (no versions carried), `_data/doc_categories.yml`, all `_docs/*`, `about.md`, header/footer nav.

**Blogged.** `_posts/2026-07-04-a-ninth-game-and-prompts-that-scale.md` (tags `fairyfox-games, random-ai-prompt,
update`) — day-centric on the siblings' 2026-07-04 work. Markers advanced in `hub/.last-seen.yml` (RAP → `5dcd621`,
games → `0a8db92`, PSE2 unchanged; all `seen` → 07-05); `reports_through` left for the inbound report-review pass
(new RAP report `2026-07-04-propose-regression-testing-standard.md` noted, not digested here).

### 2026-07-04 — Maintenance round-up: reconcile to Games 0.12.0, blog the 3rd (0.12.3)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean on `dev`
beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched —
inbound report-review's job, not this pass's).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `43f4c2b → cdfaf7c`, VERSION unchanged at `2.41.0` (all commits
  dated 2026-07-03). No force-push.
- **fairyfox-games** — clean fast-forward `3a44d61 → 1994e12`, VERSION `0.11.2 → 0.12.0` (commits dated
  2026-07-03). No force-push.

**What the siblings shipped (2026-07-03).**

- **Fairy Fox Games 0.12.0.** An eighth, mechanically-distinct game **Poise** — a balance game (tilt a beam
  to keep a rolling ball on it and roll it over the target; momentum carries through each catch, gravity ramps
  by stage). Pure normalised core (`pos` −1..1) + external render shell + growth stack; **25 tests** including a
  controllability test and a resting-ball no-drift regression. Also grew **Skyline** with a near-miss line
  ("N floors short of your best"). Collection at **244 tests**.
- **RAP 2.41.0 (unchanged).** CI/tooling only: the `2.41.0` release had failed at the keyless-signing step, so
  it never published — fixed by pinning cosign 2.x, then modernized properly onto **cosign 3.x Sigstore bundles**
  (`<asset>.sigstore.json`, pinned `v3.1.1`, `deployment.md` verify updated), plus an in-range Vite build-tool bump.
  No product change, no version bump.

**Reconciled (changed):** `_data/projects.yml` (games `0.11.2 → 0.12.0` / Jul 3; site meta `0.12.3` / Jul 4);
`_data/pulse.yml` (ticker + `when` + bars rolled — Poise, Skyline near-miss, and the RAP signing fix at the top);
`_projects/fairyfox-games.md` (added Poise to the game list).

**Left untouched (verified accurate):** `_data/downloads.yml` (RAP is source-only; the signing change touches no
download link — PSE2/PSE1 tabs unchanged), `hub/registry.yml` (no versions carried), `_data/doc_categories.yml`,
all `_docs/*`, `about.md`, header/footer nav.

**Blogged.** `_posts/2026-07-03-an-eighth-game-and-a-modernized-release-signature.md`
(tags `fairyfox-games, random-ai-prompt, update`) — day-centric on the siblings' 2026-07-03 work. Markers advanced
in `hub/.last-seen.yml` (RAP → `cdfaf7c`, games → `1994e12`, PSE2 unchanged; all `seen` → 07-04);
`reports_through` left for the inbound report-review pass.

### 2026-07-03 — Maintenance round-up: reconcile to RAP 2.41.0 + Games 0.11.2, blog the 2nd (0.12.2)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean on `dev`
beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched —
inbound report-review's job, not this pass's).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `2ed5787 → 43f4c2b`, VERSION `2.38.1 → 2.41.0` (all commits
  dated 2026-07-02). No force-push.
- **fairyfox-games** — clean fast-forward `58a3bb0 → 3a44d61`, VERSION `0.6.0 → 0.11.2` (its busiest day,
  all commits dated 2026-07-02). No force-push.

**What the siblings shipped (2026-07-02).**

- **RAP 2.38.1 → 2.41.0.** A **responsive rebuild** of the web app (`2.40.1` → `2.41.0`): a fluid token
  foundation + layout cascade layer, the top-bar pile collapsing behind an overflow menu, phone drawers
  (palette + Manage master/detail), single-view image-over-metadata stacking, 44px touch targets, full-width
  bottom-sheet submenus, and a **tablet split-screen tier** (769–1024px). Underneath, the **repo was
  flattened** — the `engine-v3`/`engine-v1-2` split retired, the project now living at the root
  (`src/` · `data/` · `gui/` · `tests/`), the pre-revival CommonJS system removed from the tree (kept in git
  history); architecture docs rewritten + stale-ref sweep. **`main` branch-protected** (releases via PR),
  **keyless-signed** release assets, repo-hygiene guardrails + `dev` deletion-protection + dependabot bumps.
- **Fairy Fox Games 0.6.0 → 0.11.2.** A seventh game **Loft** (keepy-uppy juggle, cluster bonus); game icons
  across the collection + a public-copy refresh leading with the AI-experiment story; adopted hub standards
  `0.9.11 → 0.12.1`; a **Growth Architecture** rolled across all seven games (core-fun pass + readable stage
  arc + meta-progression/badges, Polarity rebuilt as the reference); landing page ordered most-recently-updated;
  **Netlify retired** (GitHub Pages sole host). 217 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.41.0 / Jul 2; games → 0.11.2 / Jul 2; the fairyfox.io
meta card → 0.12.2 / Jul 3), `_data/downloads.yml` (**drift fix** — the RAP `Run it`/`Source` tabs still said
`cd …/engine-v3`; corrected to the flattened repo root), `_data/pulse.yml` (ticker re-led with the 07-02
highlights, `when` rolled, bars rolled), `_projects/random-ai-prompt.md` (flatten + responsive note),
`_projects/fairyfox-games.md` (added Loft + the growth-architecture note), `_docs/random-ai-prompt.md`
(dropped the stale `engine-v1-2` parenthetical).

**Verified accurate, left untouched:** `_data/downloads.yml` PSE2/PSE1 tabs (unchanged releases; RAP still
source/run-only, no binaries; games has no downloads), `hub/registry.yml` (no version fields),
`_data/doc_categories.yml`, `_docs/fairyfox-games.md` + `about.md` (count/version-agnostic), header/footer nav
(registry-driven).

**Blogged.** `_posts/2026-07-02-a-responsive-rebuild-a-seventh-game-and-six-new-standards.md` (tags
`random-ai-prompt, fairyfox-games, fairyfox-io, site, update`), day-centric — also folds in the site's own
2026-07-02 work (the `0.12.0` six-standards fold-back + the `0.12.1` standing adopt authorization, previously
unblogged). Round-up markers advanced in `hub/.last-seen.yml` (RAP → `43f4c2b`; games → `3a44d61`; PSE2
unchanged; all `seen` → 2026-07-03). `reports_through` left alone — the new sibling process reports are for
the inbound report-review pass.

**Notes/version.** This changelog entry; `VERSION` `0.12.1 → 0.12.2`; today's session log; `status.md` version
line + sibling version numbers refreshed. Build-checked before shipping.

### 2026-07-02 — Standing "adopt standards by default" authorization (0.12.1)

Made node adoption of hub standards the **default** posture, on the owner's instruction
("getting the nodes to adopt the standards and policies is very important — it should be
default"). Added a **standing express-authorization** (`adopt-standards-by-default`) to
[`hub/authorizations.yml`](../../hub/authorizations.yml) covering **all** of
`hub/standards/` and `hub/templates/`: when a node runs the fairyfox check/adopt flow,
any pending standard/template change is pre-authorized — it **adopts by default** (report
*and* apply) instead of reporting-and-waiting. Made this explicit in the `CLAUDE.md`
template and the `adopting-updates` runbook.

**Anti-recursion is untouched** (owner chose the "standing pre-authorization" option, not
scheduled checks): adoption still happens **only when the node runs the flow** — no
auto-pull, no scheduled cross-repo sync, no hub→node push — and the grant removes **only**
the redundant confirmation pause. Every other safety step still runs in full
(reconcile-not-clobber, divergence re-prompt, process report, reviewable commit, full
verification before and after; a node still bootstraps its first express-auth adoption
under the normal pause).

### 2026-07-02 — Fold RAP's proposals into six new/updated standards (0.12.0, MINOR)

Report-review fold-back: Random AI Prompt's six undigested `notes/fairyfox-reports/`
proposals adopted mesh-wide as mandatory standards (owner go-ahead in a live session;
two scope calls decided by the owner — **branch protection mandatory for all repos**,
**legal-docs mandatory for all repos**). The automated inbound briefing
(`INBOX-2026-07-02-0202.md`) had synthesized the same themes.

**git-workflow (fixed a defect):** the release flow never brought `main` back to `dev`,
so `dev` drifted a commit behind every release (RAP hit **32 behind**). Added the
invariant *"after any release, `dev` must contain `main`"* — PATCH/MINOR/MAJOR
fast-forward `dev` to `main`, hotfix does a real merge; reinforced never-commit-on-`main`;
noted feature branches aren't CI-tested (the `dev` merge is the gate); added a **PR-based
release path** for branch-protected repos. New `templates/branch-sync.yml` guard fails when
`dev` drifts behind `main`. Verify rows added.

**Five new standards** (each with a `## Verify` slice + a `compliance.md` matrix row):
- `supply-chain-hardening.md` — least-privilege workflow permissions, SHA-pinned Actions,
  `SECURITY.md`, signed releases, **mandatory branch protection** (solo config), stated
  solo ceiling (~8/10) + badge lag. Reconciled with git-workflow's PR release.
- `dependencies.md` — upgrade aggressively behind a real local test gate; Dependabot →
  `dev`; fix-don't-pin; bump on artifact change.
- `legal-docs.md` — self-hosted, code-accurate Privacy/Terms/Cookies, kept current;
  mandatory for **all** repos.
- `agent-tooling.md` — PowerShell + file tools, never the bash sandbox; execute don't
  hand off; `.gitattributes` line-ending hygiene.
- `badges.md` — the canonical grouped shields.io set, as many as apply per project.

**process-reports.md:** named the canonical **node → hub proposal** report shape
(`procedure: propose-standard`, required front matter) — the five RAP proposals arrived
with inconsistent/missing front matter.

**New templates:** `project.gitattributes`, `SECURITY.md`, `dependabot.yml`,
`branch-sync.yml`, `README-badges.md`, `legal/{privacy,terms,cookies}.html`.
**Establishment:** wired into `compliance.md`, the `CLAUDE.md` template (tooling rule +
back-merge release + legal notes-trigger), and all three lifecycle runbooks (setup /
onboard / adopt copy the new templates + enable branch protection). `hub/README.md` +
`templates/README.md` updated. RAP's `reports_through` in `.last-seen.yml` advanced past
the six digested reports. Jekyll build green. Out of scope: the fairyfox-games
setup-mirror gap (a different node's report) and actually enabling branch protection on
each node's GitHub repo (a node-side adoption action).

### 2026-07-02 — Maintenance round-up: reconcile to RAP 2.38.1 + Games 0.6.0, blog the 1st (0.11.2)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean on `dev`
beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched —
inbound report-review's job, not this pass's).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `c38373d → 2ed5787`, VERSION `2.35.1 → 2.38.1` (all commits
  dated 2026-07-01). No force-push.
- **fairyfox-games** — clean fast-forward `3cd3314 → 58a3bb0`, VERSION `0.5.1 → 0.6.0` (commit dated
  2026-07-01). A sixth game, Skyline.

**What the siblings shipped (2026-07-01).**

- **RAP 2.35.2–2.38.1.** A floor-up **CSS overhaul + theming framework** (Phases 0–7): the monolithic
  `styles.css` split into a modular `styles/` tree on a two-tier token system + cascade layers, a
  **System/Dark/Light** theme engine, **9 accent presets**, a header **Appearance picker**, built-in
  themes as a folder of JSON files, and **user theme import/export** (2.35.4→2.37.0). A **web-perf**
  pass — prompt corpus off the first-paint path (2.37.1), then the online build **prerendering its
  first paint to static HTML + hydration** (2.38.0; mobile Lighthouse Perf mid-50s→80s, A11y/BP/SEO
  stay 100). A **SonarCloud tech-debt sweep** — all **70 code smells** cleared incl. 7 ReDoS regex
  rewrites + 7 complexity refactors (2.38.1), the last bug/2 vulns resolved, and the quality-gate
  coverage measurement fixed. Plus a full dependency upgrade (2.35.2), a stylelint CSS gate (2.35.3),
  and a git-workflow back-merge (main→dev) fix + branch-sync guard.
- **Fairy Fox Games 0.6.0.** A mechanically-distinct sixth game **Skyline** (stack-and-slice tower
  builder; pure core + 18 tests, no timer-driven death), **Polarity** grown with clutch saves, and a
  backfilled Ricochet footer link. 133 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.38.1 / Jul 1; games → 0.6.0 / Jul 1; the
fairyfox.io meta card → 0.11.2 / Jul 2), `_data/pulse.yml` (ticker re-led with the 07-01 highlights,
`when` rolled, bars rolled), `_projects/random-ai-prompt.md` (added the themeable interface + online
prerender note), `_projects/fairyfox-games.md` (added Skyline as the sixth game).

**Verified accurate, left untouched:** `_data/downloads.yml` (RAP is source/run-only — no new pinned
binaries; PSE2/PSE1 releases unchanged; games has no downloads), `hub/registry.yml` (no version fields),
`_data/doc_categories.yml`, the `_docs/*` per-project pages (version-agnostic), `about.md` (lifecycle-based,
no hardcoded versions/counts), header/footer nav (registry-driven).

**Blogged.** `_posts/2026-07-01-a-theming-system-a-faster-first-paint-and-a-sixth-game.md` (tags
`random-ai-prompt, fairyfox-games, update`), day-centric. The site's own 07-01 change was the routine
maintenance bump (0.11.1) — not a notable feature — so no `site` tag this day. Markers advanced in
`hub/.last-seen.yml` (RAP → `2ed5787`; games → `58a3bb0`; PSE2 unchanged; all `seen` → 2026-07-02).
`reports_through` left alone — the new sibling process reports (RAP ×3) are for the inbound report-review
pass, not the round-up.

**Notes/version.** This changelog entry; `VERSION` `0.11.1 → 0.11.2`; today's session log; `status.md`
version line + sibling version numbers refreshed. Build-checked before shipping.

### 2026-07-01 — Maintenance round-up: reconcile to RAP 2.35.1 + Games 0.5.1, blog the 30th (0.11.1)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree was clean on
`dev` beforehand — the only untracked files were the `notes/fairyfox-reports/inbound/INBOX-*.md`
briefings from the automated inbound report-review procedure, left untouched (not this pass's
job).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326`
  (still `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward → `c38373d`, VERSION `2.28.18 → 2.35.1` (all commits
  dated 2026-06-30). No force-push.
- **fairyfox-games** — clean fast-forward → `3cd3314`, VERSION `0.4.0 → 0.5.1` (all commits dated
  2026-06-30). A fifth game (Ricochet) plus a depth pass across the rest.

**What the siblings shipped (2026-06-30).**

- **RAP 2.29.0–2.35.1.** Self-hosted **Privacy/Terms/Cookies** pages + local fonts ending the
  IP-to-Google flow (2.30.1); a **real release stage** for the local edition (`npm start`, backend
  factored out of the Vite dev plugin) + file-watch hot reload + atomic settings writes (2.32.0);
  **in-app dialogs** replacing native alert/confirm/prompt (2.34.0); a **seedable, deterministic
  engine** (real threaded PRNG, reproducible-from-seed) + async batch boundary (2.35.0); a **README
  revamp + SEO repositioning** away from SD-first and a plain-English **description alignment**
  everywhere (2.33.3/2.35.1); a header/provider UX batch (2.30.0–2.32.1), natural-language
  artists/styles + a `{#styles}` block (2.33.0), a unified cookie-free storage overhaul (2.29.0),
  and CI-side security/code-health integrations (Dependabot/CodeQL/Scorecard/Sonar/CodeRabbit +
  Codecov, no bump).
- **Fairy Fox Games 0.4.1–0.5.1.** A mechanically-distinct fifth game **Ricochet** (aim-and-fire one
  bouncing shot; pure `computeShot` core + tests), one on-mechanic depth/feedback addition to every
  game, landing-page polish, the retired "Fun" surface removed, and first-class-games standing rules.
  110 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.35.1 / Jun 30 + blurb aligned to the new
positioning; games → 0.5.1 / Jun 30; the stale **fairyfox.io meta card 0.9.14 → 0.11.1** / Jul 1),
`_data/downloads.yml` (RAP run command `npm run web` → `npm start`, the proper release stage added in
2.32.0), `_data/pulse.yml` (ticker re-led with the 06-30 highlights + `when` rolled + bars rolled),
`_projects/random-ai-prompt.md` (seedable/deterministic engine + privacy-by-design stance),
`_projects/fairyfox-games.md` (added Ricochet + the grow-daily note).

**Verified accurate, left untouched:** `_data/downloads.yml` PSE2/PSE1 tabs (unchanged releases),
`hub/registry.yml` (no version fields), `_data/doc_categories.yml`, the `_docs/*` per-project pages
(version-agnostic), `about.md`, header/footer nav (registry-driven).

**Blogged.** `_posts/2026-06-30-legal-pages-a-seedable-engine-and-a-fifth-game.md` (tags
`random-ai-prompt, fairyfox-games, fairyfox-io, site, update`), day-centric — also folds in the site's
own 2026-06-30 work (the 0.10.0 one-seamless-site standards milestone + the 0.11.0 submenu nav).
Round-up markers advanced in `hub/.last-seen.yml` (RAP → `c38373d`; games → `3cd3314`; PSE2 unchanged;
all `seen` → 2026-07-01). New sibling process reports left for the inbound report-review pass.

**Notes/version.** This changelog entry + July file created (index row added to `version.md`); today's
session log; `VERSION` `0.11.0 → 0.11.1`; `status.md` version line + sibling version numbers refreshed.
Build-checked before shipping.
